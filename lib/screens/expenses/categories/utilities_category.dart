import 'package:flutter/material.dart';

import 'expense_category.dart';

const utilitiesCategory = ExpenseCategoryDefinition(
  label: 'Utilities',
  category: 'Utilities',
  icon: ExpenseActionIcon.utilities,
  gradient: [Color(0xFF546EBC), Color(0xFF28346E)],
);
