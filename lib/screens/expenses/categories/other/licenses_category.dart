import 'package:flutter/material.dart';

import '../expense_category.dart';

const licensesCategory = ExpenseCategoryDefinition(
  label: 'Licenses',
  category: 'Licenses',
  icon: ExpenseActionIcon.registration,
  gradient: [Color(0xFF78909C), Color(0xFF37474F)],
  quickAction: false,
);
