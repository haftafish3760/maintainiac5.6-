import 'package:flutter/material.dart';

import '../expense_category.dart';

const postageCategory = ExpenseCategoryDefinition(
  label: 'Postage',
  category: 'Postage',
  icon: ExpenseActionIcon.registration,
  gradient: [Color(0xFF78909C), Color(0xFF37474F)],
  quickAction: false,
);
