// Compatibility export for older callers. The canonical live-trip odometer
// header is owned by the shared app screen shell so every app section uses the
// same vehicle-switch safety and live projection rendering.
export '../widgets/app_screen_shell.dart' show AppSection, GlobalOdometerHeader;
