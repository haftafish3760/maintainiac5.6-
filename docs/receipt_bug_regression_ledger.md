# Receipt Bug Regression Ledger

Every confirmed receipt camera, OCR, parser, fixture, or review-flow bug gets a
ledger row before the fix is considered closed. Keep entries privacy-safe: no
raw receipt text, no customer data, no VINs, no plates.
Older closed rows are archived in
`docs/receipt_bug_regression_ledger_archive_0001.md`; both active and archived
rows are still validated by `tool/receipt_bug_regression_ledger_gate.dart`.

Required fields for each entry:
- `Bug ID`
- `Category`
- `Symptom`
- `Root cause`
- `Fix`
- `Regression coverage`
- `Status`

Allowed categories:
- `camera_capture_contract`
- `camera_capture_quality`
- `multi_photo_ordering`
- `ghost_overlap_stitching`
- `source_preservation`
- `ocr_handoff_contract`
- `receipt_line_numbering`
- `receipt_line_review_mode`
- `business_personal_split`
- `parser_totals_math`
- `fixture_generation`
- `qa_harness`
- `privacy_redaction`
- `native_bridge`
- `barcode_qr_scanning`
- `camera_review_state`
- `camera_diagnostics`
- `camera_docs`
- `storage_recovery`

| Bug ID | Category | Symptom | Root cause | Fix | Regression coverage | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `BUG-RECEIPT-0498` | `native_bridge` | Native receipt camera sessions could rely on implicit default state to keep experimental blur/glare/low-light/shadow warnings off, leaving the bridge vulnerable to stale or future reordered argument loading that could resurrect noisy live warnings outside the experimental opt-in lane. | Android and iOS both depended on `experimentalReceiptQualityWarningFlag(...)` reading before or alongside policy assignment, instead of explicitly zeroing the experimental warning flags whenever the session policy was not experimental. | Move experimental warning reads behind the incoming `readabilityGuidancePolicy` assignment and add a native session-argument guard that forcibly clears all experimental live-quality warning flags unless the session is explicitly in the experimental opt-in policy. | `android/app/src/main/kotlin/com/maintainiac/ReceiptCameraSessionArguments.kt`; `ios/Runner/ReceiptCameraViewControllerSessionArguments.swift`; `test/receipt_native_android_guidance_policy_gate_test.dart`; `test/receipt_native_ios_guidance_warning_gate_test.dart` | `closed` |
| `BUG-RECEIPT-0497` | `qa_harness` | Native control diagnostics could report the long-receipt ghost guide as visible whenever a guide path existed, even if the guide panel itself stayed hidden because the image/path was invalid or the panel had not been rendered. | Android and iOS both used source-path presence instead of actual panel visibility when building `visibleControlSet()`. | Make the native visible-control diagnostics add `section_ghost_guide` only when the rendered guide panel is actually visible, and pin that rule in the bridge contracts. | `android/app/src/main/kotlin/com/maintainiac/ReceiptCameraControlReadiness.kt`; `ios/Runner/ReceiptCameraViewControllerLabels.swift`; `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0496` | `native_bridge` | Android and iPhone could show different active-settings strip wording for the same receipt camera state, which made the Phase 3 viewer feel inconsistent across platforms. | Android kept older `Price review` and `Long receipt on` wording while iOS had already moved to `Price-only lines` and `Long receipt`. | Align the Android settings status strip copy with iOS so the native receipt viewer reports the same active state labels on both platforms. | `android/app/src/main/kotlin/com/maintainiac/ReceiptCameraUiChrome.kt`; `test/receipt_native_android_bridge_ui_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0495` | `qa_harness` | Viewer diagnostics could still claim the status strip was visibly present even when the strip was hidden, making native control telemetry disagree with the actual camera chrome. | Android and iOS both left `"status"` hard-coded inside `visibleControlSet()` after the strip gained a real visibility policy. | Make the visible control set add `status` only when `shouldShowSettingsStatusStrip()` is true, and pin that behavior in the native UI bridge contracts. | `android/app/src/main/kotlin/com/maintainiac/ReceiptCameraControlReadiness.kt`; `ios/Runner/ReceiptCameraViewControllerLabels.swift`; `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0494` | `native_bridge` | The native viewer settings strip could correctly appear after settings changed but still start hidden on first open even when the camera launched with non-default or storage-constrained settings already active. | The visibility policy existed only in the post-build update methods, while the Android/iOS builders still hardcoded the initial strip state to hidden. | Make the Android and iOS status-strip builders honor `shouldShowSettingsStatusStrip()` immediately so the first rendered viewer matches the current camera settings state. | `android/app/src/main/kotlin/com/maintainiac/ReceiptCameraUiChrome.kt`; `ios/Runner/ReceiptCameraViewControllerLayout.swift`; `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0493` | `native_bridge` | The native receipt viewer built an “active settings” strip but could keep it permanently hidden because updates only replaced the text and never reevaluated visibility. | Android and iOS both created the status strip hidden by default and refreshed its copy after settings changes, but neither bridge had a visibility policy tied to non-default or storage-constrained camera settings. | Add a shared visibility rule on both native bridges so the settings status strip appears only when meaningful settings deviate from the default baseline or storage safety is active, and otherwise stays out of the preview. | `android/app/src/main/kotlin/com/maintainiac/ReceiptCameraUiChrome.kt`; `ios/Runner/ReceiptCameraViewControllerLabels.swift`; `ios/Runner/ReceiptCameraViewControllerCaptureClose.swift`; `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0492` | `native_bridge` | The iPhone receipt viewer could still lose preview space before any extra controls were visible because hidden status and ghost-guide panels were constrained as standalone blocks beneath guidance. | The iOS Phase 3 layout added the settings status strip and previous-section guide as separate constrained views, so hidden panels still preserved stacked layout space instead of collapsing cleanly. | Move the iOS settings status strip and previous-section guide into a vertical viewer-info stack so hidden arranged views collapse and the live preview keeps more space by default. | `ios/Runner/ReceiptCameraViewController.swift`; `ios/Runner/ReceiptCameraViewControllerLayout.swift`; `test/receipt_camera_phase3_viewer_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0491` | `native_bridge` | Experimental live quality warnings could still swap too quickly between adjacent signals because only two consecutive frames were required before replacing the viewer guidance text. | The Android/iOS viewer bridge used a minimal two-frame candidate threshold for experimental quality warnings, which was enough for unstable scene noise or monitor-based receipt demos to feel like a rotating guidance billboard. | Raise the experimental live-quality guidance threshold to three stable frames and expose that threshold in diagnostics/contracts so the viewer warning path stays deliberately conservative. | `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_exposure_shutter_contract_test.dart`; `test/receipt_camera_release_control_priority_test.dart`; `android/app/src/main/kotlin/com/maintainiac/ReceiptCameraDiagnosticsPayload.kt`; `ios/Runner/ReceiptCameraViewControllerDiagnostics.swift` | `closed` |
| `BUG-RECEIPT-0490` | `qa_harness` | Broad native bridge diagnostics and source contracts still claimed review-next lived in both the top bar and bottom action strip after the Phase 3 camera viewer was cleaned up to bottom-only handoff. | The focused viewer bundle removed the visible top Done control, but the broader Android/iOS diagnostics payloads and contract tests still advertised the stale `top_and_bottom_after_capture` placement string. | Update the Android/iOS diagnostics payloads and the broad bridge contracts to report `bottom_only_after_capture` so the viewer milestone, telemetry labels, and regression suite agree on the real review handoff. | `android/app/src/main/kotlin/com/maintainiac/ReceiptCameraDiagnosticsPayload.kt`; `ios/Runner/ReceiptCameraViewControllerDiagnostics.swift`; `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0489` | `native_bridge` | The live receipt camera could show a duplicate top-bar Done action even though review handoff already belonged on the bottom action strip, wasting preview space and crowding the capture UI. | Both native camera layouts kept a legacy top Done control alive alongside the bottom review action, and readiness logic still treated either location as acceptable instead of enforcing the cleaner bottom-only handoff. | Remove the visible top Done control from Android and iOS camera chrome, keep the bottom review button as the single review handoff, and tighten readiness contracts to track the bottom review action only. | `test/receipt_camera_phase3_viewer_contract_test.dart`; `test/receipt_native_android_bridge_close_controls_test.dart`; `test/receipt_native_android_bridge_capture_quality_contract_test.dart`; `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_close_capture_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0488` | `native_bridge` | Android or iPhone live receipt guidance could escalate bogus shadow, glare, or haze warnings before the camera had a trustworthy framed receipt target. | Experimental live quality warnings only required any usable framing bounds, so bright screens or weak edge hits could still reach the readability-warning path even when the receipt target was not reliable enough for those claims. | Keep framing guidance active, but gate experimental live quality warnings behind a stronger framed-target check: edge detection on, usable bounds, no edge touch, sufficient size, and strong/usable edge confidence. | `test/receipt_native_android_bridge_false_positive_guard_test.dart`; `test/receipt_native_ios_bridge_false_positive_guard_test.dart`; `docs/receipt_camera_completion_map.md`; `docs/receipt_camera_ocr_master_pass_plan.md` | `closed` |
| `BUG-RECEIPT-0487` | `qa_harness` | A physical iPhone receipt-camera build could bundle the stale x86_64 `objective_c` native asset from the wrong checkout and crash before the camera flow even launched. | Ignored `.dart_tool` and `build` artifacts inside the active 5.6 checkout still pointed at the older `/Users/rbbie/Documents/Maintainiac_5.6` native-asset outputs, so the device bundle picked up the simulator-style `objective_c.framework` instead of the fresh arm64 iPhone asset. | Clear generated artifacts, rebuild the iPhone bundle from the active checkout, and add an explicit iOS native-asset preflight that verifies `NativeAssetsManifest.json` advertises `ios_arm64` and that `objective_c.framework/objective_c` contains `arm64` before real-device QA. | `tool/receipt_camera_ios_native_asset_preflight.sh`; `test/receipt_camera_ios_native_asset_preflight_contract_test.dart`; `docs/receipt_real_device_test_script.md`; `docs/receipt_camera_completion_map.md` | `closed` |
| `BUG-RECEIPT-0486` | `qa_harness` | Edits to the camera QA execution contract were routed to milestone validation but not covered by a cheap fast-guard print-mode smoke. | The execution contract was in the high-sensitivity changed-gate route, but omitted from reusable route-smoke expectations. | Add a fast-guard print-mode smoke for `receipt_camera_qa_gate_execution_test.dart` and pin it in route coverage plus fast-guard contract tests. | `tool/receipt_fast_guard_gate.sh`; `tool/receipt_camera_changed_route_coverage_gate.dart`; `test/receipt_fast_guard_gate_contract_test.dart`; `docs/receipt_bug_regression_ledger.md` | `closed` |
| `BUG-RECEIPT-0485` | `qa_harness` | Edits to the fast-guard contract test could run quick changed-file validation even though that test owns reusable fast QA wiring. | `receipt_fast_guard_gate_contract_test.dart` was allowed by scope and run by fast guard, but missing from changed-gate milestone routes and route-smoke coverage. | Route fast-guard contract edits to milestone mode and add fast-guard print-mode smoke coverage. | `tool/receipt_camera_changed_route_coverage_gate.dart`; `tool/receipt_camera_changed_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `test/receipt_fast_guard_gate_contract_test.dart`; `docs/receipt_bug_regression_ledger.md` | `closed` |
| `BUG-RECEIPT-0484` | `qa_harness` | The focused changed-gate contract test was routed to milestone validation but not cheaply smoke-checked by fast guard. | Pass 1110 split the changed-gate contract but only added the changed-gate route, leaving the reusable route coverage gate without a fast-guard smoke expectation for the new focused test. | Add a fast-guard print-mode smoke for `receipt_camera_changed_gate_contract_test.dart` and pin it in route coverage plus fast-guard contract tests. | `tool/receipt_fast_guard_gate.sh`; `tool/receipt_camera_changed_route_coverage_gate.dart`; `test/receipt_fast_guard_gate_contract_test.dart`; `docs/receipt_bug_regression_ledger.md` | `closed` |
| `BUG-RECEIPT-0483` | `qa_harness` | Broad camera QA contract coverage was drifting toward the file line cap while also owning changed-gate policy assertions. | Changed-gate script invariants lived in `receipt_camera_qa_gate_contract_test.dart` instead of a focused contract, making future QA-harness additions more likely to exceed the line cap. | Move changed-gate policy assertions into `receipt_camera_changed_gate_contract_test.dart` and wire the focused test into quick camera QA, fast guard, changed gate, and route coverage. | `test/receipt_camera_changed_gate_contract_test.dart`; `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_qa_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `tool/receipt_camera_changed_gate.sh`; `tool/receipt_camera_changed_route_coverage_gate.dart` | `closed` |
| `BUG-RECEIPT-0482` | `qa_harness` | The new changed-route coverage gate could be removed from quick/fast validation without the older broad QA contract tests naming it directly. | Pass 1107 added the executable route gate and focused regression, but the legacy camera QA and fast-guard contract tests did not explicitly pin the new tool, test, and smoke route. | Add broad contract assertions for the route coverage gate tool, Flutter regression, executable command, and fast-guard print-mode smoke. | `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_fast_guard_gate_contract_test.dart`; `docs/receipt_bug_regression_ledger.md` | `closed` |
| `BUG-RECEIPT-0481` | `qa_harness` | Edits to the fast guard or camera scope gate could run quick changed-file validation even though those scripts decide what gets checked and what stays in camera scope. | The changed-gate high-sensitivity route covered most camera QA scripts but missed `receipt_fast_guard_gate.sh` and `receipt_camera_scope_gate.sh`. | Route fast/scope gate edits to milestone mode and pin them in the reusable changed-route coverage gate plus fast-guard print-mode smokes. | `tool/receipt_camera_changed_route_coverage_gate.dart`; `tool/receipt_camera_changed_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `docs/receipt_bug_regression_ledger.md` | `closed` |
| `BUG-RECEIPT-0480` | `qa_harness` | Future changed-gate route drift required manual script rereads to discover missing milestone routes or fast-guard smoke coverage. | Route coverage was asserted in scattered Flutter contract expectations instead of one executable reusable gate. | Add `receipt_camera_changed_route_coverage_gate.dart`, regression coverage, and wire it into quick camera QA, fast guard, changed gate, and scope. | `tool/receipt_camera_changed_route_coverage_gate.dart`; `test/receipt_camera_changed_route_coverage_gate_test.dart`; `tool/receipt_camera_qa_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `tool/receipt_camera_changed_gate.sh`; `tool/receipt_camera_scope_gate.sh` | `closed` |
| `BUG-RECEIPT-0479` | `qa_harness` | Edits to the external fixture schema Flutter regression test could run only quick changed-gate validation even though the test proves fixture intake policy. | The schema test was wired into quick camera QA, fast guard, and scope, but the changed-file route only covered the executable fixture schema tool. | Route `receipt_external_fixture_schema_gate_test.dart` edits to milestone mode and add fast-guard print-mode smoke coverage. | `tool/receipt_camera_changed_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `test/receipt_camera_dataset_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart`; `test/receipt_fast_guard_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0478` | `qa_harness` | Edits to the focused external dataset QA contract could run only quick changed-gate validation after the contract became the owner of dataset QA wiring assertions. | Pass 1104 wired the focused dataset QA contract into quick camera QA, fast guard, and scope, but did not route edits to that test through milestone changed-file validation. | Route `receipt_camera_dataset_qa_gate_contract_test.dart` edits to milestone mode and add fast-guard print-mode smoke coverage. | `tool/receipt_camera_changed_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `test/receipt_camera_dataset_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart`; `test/receipt_fast_guard_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0477` | `qa_harness` | External dataset QA wiring assertions were spread across broad camera QA contracts instead of having a focused owner. | Dataset-specific script, scope, changed-gate, and fast-guard assertions accumulated in shared camera QA contract files as the QA harness grew. | Add `receipt_camera_dataset_qa_gate_contract_test.dart` as the focused dataset QA wiring owner and wire it into quick camera QA, fast guard, and scope. | `test/receipt_camera_dataset_qa_gate_contract_test.dart`; `tool/receipt_camera_qa_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `tool/receipt_camera_scope_gate.sh` | `closed` |
| `BUG-RECEIPT-0476` | `qa_harness` | Edits to the main external dataset manifest gate or its Flutter regression test could run only quick changed-gate validation despite owning dataset safety policy. | The changed-gate QA-harness route included the local dataset audit but not `tool/receipt_external_dataset_gate.dart` or `test/receipt_external_dataset_gate_test.dart`. | Route external dataset gate edits to milestone mode and add fast-guard print-mode smoke coverage for the tool route. | `tool/receipt_camera_changed_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `test/receipt_camera_qa_gate_execution_test.dart`; `test/receipt_fast_guard_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0475` | `qa_harness` | Edits to the local external dataset audit script or test could run only quick changed-gate validation after becoming part of the reusable camera QA harness. | The changed-gate QA-harness route did not include the new local dataset audit tool and regression test. | Route local dataset audit edits to milestone mode and add fast-guard print-mode smoke coverage for the tool route. | `tool/receipt_camera_changed_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `test/receipt_camera_qa_gate_execution_test.dart`; `test/receipt_fast_guard_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0474` | `qa_harness` | Local Hugging Face or third-party receipt datasets could be downloaded under the ignored dataset root without an automated license/attribution audit before fixture use. | The external dataset manifest and gate protected metadata and Git tracking, but no reusable script inspected optional local dataset folders when present. | Add a local-only dataset audit that skips absent datasets, fails present datasets without license and attribution files, and wire it into camera QA and fast guard. | `tool/receipt_external_dataset_local_audit.dart`; `test/receipt_external_dataset_local_audit_test.dart`; `tool/receipt_camera_qa_gate.sh`; `tool/receipt_fast_guard_gate.sh` | `closed` |
| `BUG-RECEIPT-0473` | `qa_harness` | The external receipt fixture schema gate had script-level execution coverage but no direct Flutter regression test in the reusable camera QA pack. | The camera QA gate ran `tool/receipt_external_fixture_schema_gate.dart` directly, while only the dataset gate had a dedicated executable Flutter test. | Add `test/receipt_external_fixture_schema_gate_test.dart` and wire it into quick camera QA, fast guard, scope, and plan contracts. | `test/receipt_external_fixture_schema_gate_test.dart`; `tool/receipt_camera_qa_gate.sh`; `tool/receipt_fast_guard_gate.sh`; `tool/receipt_camera_scope_gate.sh` | `closed` |
| `BUG-RECEIPT-0472` | `qa_harness` | Camera QA quick mode classified the Dart external fixture schema gate as a shell syntax target. | Pass 1096 added `tool/receipt_external_fixture_schema_gate.dart` to both Dart validation and the shell `bash -n` script list. | Remove the Dart gate from shell syntax checking while keeping Dart analyze/run coverage, and pin the contract. | `tool/receipt_camera_qa_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0471` | `qa_harness` | Fast receipt guard could pass without cheaply proving the external fixture schema gate changed-file route. | Pass 1097 added milestone routing for fixture schema gate edits, but fast guard did not smoke that route. | Add a print-only fast-guard changed-gate smoke for `tool/receipt_external_fixture_schema_gate.dart` and pin it in the fast guard contract. | `tool/receipt_fast_guard_gate.sh`; `test/receipt_fast_guard_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0470` | `qa_harness` | Edits to the external fixture schema gate could run only quick changed-gate validation after the gate became part of reusable camera QA. | The changed-gate QA-harness route did not include `tool/receipt_external_fixture_schema_gate.dart`. | Route external fixture schema gate edits to milestone mode and add executable changed-gate coverage for that path. | `tool/receipt_camera_changed_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0469` | `qa_harness` | Reusable camera QA could pass without running the external receipt fixture schema gate, even though fast guard already checked it. | The fixture schema gate was wired into broader fast guard validation but not the camera QA quick path used for focused camera-lane validation. | Add the external fixture schema gate to camera QA quick syntax, analysis, and executable gate steps, and allow the gate file in camera scope. | `tool/receipt_camera_qa_gate.sh`; `tool/receipt_camera_scope_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0468` | `qa_harness` | External receipt dataset metadata could drift from the local-only fixture-import policy or omit the SROIE candidate needed for future receipt OCR/parser QA discovery. | The executable dataset gate checked raw-image and attribution policy fields but left `fixtureImportMode` enforced only by the Flutter test, and the Hugging Face SROIE source was not listed for later local review. | Require local sampled redacted metadata-only import mode in the executable gate, add SROIE as candidate metadata only, and correct the Voxel51 source handle. | `tool/receipt_external_dataset_gate.dart`; `test/receipt_external_dataset_gate_test.dart`; `test/fixtures/receipt_qa/external_dataset_manifest.json` | `closed` |
| `BUG-RECEIPT-0467` | `qa_harness` | Fast receipt guard could pass without proving quiet-batch helper edits route through the changed gate at milestone depth. | The fast guard smoked only a contract-test override and the reusable QA gate script override after quiet-batch helpers joined the camera QA harness list. | Add a print-only fast-guard smoke for `tool/receipt_quiet_batch_status.sh` changed-file routing and pin it in the fast guard contract. | `tool/receipt_fast_guard_gate.sh`; `test/receipt_fast_guard_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0466` | `qa_harness` | Camera QA quiet-batch helper edits could be classified as milestone changes by the changed gate but still fail the camera scope gate before the route ran. | The changed-gate harness list and camera scope allowlist drifted after quiet-batch helpers became part of reusable camera QA automation. | Allow quiet-batch helper scripts in the camera scope gate and pin changed-gate milestone routing for a quiet-batch helper edit. | `tool/receipt_camera_scope_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0465` | `qa_harness` | The fast receipt guard did not directly smoke the changed-gate route for edits to the reusable camera QA gate itself. | Pass 1090 added milestone routing for camera QA harness files, but the fast guard still only exercised a contract-test path override. | Add a print-only fast-guard smoke for `tool/receipt_camera_qa_gate.sh` changed-file routing and pin it in the fast guard contract. | `tool/receipt_fast_guard_gate.sh`; `test/receipt_fast_guard_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0464` | `qa_harness` | Editing the reusable camera QA harness itself could fall through to default quick validation instead of an explicit harness-depth route. | `receipt_camera_changed_gate.sh` routed native, review, handoff, stitch, and OCR-source paths, but did not classify camera QA scripts/tests as a higher-risk harness change. | Route camera QA harness scripts and tests to milestone mode and add executable changed-gate coverage for QA gate and execution-test edits. | `tool/receipt_camera_changed_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0463` | `qa_harness` | Hugging Face/CORD receipt dataset intake could be added as metadata without explicitly pinning attribution, raw-image Git exclusion, or local sampled fixture import mode per dataset. | The external dataset manifest required local paths and license files, but did not require per-dataset attribution and raw-image policy fields. | Require source URL, no traversal, `rawImagesCommittedToGit=false`, `attributionRequired=true`, and local redacted fixture import mode; add the official Hugging Face CORD v2 source as metadata only. | `tool/receipt_external_dataset_gate.dart`; `test/receipt_external_dataset_gate_test.dart`; `test/fixtures/receipt_qa/external_dataset_manifest.json` | `closed` |
| `BUG-RECEIPT-0462` | `qa_harness` | Detached camera QA summaries could dump the last 80 log lines back into Codex when a failed batch had no recognized actionable pattern. | The summary fallback printed a raw log tail instead of reporting the saved log path for later inspection. | Replace the noisy fallback with a concise `log_path` summary and add process-level coverage proving neutral log text is not printed. | `tool/receipt_camera_qa_summary.sh`; `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0461` | `qa_harness` | Camera QA contract tests did not explicitly pin single-owner stitch QA wiring, so a future edit could reintroduce a second handwritten stitch test list after the de-duplication fix. | The execution test proved the legacy stitch gate delegates, but the source contract did not require the reusable `stitch_tests` pack or reject direct Flutter execution from the legacy script. | Add contract assertions for the shared `stitch_tests` pack, printed stitch plan, stitch Flutter execution, and legacy stitch-gate delegation. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_qa_gate.sh`; `tool/receipt_camera_stitch_gate.sh` | `closed` |
| `BUG-RECEIPT-0460` | `qa_harness` | Focused stitch QA still had a second handwritten test list inside `receipt_camera_stitch_gate.sh`, so future long-receipt regressions could be added to one stitch entry point but not the other. | The reusable camera QA gate and the legacy stitch gate each owned their own analyze/test path list. | Move the stitch test pack into `receipt_camera_qa_gate.sh` and make `receipt_camera_stitch_gate.sh` delegate to the reusable stitch mode. | `tool/receipt_camera_qa_gate.sh`; `tool/receipt_camera_stitch_gate.sh`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0459` | `qa_harness` | Stitch QA print-plan output could omit `receipt_photo_section_labels_test.dart` even though the focused stitch gate executed it, making non-interactive QA summaries under-report real coverage. | `receipt_camera_qa_gate.sh --print-plan stitch` duplicated the stitch test list manually and drifted from `receipt_camera_stitch_gate.sh`. | Add the missing photo-section label test to the printed stitch plan and add an executable comparison between the printed plan and the focused stitch gate's Flutter test list. | `tool/receipt_camera_qa_gate.sh`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0458` | `qa_harness` | Full camera QA could skip the focused photo-review retake-order regression even though stitch mode ran it, leaving long-receipt replacement/order bugs outside milestone and full validation. | The reusable camera QA gate listed `receipt_photo_review_retake_order_test.dart` only in stitch mode, while milestone/full mode expanded quick plus milestone/full-only packs. | Add the retake-order regression to milestone/full camera QA and teach the executable full-plan audit to require it. | `tool/receipt_camera_qa_gate.sh`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0457` | `native_bridge` | iOS receipt camera configured Add Photo and bottom Next review controls, but they were not added to the bottom bar and were hidden after capture, leaving long-receipt continuation and review actions undiscoverable. | The iOS layout created the controls without adding them to the visible bottom stack, while `updateDoneButton()` forced the add/review controls hidden even after photos existed. | Add the Add Photo and bottom Next controls to the iOS bottom bar, reveal them only after a captured section exists, and pin approved accessibility labels. | `test/receipt_native_ios_bridge_close_capture_test.dart` | `closed` |
| `BUG-RECEIPT-0456` | `qa_harness` | Full camera QA did not include every existing camera/native/stitch/OCR-source test, so a final full run could miss camera-lane regressions already covered elsewhere. | `receipt_camera_qa_gate.sh` had curated quick/milestone packs but no contract proving full mode covered all camera-specific test files on disk. | Add the missing camera/native/stitch/OCR-source tests to full mode and add an executable plan audit that compares the full plan against current test files. | `tool/receipt_camera_qa_gate.sh`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0455` | `qa_harness` | Camera quick QA could skip the real-device metadata snapshot contract, allowing future edits to add unsafe device control before full QA noticed. | `test/receipt_camera_real_device_snapshot_contract_test.dart` existed but was not part of the reusable camera quick pack. | Add the real-device metadata snapshot contract to quick camera QA and pin it in executable plan coverage. | `tool/receipt_camera_qa_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0454` | `multi_photo_ordering` | Uploaded long-receipt screenshot ordering regressions were allowlisted but not executed by the reusable camera quick/stitch QA gates. | `test/receipt_photo_section_labels_test.dart` covered selected screenshot order, but the active camera QA and stitch scripts did not run it. | Add the uploaded photo-section ordering regression to camera quick mode and the focused stitch gate. | `tool/receipt_camera_qa_gate.sh`; `tool/receipt_camera_stitch_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0453` | `qa_harness` | Camera QA quick mode ran the external dataset script but skipped the executable Flutter regression test that proves the manifest and gate behavior. | `test/receipt_external_dataset_gate_test.dart` was wired into the fast guard but not the reusable camera QA quick test pack. | Add the external dataset gate test to the camera QA quick pack and pin it in the plan/execution contracts. | `tool/receipt_camera_qa_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_camera_qa_gate_execution_test.dart` | `closed` |
| `BUG-RECEIPT-0452` | `qa_harness` | The camera QA gate could pass without checking the local-only external receipt dataset guard, even though the fast guard covered it. | `receipt_external_dataset_gate.dart` was wired into the broader fast guard but not the reusable camera QA gate used for camera-lane validation. | Add the external dataset gate to the camera QA quick path and pin that wiring in the camera QA gate contract. | `tool/receipt_camera_qa_gate.sh`; `test/receipt_camera_qa_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0451` | `ocr_handoff_contract` | Receipt stitch input, OCR-source, overlap, and pair lists could be changed after result construction by mutating the caller-owned lists, even though getters returned unmodifiable views. | `ReceiptStitchResult` stored constructor list references directly, so OCR-source contract evidence could drift after review. | Freeze stitch result lists at construction time and update model regressions to prove caller-side mutations no longer change the handoff. | `test/receipt_stitching_result_contract_test.dart`; `lib/shared/widgets/receipt_capture/receipt_capture_stitch_models.dart` | `closed` |
| `BUG-RECEIPT-0450` | `ghost_overlap_stitching` | A default or unset manual overlap pixel value of `0` could force long-receipt stitching into a manual-overlap unsafe fallback instead of allowing automatic overlap matching. | Manual overlap fractions treated `0` as no override, but manual overlap pixels returned `0` directly and failed the safe overlap range check. | Treat non-positive manual overlap pixel values as absent so automatic stitching can handle the pair unless the user provides a real positive override. | `test/receipt_stitching_manual_overlap_test.dart`; `lib/shared/widgets/receipt_capture/receipt_image_processor_stitch_helpers.dart` | `closed` |
| `BUG-RECEIPT-0449` | `qa_harness` | A detached camera QA batch with stale running metadata could be reported as still running, forcing manual inspection instead of giving a restart action. | `receipt_camera_qa_summary.sh` called the quiet-batch status helper but then reread `status.txt` directly, bypassing the helper's stale-process detection. | Parse the quiet-batch status helper output as the source of truth and emit a restart command for stale camera QA batches without tailing stale logs. | `test/receipt_camera_qa_gate_execution_test.dart`; `tool/receipt_camera_qa_summary.sh` | `closed` |
| `BUG-RECEIPT-0448` | `qa_harness` | Third-party receipt datasets could be discussed or downloaded locally without a repo guard proving raw images stay out of Git and license/privacy status is explicit before fixture ingestion. | The external fixture schema planned real samples, but there was no dedicated local dataset manifest or executable gate for Hugging Face/CORD-style receipt dataset sources. | Add a local-only external dataset manifest, ignore raw dataset storage, wire an executable dataset gate into the fast guard, and cover it with regression tests. | `tool/receipt_external_dataset_gate.dart`; `test/receipt_external_dataset_gate_test.dart`; `test/fixtures/receipt_qa/external_dataset_manifest.json` | `closed` |
| `BUG-RECEIPT-0447` | `qa_harness` | The camera scope gate rejected legitimate receipt photo-section review tests while hardening uploaded long-receipt screenshot ordering. | `test/receipt_photo_section_labels_test.dart` covers shared receipt photo ordering labels and picker behavior, but the camera-lane scope allowlist did not include it. | Allowlist the focused receipt photo-section test and pin the scope gate contract so future uploaded/camera ordering hardening stays inside the camera lane. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_scope_gate.sh` | `closed` |
| `BUG-RECEIPT-0446` | `multi_photo_ordering` | Uploaded receipt screenshot order could be mutated after picker normalization and before long-receipt review/stitching. | `ReceiptPickedPhotoSet.paths` exposed the mutable list produced by normalization, so later UI code could accidentally reorder or append screenshots outside the explicit review controls. | Store picked receipt image paths as an unmodifiable ordered list while preserving null/blank filtering and trim normalization. | `test/receipt_photo_section_labels_test.dart`; `lib/shared/widgets/receipt_capture/receipt_image_picker.dart` | `closed` |
| `BUG-RECEIPT-0445` | `multi_photo_ordering` | Uploaded long-receipt screenshots could lose user-selected top/middle/bottom ordering if the picker wrapper later filtered or normalized paths unsafely. | Existing upload wording checks only verified `pickMultiImage` existed, not that `ReceiptPickedPhotoSet.fromFiles` preserves non-empty paths in selection order. | Add a behavior regression for uploaded receipt image sets that trims paths, drops null/blank picks, and preserves the remaining screenshot order. | `test/receipt_photo_section_labels_test.dart`; `lib/shared/widgets/receipt_capture/receipt_image_picker.dart` | `closed` |
| `BUG-RECEIPT-0444` | `ghost_overlap_stitching` | Uploaded/gallery long-receipt screenshots could later bypass stitching if the review flow were tied to native camera metadata instead of the reviewed photo list. | Existing guidance coverage proved camera long-receipt controls existed, but did not pin that any non-best-shot multi-photo review list uses the shared stitch/OCR-source path. | Add a source contract that multi-photo review requires stitch review from `_photoPaths.length > 1` and routes final OCR through `ReceiptImageProcessor.stitchReceiptPhotosForOcr` without camera-source gating. | `test/receipt_camera_long_receipt_guidance_test.dart`; `lib/shared/widgets/receipt_capture/receipt_photo_review_exit_stitch_actions.dart` | `closed` |
| `BUG-RECEIPT-0443` | `qa_harness` | Native camera guidance could regress into fake rotating blur/glare/shadow warning copy instead of evidence-backed receipt guidance. | Existing policy tests proved experimental quality warnings were gated, but did not ban timer/carousel/random guidance patterns that could display unearned warning messages. | Add Android and iOS native source contracts rejecting rotating guidance mechanisms and proving the neutral receipt guidance toggle cannot enable experimental quality-warning families. | `test/receipt_native_android_guidance_policy_gate_test.dart`; `test/receipt_native_ios_guidance_warning_gate_test.dart` | `closed` |
| `BUG-RECEIPT-0442` | `qa_harness` | Detached receipt camera QA summaries were source-inspected but not executed against real completed pass/fail quiet-batch folders. | The contract verified summary script strings but did not prove that a passed batch exits cleanly or that a failed batch prints actionable lines plus a regression-task command. | Add process-level summary coverage using temporary quiet-batch folders for passed and failed camera QA outcomes. | `test/receipt_camera_qa_gate_execution_test.dart`; `tool/receipt_camera_qa_summary.sh` | `closed` |
| `BUG-RECEIPT-0441` | `qa_harness` | The active receipt bug ledger had only a few lines of headroom before future regression rows would break the project line-count cap. | All closed bug rows stayed in one active markdown file, and the gate validated only that single file instead of allowing archived history. | Add an archive utility, validate archive files in the ledger gate, cap the active ledger below the source limit, and move older closed rows into the archive without losing history. | `test/receipt_bug_regression_ledger_archive_test.dart`; `tool/receipt_bug_regression_ledger_archive.dart`; `tool/receipt_bug_regression_ledger_gate.dart` | `closed` |
| `BUG-RECEIPT-0440` | `qa_harness` | Camera QA `--print-plan` could advertise stale test paths without failing until a later QA run tried to execute the missing file. | The executable plan contract verified representative mode output but did not prove that every printed quick, stitch, milestone, and full test path exists on disk. | Add a process-level plan existence regression that runs every printed camera QA mode plan and fails on missing test paths. | `test/receipt_camera_qa_gate_execution_test.dart`; `tool/receipt_camera_qa_gate.sh` | `closed` |
| `BUG-RECEIPT-0439` | `privacy_redaction` | Fast receipt guard failed Firestore redaction/docs contracts because newer OCR source coverage, continuation, section-order, and native capture-readiness count maps were missing from redacted map field documentation/source allowlists. | `expectedExpenseTelemetryRedactedMapFields` already required the fields, but `_ExpenseTelemetryFirestoreRedactor` and active docs had not been updated with the newer safe count-map names. | Add the missing OCR source count-map fields to the Firestore redactor and add OCR/native readiness count-map names to the active OCR/Firebase sync contracts. | `test/expense_telemetry_redaction_contract_guard_test.dart`; `test/firestore_data_model_guard_test.dart`; `lib/shared/firebase/maintainiac_firestore_expense_telemetry_redactor.dart`; `docs/expense_command_center_ocr_contract.md`; `docs/firebase_sync_receipt_expense_schema_spec.md` | `closed` |
| `BUG-RECEIPT-0438` | `qa_harness` | Fast receipt guard could fail inside a print-only camera changed-gate smoke when broader receipt/expense files were tracked. | `receipt_camera_changed_gate.sh` always ran the real camera scope gate before honoring `RECEIPT_CAMERA_CHANGED_FILES_FOR_TEST`, so injected route tests still inspected unrelated working-tree changes. | Run the camera scope gate only for real tracked-file routing, and keep test-injected route checks isolated from the current worktree. | `test/receipt_camera_qa_gate_execution_test.dart`; `tool/receipt_camera_changed_gate.sh`; `tool/receipt_fast_guard_gate.sh` | `closed` |
| `BUG-RECEIPT-0437` | `qa_harness` | Fast receipt guard failed tests-only source audit because `test/receipt_ocr_service_test.dart` exceeded the 500-line cap. | OCR service regressions accumulated in one file until the test file reached 567 lines. | Split review-focused OCR service regressions into `receipt_ocr_service_review_contract_test.dart` and wire the new file into the receipt quality gate. | `test/receipt_ocr_service_test.dart`; `test/receipt_ocr_service_review_contract_test.dart`; `tool/receipt_quality_gate.sh`; `test/receipt_quality_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0436` | `qa_harness` | Fast receipt guard failed source audit because expense telemetry health snapshot had compact field lines longer than the 220-character source-audit limit. | The DTO keeps `dart format` off to stay under the 500-line file cap, but three compact field declaration lines drifted past the max line-length guard. | Wrap only the over-limit field declarations while preserving behavior and the compact DTO structure. | `tool/receipt_fast_guard_gate.sh`; `dart tool/maintainiac_source_audit.dart --max-line-length=220`; `lib/screens/expenses/data/expense_screen_telemetry_health_snapshot.dart` | `closed` |
| `BUG-RECEIPT-0435` | `qa_harness` | Camera QA contract coverage was approaching the file line cap and mixing source-inspection contracts with executable process checks. | Newly added route/plan/failure-wrapper execution tests lived in the broad contract file, making future QA additions likely to exceed the cap. | Split executable camera QA checks into `receipt_camera_qa_gate_execution_test.dart` and wire it into quick camera QA plus the fast guard. | `test/receipt_camera_qa_gate_execution_test.dart`; `test/receipt_camera_qa_gate_contract_test.dart`; `test/receipt_fast_guard_gate_contract_test.dart`; `tool/receipt_camera_qa_gate.sh`; `tool/receipt_fast_guard_gate.sh` | `closed` |
| `BUG-RECEIPT-0434` | `qa_harness` | Camera QA `--print-plan stitch` overstated coverage by listing quick tests even though stitch mode runs only the focused stitch gate. | The new plan printer emitted the quick pack before branching by mode, so the stitch plan did not match `run_stitch`. | Move quick-plan output into quick/milestone/full modes only and assert stitch plans contain no quick, milestone, or full pack rows. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_qa_gate.sh` | `closed` |
| `BUG-RECEIPT-0433` | `qa_harness` | Camera QA failure-to-regression wiring could be source-inspected without proving a failed camera log creates a durable regression task. | The camera QA contract verified wrapper strings but did not execute `receipt_camera_failure_to_regression.sh` against a real temp failure log. | Add a process-level wrapper regression that converts a `camera_stitch_gate` failure log into a stitch regression task with the correct family and ledger category. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_failure_to_regression.sh` | `closed` |
| `BUG-RECEIPT-0432` | `qa_harness` | The fast receipt guard could pass without proving the camera changed-gate selector and QA-plan printer were executable. | Fast guard syntax-checked camera scripts and ran the broader contract test, but did not directly smoke the print-only route/plan automation that prevents manual rereads. | Add print-only changed-gate and QA-plan smokes to the fast guard, with contract coverage for quick, stitch, milestone, and full plan modes. | `test/receipt_fast_guard_gate_contract_test.dart`; `tool/receipt_fast_guard_gate.sh` | `closed` |
| `BUG-RECEIPT-0431` | `qa_harness` | Camera QA mode pack contents could drift while the contract relied on source-string inspection instead of executing the gate's own mode planner. | `receipt_camera_qa_gate.sh` had runnable modes but no non-running plan output for tests to verify quick, stitch, milestone, and full coverage without launching QA. | Add `--print-plan` to the camera QA gate and process-level contract coverage for each mode's expected test pack boundaries. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_qa_gate.sh` | `closed` |
| `BUG-RECEIPT-0430` | `qa_harness` | Camera changed-gate mode selection was source-inspected but not executable without dirtying the worktree. | Contract coverage checked script text for `quick`, `stitch`, `milestone`, and `full`, but did not run the selector against representative camera-lane file paths. | Add a test-only changed-file override and process-level coverage for quick, stitch, milestone, and full mode selection. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_changed_gate.sh` | `closed` |
| `BUG-RECEIPT-0429` | `qa_harness` | Detached camera QA starter mode validation was only source-inspected, not exercised as a process-level contract. | The contract test checked that allowed mode text existed but did not prove an invalid mode exits before launching a quiet batch. | Add a process-level regression that calls `receipt_start_camera_qa_gate.sh` with an invalid mode and requires usage output plus exit code 64. | `test/receipt_camera_qa_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0428` | `qa_harness` | Editing the focused stitch QA script itself could run only quick camera QA instead of exercising the stitch regression pack. | `receipt_camera_changed_gate.sh` routed stitch source and stitch tests to `stitch`, but omitted `tool/receipt_camera_stitch_gate.sh`, so changes to the gate could be syntax-checked without running the pack it controls. | Route tracked edits to `tool/receipt_camera_stitch_gate.sh` through focused stitch QA. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_changed_gate.sh` | `closed` |
| `BUG-RECEIPT-0427` | `qa_harness` | Camera QA scope guard could reject legitimate camera failure-regression task generator edits. | Camera QA now depends on `receipt_pipeline_failure_to_regression.dart` for stitch/camera failure tasks, but the scope allowlist only covered camera-named tools and missed the shared generator plus its contract test. | Add explicit camera-scope allowlist entries for the regression-task generator and its test while keeping unrelated untracked files ignored. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_scope_gate.sh` | `closed` |
| `BUG-RECEIPT-0426` | `qa_harness` | Full camera QA could prove source/contracts but omit a fresh metadata snapshot of connected real-device availability. | The real-device snapshot script existed and was syntax-checked, but `receipt_camera_qa_gate.sh full` did not run it, leaving full camera QA without current device-environment evidence. | Run the metadata-only real-device snapshot during full camera QA after milestone tests and before platform compile gates, without installing, launching, tapping, or navigating device UI. | `test/receipt_camera_real_device_snapshot_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0425` | `qa_harness` | Focused stitch QA failures could generate generic camera pipeline regression tasks instead of stitch/overlap regression tasks. | The camera failure wrapper accepted stitch gate failures but rewrote them to `camera_pipeline_contracts`, and the task generator had no focused stitch family/category mapping. | Preserve `camera_stitch_gate` as the failed phase and map it to long-receipt stitch/OCR-source contracts with `ghost_overlap_stitching` as the suggested ledger category. | `test/receipt_pipeline_failure_to_regression_test.dart`; `test/receipt_camera_qa_gate_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0424` | `qa_harness` | Detached camera QA summary could emit a focused stitch failure phase that the regression-task wrapper rejected as unknown. | `receipt_camera_qa_summary.sh` gained `camera_stitch_gate` routing, but `receipt_camera_failure_to_regression.sh` only accepted older camera phase names. | Accept `camera_stitch_gate` as a camera pipeline failure phase and guard the wrapper contract. | `test/receipt_camera_qa_gate_contract_test.dart`; `tool/receipt_camera_failure_to_regression.sh camera_stitch_gate <log>` | `closed` |
| `BUG-RECEIPT-0423` | `ocr_handoff_contract` | Low-confidence overlap fallback could keep ordered separate photos while reporting the OCR source contract as ready. | The stitcher correctly refused to merge weak-overlap sections, but the OCR contract treated the fallback ordered path list as safe because counts and positions matched. | Mark low-confidence overlap fallback as an invalid OCR-source contract requiring section-order review before app-assisted OCR. | `test/receipt_stitching_test.dart` | `closed` |
| `BUG-RECEIPT-0422` | `ocr_handoff_contract` | Oversized stitched output fallback could report the OCR source contract as ready even though the derived stitched source was not produced. | Output-size guards preserved the original ordered photos after refusing the combined artifact, and the contract did not distinguish size-safety fallback from a normal ordered-source path. | Mark output-too-large fallback as an invalid OCR-source contract requiring review before OCR handoff. | `test/receipt_stitching_test.dart` | `closed` |
| `BUG-RECEIPT-0421` | `ocr_handoff_contract` | Unreadable/corrupted receipt section paths could fall back from stitching but still report the OCR source contract as ready. | Decode-failed fallback preserved the original ordered path list, and the contract only checked count and position instead of marking unreadable inputs as unsafe for app-assisted OCR. | Mark decode-failed fallback sources as an invalid OCR-source contract requiring review or manual entry. | `test/receipt_stitching_test.dart` | `closed` |
| `BUG-RECEIPT-0420` | `ocr_handoff_contract` | Duplicate receipt section paths could fall back from stitching but still report the OCR source contract as ready. | Duplicate input paths fell back with ordered OCR sources, and the stitch contract only checked count and position, not repeated normalized source identity. | Mark duplicate input or OCR source paths as invalid stitch OCR-source contracts requiring review. | `test/receipt_stitching_test.dart` | `closed` |
| `BUG-RECEIPT-0418` | `camera_diagnostics` | OCR source bottom-coverage and continuation signals could reach expense read handoff and parser diagnostics but disappear from telemetry health snapshots and command-center summaries. | Expense OCR-source telemetry aggregation tracked handoff, stitch, scanner, capture-source, quality, and section-order fields but omitted coverage and continuation count maps. | Added OCR source coverage and continuation count aggregation plus command-center export keys and regression coverage. | `test/expense_screen_telemetry_failure_source_workflow_test.dart` | `closed` |
| `BUG-RECEIPT-0419` | `ghost_overlap_stitching` | Empty receipt photo lists could be classified as no-stitch-needed and look ready for OCR handoff. | The stitch API trimmed blank paths and then treated zero valid paths the same as a single photo, producing a not-needed stitch result instead of a safe fallback. | Return a `no_input_paths` fallback before single-photo handling and pin the empty-list family with a stitch regression. | `test/receipt_stitching_test.dart` | `closed` |
| `BUG-RECEIPT-0417` | `ocr_handoff_contract` | OCR section-order review status and failed-pair evidence could stop at the OCR handoff contract and disappear from expense read-handoff metadata, parser telemetry, and command-center aggregation. | The expense receipt telemetry path forwarded source-quality review fields but did not preserve section-order signal/status fields from OCR diagnostics into parse diagnostics or downstream telemetry summaries. | Added section-order signal/status fields to parse diagnostics, expense read-handoff metadata, parser telemetry metadata, OCR-source telemetry summaries, and command-center snapshot export. | `test/expense_receipt_parser_assisted_review_test.dart`; `test/expense_screen_telemetry_failure_source_workflow_test.dart` | `closed` |
| `BUG-RECEIPT-0416` | `camera_capture_quality` | Native-baseline receipt capture could still emit live blur/glare/shadow/dirty-lens guidance even though Maintainiac is not supposed to claim those quality judgments in the default workflow. | The native live-readability engine only checked per-warning booleans and did not require an explicit future policy gate, so experimental quality signals could leak into the baseline workflow. | Added an explicit `experimental_live_receipt_quality_opt_in` policy gate on Android and iOS and clarified settings copy so current live guidance stays limited to framing/workflow while the native camera owns focus, blur, glare, and exposure behavior. | `test/receipt_native_ios_guidance_warning_gate_test.dart`; `test/receipt_native_android_guidance_policy_gate_test.dart` | `closed` |
| `BUG-RECEIPT-0001` | `qa_harness` | Focused review-copy test looked for readiness copy in the wrong source bundle. | Static test assertion mixed the controls source bundle with the context-controls helper file. | Moved assertions to the source file that owns the readiness copy and reran the focused test. | `test/receipt_photo_review_quality_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0002` | `fixture_generation` | Contractor supply fixture expected compact item labels `Line 1..3` even though parser review labels preserve source receipt row numbers. | Fixture expectation treated parsed item index as receipt source line number and ignored merchant/date/continuation rows. | Updated contractor fixtures to expect source-line labels and added review-mode/line-label QA checks. | `tool/receipt_qa_runner.dart --pack=contractor_supply --fail-under=1.0 --summary-json`; `test/receipt_qa_runner_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0003` | `multi_photo_ordering` | Retake order planning could accept empty replacement paths, duplicate replacement paths, or a replacement path already used by another receipt section. | Replacement path validation only checked that the list was non-empty before rebuilding the ordered photo list. | Reject unsafe replacement path lists before order mutation. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0004` | `multi_photo_ordering` | Malformed native/recovery retake diagnostics with a final section before the original section were counted as ordinary retake metadata. | Section-order metadata counted positive retake section numbers but did not emit a privacy-safe invalid-order bucket. | Added invalid retake order buckets for final-before-original and preserved-slot-moved diagnostics. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0005` | `ghost_overlap_stitching` | Previous-section ghost guide inputs could resolve to a zero-height or invisible/fully opaque overlay after generic 0..1 bounding. | Native session factory bounded values numerically but did not enforce visually usable ghost-slice minimums and maximums. | Added semantic bounds for source slice, overlay placement, overlay height, and opacity. | `test/receipt_native_camera_session_limits_test.dart` | `closed` |
| `BUG-RECEIPT-0006` | `source_preservation` | Duplicate saved proof or OCR source paths could inflate receipt section counts and handoff metadata. | Receipt review results trimmed blank paths but did not de-duplicate repeated paths while preserving order. | Added order-preserving unique path normalization for saved proof and OCR source path lists. | `test/receipt_camera_result_test.dart` | `closed` |
| `BUG-RECEIPT-0007` | `qa_harness` | Pipeline failure tasks could ask for a regression without requiring a bug ledger category or ledger row. | Regression task generation named a failure family but did not connect the task to the permanent categorized bug ledger. | Added suggested ledger categories and required `BUG-RECEIPT-####` ledger work to Dart and shell failure-task generators. | `test/receipt_pipeline_failure_to_regression_test.dart` | `closed` |
| `BUG-RECEIPT-0008` | `native_bridge` | Android and iOS auto-capture readiness thresholds could drift from the shared Flutter receipt camera session contract. | Stable-frame, motion, brightness, and cooldown thresholds were hardcoded in native camera code instead of being sent as session arguments. | Added shared auto-capture readiness thresholds to the Dart session contract, passed them through the native bridge, and made Android/iOS use the configured values. | `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_android_bridge_auto_capture_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0009` | `receipt_line_numbering` | OCR parser line drafts did not expose a source-first receipt line label for multi-section receipt review. | Draft labels kept parser index labels primary even when section/line source metadata was available. | Added `sourceFirstLineLabel` to parser line drafts and included it in local review and privacy-safe summary maps. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0010` | `privacy_redaction` | Client proof redaction plans that requested totals context could still hide subtotal lines. | Redaction planning added tax and total lines for totals context but skipped subtotal candidates. | Keep subtotal, tax, and total lines together whenever totals context is requested, while still hiding unselected item and payment lines. | `test/expense_receipt_parser_direct_parity_test.dart` | `closed` |
| `BUG-RECEIPT-0011` | `receipt_line_review_mode` | Multi-photo receipt handoff could downgrade detailed line review intent to prices-only when an earlier section reported `pricesOnly`. | Native review depth returned the first valid section diagnostic instead of preserving the most detailed requested review depth across sections. | Prefer `detailedLines` when any captured section requests it and expose review-depth counts in privacy-safe handoff metadata. | `test/receipt_camera_result_frozen_brain_install_test.dart` | `closed` |
| `BUG-RECEIPT-0012` | `business_personal_split` | Malformed in-memory split receipt percentages could produce impossible allocation totals such as more than 100% business or negative personal amounts. | Storage hydration clamped `businessPercent`, but direct model construction from parsers, adapters, or future sync could still feed unbounded values into derived totals and serialized maps. | Bound split percentages at derived math and serialization boundaries while keeping business/personal fixed at 100%/0%. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0013` | `business_personal_split` | The active receipt entry review model could preview impossible split allocation totals before saving. | The entry draft line model used raw `businessPercent` in review labels and subtotal allocation math even though editor input and saved ledger records were bounded elsewhere. | Added bounded split percentage math to receipt entry computed fields so preview labels, business totals, and personal totals stay in range. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0301` | `camera_review_state` | Backup scanner and phone-camera fallback captures reached review with only generic `review_if_readable` action and parser-risk buckets. | Saved-photo warning details detected fallback capture sources but did not map them to explicit crop/focus review actions or OCR risk families. | Added backup-scan crop and phone-backup focus action/risk mappings while keeping fallback-only captures from becoming the primary accepted warning profile. | `test/receipt_camera_result_native_quality_test.dart`; `test/helpers/receipt_native_quality_warning_expectations.dart` | `closed` |
| `BUG-RECEIPT-0302` | `ocr_handoff_contract` | Explicit backup scanner and phone-camera fallback risk tokens could stop at the photo-review result and collapse back to generic scanner-prep review in OCR diagnostics. | OCR source handoff review did not recognize the new fallback-source action/risk tokens as a source-quality review family or parser-task diagnostic bucket. | Added `backup_capture_review`, crop/focus/totals action mapping, and backup scan/phone backup parser task counts. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0303` | `ocr_handoff_contract` | Saved-photo shadow risk could surface as a source-quality review but still report the higher-level OCR handoff warning profile as `saved_photo_ok`. | The OCR handoff warning-profile priority list was missing `receipt_handoff_warning_saved_photo_shadow_risk` after the shadow warning family was added. | Added shadow risk to the warning-profile priority list and pinned the privacy-safe review cue with a focused regression. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0304` | `qa_harness` | Future saved-photo warning profiles could be added to photo review without being wired into OCR handoff warning-profile/review-cue status. | The suite had focused regressions for individual warning families but no class-level parity contract between `acceptedPhotoWarningProfile` and `warningProfileStatus`. | Added a source contract test that compares both warning-profile lists and fails if an accepted profile is missing from OCR handoff. | `test/receipt_warning_profile_parity_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0305` | `ocr_handoff_contract` | Saved-photo parser-risk codes could be present in photo-review metadata but absent from OCR source attachment risk flags. | The OCR source risk builders emitted each saved-photo warning code and action, but not `parserRiskCode`, forcing downstream diagnostics to infer parser risk from less precise action tokens. | Emit `ocr_source_parser_risk_*` flags from both source builders and classify those tokens in OCR handoff/admin diagnostics. | `test/receipt_capture_flow_handoff_contract_test.dart`; `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0306` | `qa_harness` | Future saved-photo parser-risk codes could be added without OCR source handoff and parser/admin diagnostic support. | Pass 820 added parser-risk handoff wiring, but no class-level contract forced future `parserRiskCode` additions to update OCR handoff and diagnostics. | Added a parity contract that checks every saved-photo `parserRiskCode` has matching `ocr_source_parser_risk_*` handling in OCR handoff and diagnostics helpers. | `test/receipt_warning_profile_parity_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0307` | `receipt_line_review_mode` | OCR source handoff stored receipt review depth only as a raw document signal, so downstream diagnostics could miss whether the camera was in price-only or detailed-line mode. | `ReceiptOcrSourceHandoffSummary` counted handoff, stitch, coverage, continuation, completion, and quality families but did not classify `receipt_review_depth_*` signals. | Added explicit review-depth counts/status to the OCR source handoff contract and pinned detailed-line handoff with a focused OCR service regression. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0308` | `qa_harness` | Focused receipt OCR service tests failed to compile because the PDF inspector referenced a missing private helper. | `ReceiptPdfInspector.detectRiskFlags` called `_containsPdfName` even though the available helper lives on `AppPdfSecurityPolicy`. | Routed trailer-name detection through `AppPdfSecurityPolicy.containsPdfName` and reran the focused OCR service/PDF inspector checks. | `test/receipt_ocr_service_test.dart`; `test/receipt_pdf_inspector_edge_cases_test.dart` | `closed` |
| `BUG-RECEIPT-0309` | `receipt_line_review_mode` | Receipt review depth was classified in OCR source handoff but not exposed as typed OCR diagnostics fields for downstream UI/admin/telemetry. | Diagnostics only carried review-depth data inside `ocrSourceHandoffContract`, forcing callers to scrape a loose map instead of stable fields. | Added typed `ocrSourceReviewDepthSignalCounts` and `ocrSourceReviewDepthStatus` diagnostics fields and pinned them with a focused OCR service regression. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0310` | `receipt_line_review_mode` | Price-only versus detailed-line review depth could disappear before expense telemetry/admin rollups. | Pass 823 exposed typed OCR diagnostics, but OCR completion telemetry, parser telemetry, privacy-safe receipt events, and Command One rollups did not forward those fields. | Forward review-depth signal counts/status through OCR and parser metadata, privacy-safe events, telemetry summary rollups, and Command One map output with focused regressions. | `test/expense_screen_telemetry_failure_source_workflow_test.dart`; `test/expense_screen_telemetry_camera_health_test.dart`; `test/receipt_camera_ocr_source_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0311` | `barcode_qr_scanning` | Compact customer/account QR identifiers could become inventory lookup candidates. | Sensitive barcode payload detection required a separator or end boundary after privacy tokens, so values like `CUSTOMER12345` or `receipt_account#A12345` were treated as generic text. | Treat digits and hash separators after sensitive payload tokens as private evidence, block lookup, and keep raw QR identifiers out of privacy-safe summaries. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0312` | `barcode_qr_scanning` | Multi-image barcode/QR scan summaries did not show aggregate format or value-type coverage. | Single-image summaries exposed privacy-safe format/type counts, but batch summaries only exposed total code/QR counts and lookup candidates, leaving long-receipt scanner QA blind to UPC versus QR mix. | Add aggregate `formatCounts` and `valueTypeCounts` to batch privacy-safe summaries and pin them with a focused barcode scanner regression. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0313` | `qa_harness` | Review-result barcode handoff QA did not prove UPC/QR batch summary evidence survived the OCR-source-first scan path. | Pass 826 tested batch summaries directly but the review-result handoff regression only checked source path order and purpose. | Extend the review-result handoff regression with UPC/QR fixtures and privacy-safe format/type count assertions. | `test/receipt_capture_flow_barcode_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0314` | `native_bridge` | Android native camera diagnostics could report `lastFocusStatus` as `not_used` after continuous autofocus was configured. | CameraX was configured with continuous-picture AF request options, but the Android bridge never updated the focus-status diagnostic after binding the camera. | Set `lastFocusStatus` from the effective continuous-focus policy after native bind and pin configured/unavailable/not-requested status tokens with Android bridge QA. | `test/receipt_native_android_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0315` | `barcode_qr_scanning` | Multi-image barcode/QR summaries could hide how many receipt image inputs were actually scanned versus rejected. | Batch summaries exposed total image result count and warning buckets, but not scanned, invalid, or warning image counts for mixed valid/invalid long-receipt input batches. | Add privacy-safe scanned/warning/invalid image counts to batch summaries and pin mixed valid/invalid image behavior without exposing raw paths. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0354` | `ghost_overlap_stitching` | A mismatched stitch/OCR source contract could still route an accepted long receipt straight into filled receipt details. | The OCR-source relationship marked manual review required, but accepted-photo route labels and must-open booleans only checked kept-for-later, partial receipt, and section-order blockers. | Treat manual-review-required OCR source handoffs as `needs_review_before_ocr`, route them to OCR source review, and block direct filled-review/details opening until reviewed. | `test/receipt_ocr_source_relationship_test.dart` | `closed` |
| `BUG-RECEIPT-0355` | `ocr_handoff_contract` | Scanner preparation review risks could still label the accepted photo path as a normal prepared source. | The scanner risk set manual review required, but the handoff path label did not distinguish scanner-preparation review from a clean prepared OCR source. | Add a scanner-preparation review handoff path and block direct receipt-details opening for scanner operator-review cases. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0356` | `ocr_handoff_contract` | Attachment-panel OCR source signals could claim the clear-source-before-proof path even when the result fell back to saved proof. | The shared attachment signal builder always added `ocr_reads_clear_source_before_saved_proof`, while the capture-flow builder only added it when the review result proved a clear OCR source. | Make attachment signals add the clear-source token only when `ocrReadsClearSourceBeforeSavedProof` is true and pin the source contract. | `test/receipt_capture_flow_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0357` | `ocr_handoff_contract` | OCR-source document signals could claim a prepared non-backup source even when OCR fell back to saved proof. | Both capture-flow and attachment-panel signal builders added `ocr_reads_prepared_source_not_saved_backup` before checking whether the result actually had a clear OCR source. | Gate the prepared-source token behind `ocrReadsClearSourceBeforeSavedProof` and add an explicit saved-proof fallback review token. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0358` | `qa_harness` | Photo-review UI copy regression inspected only the old controls source after the next/details copy moved into exit actions. | The source-contract test kept asserting labels against `receipt_photo_review_controls.dart`, even though the implementation now owns exit-copy labels in `receipt_photo_review_exit_actions.dart`. | Add exit-actions source to the UI-copy contract so the test follows the modularized review controls. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0359` | `qa_harness` | Photo-review UI sizing regression inspected preview sources but missed the modular mode-control file that owns the expected segmented-control minimum size. | The source-contract test continued checking `minimumSize: const Size(92, 36)` against preview controls after that size moved into `receipt_photo_review_mode_controls.dart`. | Add mode-controls source to the UI-copy contract so modularized review control sizing remains covered. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0360` | `qa_harness` | Photo-review UI wording regression looked for stale `Next opens receipt` copy after review guidance moved to clearer open-receipt-details wording. | The test pinned old copy instead of the active review-flow language used by exit actions and preview controls. | Update the contract to require active `open receipt details` guidance while still checking review/detail labels. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0361` | `qa_harness` | Photo-review UI wording regression expected an old business/personal sentence that no active preview source owned. | Review copy now describes readable store/date/total/item-price evidence in the preview status and keeps Business/Personal/Mixed in the handoff model. | Update the UI source contract to pin active readability copy while the model contract continues pinning Business/Personal/Mixed review language. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0362` | `qa_harness` | Photo-review UI copy regression forbade the active `Use this photo` review guidance. | The test still treated `Use this photo` as stale copy, even though the active review flow intentionally offers Use/Retake/Add Photo choices after capture. | Update the contract to allow the active review choice copy while continuing to reject stale `Read receipt` and `Saved copy` wording. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0363` | `camera_capture_quality` | iOS receipt guidance toggle parity could regress and re-enable experimental blur/glare/shadow/dirty-lens claims through the general guidance setting. | Android had a focused guidance-toggle source regression, but iOS only indirectly proved the experimental quality warnings were gated. | Add an iOS source regression proving receipt guidance toggles only framing/text-size guidance and cannot touch experimental quality-warning toggles. | `test/receipt_native_ios_guidance_warning_gate_test.dart` | `closed` |
| `BUG-RECEIPT-0364` | `camera_capture_quality` | Default camera guidance copy could drift back into unproven blur/glare/shadow/dirty-lens claims even with experimental quality warnings disabled. | Existing release-control tests checked docs and native warning gates but did not pin the neutral default guidance text blocks directly. | Add Android/iOS source regressions requiring neutral readable-receipt guidance and rejecting experimental quality-warning copy from the default guidance helpers. | `test/receipt_camera_release_control_priority_test.dart` | `closed` |
| `BUG-RECEIPT-0365` | `ocr_handoff_contract` | Long-receipt handoff coverage could miss a mismatch where several saved proof photos produce one stitched OCR source. | Existing source contracts checked source strings and saved-proof fallback, but did not run the attachment builder with different saved-proof and OCR-source counts. | Add a runtime regression proving OCR attachments are built from OCR-source count/path, not saved-proof count/path, and keep saved-proof fallback signals out of the stitched-source path. | `test/receipt_capture_flow_ocr_source_count_test.dart` | `closed` |
| `BUG-RECEIPT-0366` | `ocr_handoff_contract` | Save-only/manual kept-for-later receipt photos could be accidentally treated as OCR attachments by future capture-flow changes. | Kept-for-later suppressed OCR source fallback in the model, but the runtime attachment contract did not directly prove no OCR attachments are emitted. | Add a kept-for-later regression proving OCR source paths and OCR attachments stay empty while receipt details remain blocked until user resumes review. | `test/receipt_capture_flow_ocr_source_count_test.dart` | `closed` |
| `BUG-RECEIPT-0367` | `camera_diagnostics` | iOS native diagnostics duplicated previous-section ghost-guide keys, allowing later values to overwrite earlier values in admin/debug reports. | The diagnostics payload emitted `previousSectionGhostGuideUsesNextContext`, `previousSectionGhostGuidePolicy`, and `previousSectionGhostGuideMatchTarget` in two different sections. | Remove the duplicate iOS diagnostics entries and rely on the existing no-duplicate diagnostics payload regression. | `test/receipt_native_ios_diagnostics_payload_test.dart` | `closed` |
| `BUG-RECEIPT-0368` | `camera_diagnostics` | Privacy-safe camera diagnostics could drop finite numeric quality scores needed for debugging capture failures. | The diagnostics publish policy allowed integers but rejected all non-integer numbers, so brightness, motion, shadow, and edge-coverage scores could disappear even when privacy-safe. | Preserve finite numeric diagnostics while still dropping NaN and infinite values from nested and top-level camera diagnostic envelopes. | `test/receipt_capture_diagnostics_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0369` | `camera_review_state` | Receipt Assist could be silently enabled before the user chose app-assisted filling. | Receipt capture settings defaulted the global and area-specific app-assisted switches to true, and the capture flow fell back to assisted mode when settings were missing. | Default app-assisted receipt fill off, keep first-use/user settings as the enable path, and keep reset from silently re-enabling area assistance. | `test/receipt_capture_settings_store_test.dart`; `test/receipt_capture_flow_assist_opt_in_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0370` | `camera_review_state` | Receipt photo, PDF, and text read paths could still treat missing settings as app-assisted receipt fill enabled. | Several read boundaries checked for `!= false` or only blocked explicit false settings, so a missing settings scope could start OCR/import parsing without a confirmed Receipt Assist choice. | Route receipt read boundaries through one explicit opt-in helper and require area assistance to be true before OCR/read handoff can start. | `test/receipt_capture_flow_assist_opt_in_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0371` | `ocr_handoff_contract` | Duplicate, blank, or non-normalized receipt source paths could be silently cleaned before OCR handoff review saw them. | `ReceiptPhotoReviewResult` deduplicated and trimmed saved-proof and OCR-source paths in the constructor, so malformed long-receipt source input could appear to match the stitch contract after sanitization. | Preserve privacy-safe sanitized-input flags and route sanitized saved-proof or OCR-source input to manual review before receipt details are saved. | `test/receipt_ocr_source_relationship_test.dart` | `closed` |
| `BUG-RECEIPT-0372` | `camera_review_state` | Add Photo or Retake Photo from receipt review could silently re-enable Receipt Assist. | `ReceiptPhotoReviewScreen` did not carry the confirmed assist choice, and its native-camera helper hard-coded `assistedReceiptFill: true`; the low-level native setting descriptor also still advertised assist as default-on. | Carry an explicit assist flag into photo review, route add/retake camera sessions through that flag, and default native assist descriptors/settings off. | `test/receipt_capture_flow_assist_opt_in_contract_test.dart`; `test/receipt_native_camera_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0373` | `camera_review_state` | Resuming an interrupted native receipt capture could change Receipt Assist behavior from the original capture. | Recovery review recomputed assist state from current settings/options only, even though the staged capture diagnostics can preserve the assist flag that was active when the photos were taken. | Restore the saved `assistedReceiptFill` recovery diagnostic when present and fall back to current settings only when old recovery data lacks the flag. | `test/receipt_native_capture_recovery_record_test.dart`; `test/receipt_capture_flow_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0374` | `camera_capture_quality` | Default native live diagnostics could still claim the receipt frame was steady when experimental quality warnings were disabled. | Android and iOS correctly reset visible blur/glare/shadow guidance when experimental warnings were off, but still wrote `latestMotionSignal` as `steady` from live samples in the default neutral path. | Use a neutral workflow-guidance motion signal while experimental quality warnings are disabled, leaving real steady/moving diagnostics to opt-in quality or auto-capture flows. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0375` | `source_preservation` | Successful stitched receipt review could leave temporary per-section OCR prep files behind. | The success path removed all generated prep artifacts from cleanup when they were saved proofs, per-section OCR prep files, or final stitched OCR files, even though per-section OCR prep files are no longer final sources after stitching succeeds. | Keep saved proof files and final stitch OCR sources, then clean remaining generated prep artifacts before closing review. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0376` | `camera_docs` | Active receipt blueprints still described blur/glare/low-light warnings as default release-one camera behavior. | The native implementation now keeps unproven quality claims experimental/off by default, but the camera and expense release blueprints still used wording that could steer future UI work back toward fake live-quality prompts. | Reword active blueprints around neutral receipt framing/readability guidance and add release-control regressions rejecting the stale default blur/glare warning copy. | `test/receipt_camera_release_control_priority_test.dart` | `closed` |
| `BUG-RECEIPT-0377` | `ghost_overlap_stitching` | Stitch review labels could surface non-finite confidence, zoom, or rotation evidence from an image-processing boundary. | `ReceiptStitchResult` and `ReceiptStitchPairResult` formatted raw doubles directly, so NaN or infinite values could leak into review labels or make overlap evidence look trusted. | Clamp confidence to a finite 0-100% display range, treat non-finite scale/rotation as neutral, and add stitch contract regressions for NaN/infinite evidence. | `test/receipt_stitching_result_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0378` | `privacy_redaction` | Opt-in camera diagnostics could publish machine-looking receipt content under safe-looking keys. | The diagnostic publish policy rejected unsafe keys and freeform values, but still allowed uppercase token values and note/label/message/item-name keys that could carry merchant names, receipt lines, or address tokens. | Drop human-facing text key families, require lowercase machine-token string values, and add regressions for merchant/address/line-item tokens. | `test/receipt_capture_diagnostics_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0379` | `source_preservation` | Shared capture-flow handoff diagnostics still used original-source wording for temporary OCR sources. | The receipt model and staging tokens had moved to temporary full-quality source wording, but `_receiptReaderHandoffDiagnosticsFor` still published `read_original_or_prepared_source_before_saved_proof`, which could confuse future storage/UI work into permanent original retention. | Rename the handoff policy token to temporary full-quality/prepared source wording and add a regression rejecting the stale original-source token. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0380` | `source_preservation` | Photo-edit source-selection diagnostics used `original_source_retained` even when they only meant the accepted review source stayed selected. | Review and OCR-source signals used old original-retention wording for an edit-state bucket, which could be confused with permanent original-quality proof retention. | Rename the source-selection bucket to `accepted_source_retained` across handoff signals and add regressions rejecting the stale token. | `test/receipt_camera_result_recovery_metadata_test.dart`; `test/receipt_photo_review_quality_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0381` | `native_bridge` | Native camera results could reach staging with missing or duplicate temporary capture ids. | The Dart bridge sanitized temp IDs and tolerated extras, but did not reject too few IDs or duplicate IDs after sanitization, so staged long-receipt sections could lose one-to-one identity with native photo paths. | Require one unique temporary capture id per accepted native photo path and add bridge regressions for missing and duplicate IDs. | `test/receipt_native_camera_result_path_validation_test.dart` | `closed` |
| `BUG-RECEIPT-0382` | `camera_review_state` | Receipt review results could change routing after construction when the caller mutated stitch source path lists. | `ReceiptPhotoReviewResult` froze its own path and diagnostics maps, but kept the caller-owned `ReceiptStitchResult` object by reference, so late list mutation could turn an accepted OCR handoff into a review-required handoff. | Snapshot stitch input paths, OCR source paths, overlap pixels, and pair lists when constructing the review result, and add direct immutability regressions. | `test/receipt_camera_result_frozen_brain_install_test.dart` | `closed` |
| `BUG-RECEIPT-0383` | `native_bridge` | Native capture staging results exposed mutable returned collections to later UI/review code. | Staging copied native photos safely, but `ReceiptNativeCaptureStagingResult` returned mutable path lists, path maps, diagnostics maps, and attachment lists, so downstream UI changes could alter staged workflow state after persistence. | Freeze staged-photo paths, original-to-staged path maps, nested capture diagnostics, and staged attachment lists in the staging result constructor. | `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0384` | `camera_review_state` | Camera capture results exposed mutable photo paths, quality checks, and nested diagnostics to review UI code. | `ReceiptCameraResult` kept caller-owned lists and returned only an outer unmodifiable diagnostics map, so later mutation could change capture membership or diagnostics after the result was handed to review. | Freeze camera result path/quality lists and each per-photo diagnostics map before returning capture diagnostics. | `test/receipt_camera_result_quality_test.dart`; `test/receipt_camera_result_best_shot_ocr_test.dart` | `closed` |
| `BUG-RECEIPT-0385` | `camera_review_state` | Picked-photo review handoff objects could keep mutable paths, quality maps, and phone-camera backup diagnostics. | `_PickedReceiptPhotos` was private review UI plumbing but stored caller-owned collections directly, so Add Photo or Retake work could mutate picked paths or diagnostics after handoff. | Freeze picked paths, quality checks, and nested diagnostics in the picked-photo constructor while keeping empty/canceled sentinels const. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0386` | `camera_review_state` | Camera capture evidence and scanner decision diagnostics exposed mutable list values after handoff. | `ReceiptCameraCaptureEvidence` kept caller-owned exposure candidate lists, and `scannerDecisionCodes` returned a mutable list inside per-photo diagnostics. | Freeze exposure candidate offsets at evidence construction and publish scanner decision codes as an unmodifiable diagnostics list. | `test/receipt_camera_result_best_shot_ocr_test.dart`; `test/receipt_camera_result_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0387` | `camera_review_state` | Add Photo and Retake order diagnostics could be merged back into review state as mutable nested maps. | The picked-photo handoff was frozen, but `_mergeOrderCaptureDiagnostics` rebuilt merged order/retake diagnostics with ordinary mutable maps before adding them to review state. | Freeze the merged diagnostics map and each per-photo diagnostics map at the Add Photo/Retake merge boundary. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0388` | `camera_review_state` | Picked-only Add Photo diagnostics could remain mutable after the order-diagnostics freeze. | The merge boundary wrapped order-overlap entries but passed picked-only nested diagnostic maps through by reference. | Freeze picked-only diagnostic maps with `Map.unmodifiable` before merging order diagnostics. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0389` | `camera_diagnostics` | Camera flow diagnostics could be mutated after review opening or cancellation handoff. | Flow helper diagnostics returned ordinary mutable maps, including nested per-photo review-opening metadata. | Freeze previous-section, flow-result, and per-photo review-opening diagnostics at the helper boundary. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0390` | `camera_capture_quality` | Experimental live receipt-quality guidance could rotate warning copy every sampled frame. | Live analysis changed blur, low-light, glare, shadow, and dirty-lens messages immediately from one sampled frame, so unstable measurements could look like random billboard text. | Require the same experimental quality signal to repeat before changing guidance copy, and reset the candidate when receipt target or quality mode is neutral. | `test/receipt_camera_release_control_priority_test.dart` | `closed` |
| `BUG-RECEIPT-0391` | `native_bridge` | Native capture result objects could be mutated after the method-channel handoff. | `ReceiptNativeCaptureResult` stored caller-owned photo path lists, capture id lists, and nested diagnostic collections directly. | Snapshot result paths, capture ids, and nested diagnostics into unmodifiable collections at construction. | `test/receipt_native_camera_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0392` | `camera_diagnostics` | Experimental live-warning stability state was not available in native capture diagnostics. | The bridge reported whether experimental quality warnings were enabled, but not which candidate warning was being stabilized or how many frames had repeated. | Publish privacy-safe experimental quality candidate signal and repeat count from Android and iOS diagnostics. | `test/receipt_camera_release_control_priority_test.dart` | `closed` |
| `BUG-RECEIPT-0393` | `camera_diagnostics` | Native diagnostics did not report every experimental receipt-quality warning toggle. | Android and iOS diagnostics exposed shadow warning state and the aggregate experimental flag but omitted motion-blur, glare, dirty-lens, and low-light warning toggles. | Publish all individual experimental warning toggle states in privacy-safe native diagnostics. | `test/receipt_camera_release_control_priority_test.dart` | `closed` |
| `BUG-RECEIPT-0394` | `camera_diagnostics` | Retired tap-focus state could still leak through native diagnostics. | Android and iOS capture diagnostics serialized `tapFocusEnabled` from runtime controller state even though tap focus is not an allowed receipt-camera control. | Hard-code native diagnostic `tapFocusEnabled` to false and keep control expectation false. | `test/receipt_camera_release_control_priority_test.dart` | `closed` |
| `BUG-RECEIPT-0395` | `multi_photo_ordering` | Section-order diagnostic helper lists could be returned mutable. | Retake, insert, and manual-reorder helper paths returned locally mutable `codes` lists, leaving private handoff plumbing easier to mutate accidentally before counts were built. | Return unmodifiable section-order diagnostic code lists from every helper branch. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0396` | `camera_diagnostics` | Sanitized native recovery diagnostics could still be mutated after restore. | The native capture diagnostics sanitizer filtered unsafe keys and non-finite values but returned mutable top-level, nested-map, and list collections. | Return unmodifiable sanitized diagnostics at every nested collection boundary. | `test/receipt_native_capture_recovery_index_test.dart` | `closed` |
| `BUG-RECEIPT-0397` | `native_bridge` | Native capture recovery records could keep mutable staged-photo and attachment lists. | Recovery index entries and manifest recovery records accepted caller-owned lists directly. | Snapshot staged photo paths and attachments into unmodifiable lists at recovery object construction. | `test/receipt_native_capture_recovery_index_test.dart`; `test/receipt_native_capture_recovery_record_test.dart` | `closed` |
| `BUG-RECEIPT-0398` | `camera_diagnostics` | Direct native recovery constructors could keep mutable or unsafe diagnostics. | `fromMap` and manifest restore used the sanitizer, but direct recovery record/index constructors accepted caller-owned diagnostic maps as-is. | Route direct recovery constructor diagnostics through the same sanitizer and immutable nested collection boundary. | `test/receipt_native_capture_recovery_index_test.dart`; `test/receipt_native_capture_recovery_record_test.dart` | `closed` |
| `BUG-RECEIPT-0399` | `camera_diagnostics` | Receipt-reader flow handoff diagnostics could be mutated after helper construction. | `_receiptReaderHandoffDiagnosticsFor` returned a mutable map even though it carries routing, OCR-source, and review-count contracts into later flow layers. | Freeze the receipt-reader handoff diagnostics map before returning it to capture flow callers. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0400` | `camera_review_state` | Receipt attachment risk, signal, and photo-quality warning lists could be mutated through public getters. | `ReceiptAttachmentRecord` exposed list fields directly, so downstream receipt handoff code could append or remove OCR-source signals after construction. | Store attachment signal lists privately and expose unmodifiable list views from public getters. | `test/receipt_attachment_record_metadata_test.dart` | `closed` |
| `BUG-RECEIPT-0401` | `ghost_overlap_stitching` | Stitch result path, overlap, and pair lists could be mutated through public getters after construction. | `ReceiptStitchResult` exposed OCR-source paths, input paths, overlap pixels, and pair lists directly, so downstream review and OCR handoff code could rewrite long-receipt stitching evidence after the result was built. | Store stitch result list fields privately and expose unmodifiable list views from public getters. | `test/receipt_stitching_result_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0402` | `ghost_overlap_stitching` | Final OCR stitch reuse could reject a valid ordered long-receipt preview because it compared raw path strings instead of normalized receipt path identity. | `_sameReceiptPhotoOrder` required exact raw string equality, so a valid top-to-bottom section set could lose its trusted preview/review handoff if the same files re-entered through a different path spelling. | Route final stitch order matching through the shared normalized receipt-photo identity helper so preview reuse and manual-overlap carryover only depend on valid ordered section identity. | `test/receipt_photo_path_identity_test.dart` | `closed` |
| `BUG-RECEIPT-0403` | `ghost_overlap_stitching` | Generic numbered-section guidance could hide a retake-preserved long-receipt outcome. | Section-order outcome classification checked generic numbered/ghost-guided multi-section signals before retake/insert/manual preserved outcomes, so a valid section replacement could be mislabeled as ordinary long-receipt numbering. | Prioritize preserved retake/insert/manual order outcomes ahead of generic numbered-section guidance and pin the precedence with a regression. | `test/receipt_camera_result_section_order_test.dart` | `closed` |
| `BUG-RECEIPT-0404` | `ghost_overlap_stitching` | Final accepted-photo handoff labels could flatten preserved retake/insert/manual section outcomes into a generic ordered-section fallback action. | `acceptedPhotoHandoffActionLabel` and `acceptedPhotoHandoffUserAction` only specialized order handling for blocking review cases, so safe retake/insert/manual reorder outcomes lost their specific follow-through instructions once stitch fallback was otherwise acceptable. | Preserve non-blocking retake/insert/manual section-order follow-through labels and user actions in the final accepted-photo handoff. | `test/receipt_camera_result_section_order_test.dart` | `closed` |
| `BUG-RECEIPT-0405` | `ghost_overlap_stitching` | Preserved retake/insert section outcomes could still collapse into generic receipt-details messaging in final next-step labels. | The accepted-photo handoff preserved specific user actions, but `acceptedPhotoHandoffNextStepLabel` and `acceptedPhotoHandoffRouteResultLabel` still dropped back to generic receipt-details wording after safe stitch fallback. | Keep explicit retake/insert/manual section-order follow-through messaging in final next-step and route-result labels. | `test/receipt_camera_result_section_order_follow_through_test.dart` | `closed` |
| `BUG-RECEIPT-0406` | `ghost_overlap_stitching` | Weak-overlap stitch fallback could hide which section boundary failed in the next-review labels. | The fallback result tracked `failedPairIndex`, but `nextReviewMatchReadinessLabel` and `nextReviewHandoffLabel` still reduced the message to generic ordered-sections wording, forcing the user to hunt for the bad middle-section boundary. | Surface the failed pair label directly in ordered-section fallback review copy so retake and overlap fixes target the right section boundary. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0407` | `ghost_overlap_stitching` | The weak-overlap recovery control could stay generic even when the exact failing pair was known. | The fallback card exposed `failedPairLabel`, but the primary recovery pill still only said `Fix Photo Order`, which slowed retakes for long receipts with one bad middle boundary. | Make the fallback recovery pill name the failing pair when available, while preserving the generic label when the failing boundary is unknown. | `test/receipt_photo_review_exit_completion_test.dart`; `test/receipt_camera_long_receipt_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0316` | `barcode_qr_scanning` | Invalid barcode/QR image paths could consume the multi-image scan limit and prevent valid receipt segments from being scanned. | `scanImageFiles` counted invalid warning results against `maxImageCount`, even though invalid paths never invoke ML Kit. | Track input count separately, cap decoder work by scanned valid images, and keep invalid-path warning evidence privacy-safe. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0317` | `native_bridge` | iOS native camera diagnostics could keep `lastFocusStatus` as `not_used` when continuous focus was intentionally not requested. | The iOS bridge set configured/unavailable focus statuses for continuous mode but left non-continuous sessions on the default placeholder token. | Add an explicit `continuous_focus_not_requested` status for non-continuous iOS sessions and pin it with native bridge QA. | `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0318` | `native_bridge` | Native focus-status diagnostics were captured but not classified into review/admin health outcomes. | Android and iOS now emit focus-status tokens, but the review result health classifier ignored `lastFocusStatus`, so configuration failures or stale `not_used` tokens could be hidden behind otherwise-ready controls. | Add focus-status health counts, risk outcomes, and attachment risk flags for configured, unavailable, not-requested, configuration-failed, and stale not-used diagnostics. | `test/receipt_camera_result_native_ui_ready_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0319` | `ghost_overlap_stitching` | Retaking a long-receipt segment could reopen the native camera with generic continuation guidance instead of the selected segment's retake-alignment context. | Retake flow sent the guide photo path but did not forward the retake reason code or human guidance into the native session handoff. | Forward retake alignment reason/guidance through capture picking into the native camera session while keeping coverage guidance as the fallback. | `test/receipt_photo_review_retake_order_test.dart`; `test/receipt_camera_long_receipt_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0320` | `ghost_overlap_stitching` | Retaking the top receipt section could pass the next section into the native previous-section ghost overlay slot. | Top-section retake context used the next photo as the preferred guide even though native ghost guidance only supports previous-section top overlays. | Add a previous-section-only native guide path and route retakes through it so next-section context stays review-only until a true next-guide overlay exists. | `test/receipt_photo_review_retake_order_test.dart`; `test/receipt_camera_long_receipt_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0321` | `multi_photo_ordering` | Malformed retake, insert, or manual reorder metadata could reach OCR handoff without a review-required action. | Section-order diagnostics only checked basic section-number ordering and did not validate offsets, alignment context flags, top-retake ghost mismatches, or action metadata. | Add invalid-order families, privacy-safe section-order review action codes/labels, and review-required handoff counts/metadata. | `test/receipt_camera_result_section_order_test.dart`; `test/receipt_camera_result_section_order_invalid_context_test.dart`; `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0322` | `multi_photo_ordering` | Invalid receipt section-order metadata could still allow the handoff route to open receipt details/OCR. | The review-required section-order action updated labels and counts, but the route/outcome booleans only blocked kept-for-later and missing-bottom continuation cases. | Make section-order review a `needs_review_before_ocr` outcome, pause receipt-details handoff, and emit the review-needed attachment signal. | `test/receipt_camera_result_section_order_invalid_context_test.dart`; `test/receipt_camera_result_section_order_test.dart`; `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0323` | `multi_photo_ordering` | Retake or inserted-section diagnostics with missing section numbers could be treated as ordinary unordered metadata. | The malformed-order helpers returned early when original/final or anchor/final section numbers were missing, even if other retake/insert metadata proved the operation existed. | Detect missing retake original/final and insert anchor/final section numbers as invalid order metadata and require review before OCR. | `test/receipt_camera_result_section_order_invalid_context_test.dart`; `test/receipt_camera_result_section_order_test.dart`; `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0324` | `ocr_handoff_contract` | Section-order review actions could stop at photo-review metadata without reaching OCR-source attachment signals or risk flags. | The attachment signal builders emitted the general handoff outcome but did not include section-order outcome/action evidence or an OCR-source risk flag for review-required ordering. | Emit section-order outcome/action document signals and review-required OCR source risk flags from both shared capture flow and attachment-panel paths. | `test/receipt_capture_flow_handoff_contract_test.dart`; `test/receipt_camera_result_section_order_invalid_context_test.dart` | `closed` |
| `BUG-RECEIPT-0325` | `ocr_handoff_contract` | Section-order document/risk signals could be present on OCR attachments but not classified as typed section-order evidence. | `ReceiptOcrSourceHandoffSummary` counted many OCR-source signal families but did not expose a section-order family, and generic OCR-source risk bucketing could mix section-order review flags with photo-quality risks. | Add `sectionOrderSignalCounts`, expose it through privacy-safe handoff diagnostics, classify review-required section order as `section_order_review_required`, and keep section-order risk flags out of generic photo-quality counts. | `test/receipt_ocr_source_section_order_handoff_test.dart`; `test/receipt_capture_flow_handoff_contract_test.dart`; `test/receipt_camera_result_section_order_invalid_context_test.dart` | `closed` |
| `BUG-RECEIPT-0326` | `native_bridge` | Stale native UI contract tags for retired controls could make the camera look like it was missing tap-focus or lock controls instead of flagging the stale contract as a regression. | Control-readiness health checks treated contract tags as expected controls even when product policy explicitly retired tap focus, focus lock, exposure lock, and white-balance lock. | Classify retired control contract tags as retirement regressions, keep them out of missing-control readiness counts, and surface the regression as native camera UI risk. | `test/receipt_camera_result_retired_control_contract_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0327` | `native_bridge` | Native diagnostics could report retired tap-focus or lock controls as active without any stale contract tag and still avoid a camera UI risk. | The retirement guard added for stale contract tags returned before normal missing-control checks, but controls with expected=false and no tag were skipped before inspecting active actual statuses. | Inspect retired control actual statuses even when no contract tag exists, classify active retired controls as retirement regressions, and keep them out of ready/missing control counts. | `test/receipt_camera_result_retired_control_contract_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0328` | `native_bridge` | A lock-only retired-control regression could be counted as risk evidence but still leave the native UI outcome looking ready. | Native UI health outcome priority only named tap-focus retirement regressions before the normal readiness checks, so focus-lock, exposure-lock, or white-balance-lock regressions could be hidden behind ready summaries. | Promote any positive `*_retirement_regressed` count ahead of normal readiness outcomes so the primary camera health label matches the real regression. | `test/receipt_camera_result_retired_control_contract_test.dart` | `closed` |
