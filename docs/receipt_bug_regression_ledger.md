# Receipt Bug Regression Ledger

Every confirmed receipt camera, OCR, parser, fixture, or review-flow bug gets a
ledger row before the fix is considered closed. Keep entries privacy-safe: no
raw receipt text, no customer data, no VINs, no plates.

Required fields for each entry:
- `Bug ID`
- `Category`
- `Symptom`
- `Root cause`
- `Fix`
- `Regression coverage`
- `Status`

Allowed categories:
- `camera_capture_quality`
- `multi_photo_ordering`
- `ghost_overlap_stitching`
- `source_preservation`
- `ocr_handoff_contract`
- `receipt_line_numbering`
- `receipt_line_review_mode`
- `business_personal_split`
- `parser_totals_math`
- `fixture_generation`
- `qa_harness`
- `privacy_redaction`
- `native_bridge`
- `barcode_qr_scanning`
- `camera_review_state`
- `camera_diagnostics`
- `camera_docs`
- `storage_recovery`

| Bug ID | Category | Symptom | Root cause | Fix | Regression coverage | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `BUG-RECEIPT-0001` | `qa_harness` | Focused review-copy test looked for readiness copy in the wrong source bundle. | Static test assertion mixed the controls source bundle with the context-controls helper file. | Moved assertions to the source file that owns the readiness copy and reran the focused test. | `test/receipt_photo_review_quality_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0002` | `fixture_generation` | Contractor supply fixture expected compact item labels `Line 1..3` even though parser review labels preserve source receipt row numbers. | Fixture expectation treated parsed item index as receipt source line number and ignored merchant/date/continuation rows. | Updated contractor fixtures to expect source-line labels and added review-mode/line-label QA checks. | `tool/receipt_qa_runner.dart --pack=contractor_supply --fail-under=1.0 --summary-json`; `test/receipt_qa_runner_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0003` | `multi_photo_ordering` | Retake order planning could accept empty replacement paths, duplicate replacement paths, or a replacement path already used by another receipt section. | Replacement path validation only checked that the list was non-empty before rebuilding the ordered photo list. | Reject unsafe replacement path lists before order mutation. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0004` | `multi_photo_ordering` | Malformed native/recovery retake diagnostics with a final section before the original section were counted as ordinary retake metadata. | Section-order metadata counted positive retake section numbers but did not emit a privacy-safe invalid-order bucket. | Added invalid retake order buckets for final-before-original and preserved-slot-moved diagnostics. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0005` | `ghost_overlap_stitching` | Previous-section ghost guide inputs could resolve to a zero-height or invisible/fully opaque overlay after generic 0..1 bounding. | Native session factory bounded values numerically but did not enforce visually usable ghost-slice minimums and maximums. | Added semantic bounds for source slice, overlay placement, overlay height, and opacity. | `test/receipt_native_camera_session_limits_test.dart` | `closed` |
| `BUG-RECEIPT-0006` | `source_preservation` | Duplicate saved proof or OCR source paths could inflate receipt section counts and handoff metadata. | Receipt review results trimmed blank paths but did not de-duplicate repeated paths while preserving order. | Added order-preserving unique path normalization for saved proof and OCR source path lists. | `test/receipt_camera_result_test.dart` | `closed` |
| `BUG-RECEIPT-0007` | `qa_harness` | Pipeline failure tasks could ask for a regression without requiring a bug ledger category or ledger row. | Regression task generation named a failure family but did not connect the task to the permanent categorized bug ledger. | Added suggested ledger categories and required `BUG-RECEIPT-####` ledger work to Dart and shell failure-task generators. | `test/receipt_pipeline_failure_to_regression_test.dart` | `closed` |
| `BUG-RECEIPT-0008` | `native_bridge` | Android and iOS auto-capture readiness thresholds could drift from the shared Flutter receipt camera session contract. | Stable-frame, motion, brightness, and cooldown thresholds were hardcoded in native camera code instead of being sent as session arguments. | Added shared auto-capture readiness thresholds to the Dart session contract, passed them through the native bridge, and made Android/iOS use the configured values. | `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_android_bridge_auto_capture_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0009` | `receipt_line_numbering` | OCR parser line drafts did not expose a source-first receipt line label for multi-section receipt review. | Draft labels kept parser index labels primary even when section/line source metadata was available. | Added `sourceFirstLineLabel` to parser line drafts and included it in local review and privacy-safe summary maps. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0010` | `privacy_redaction` | Client proof redaction plans that requested totals context could still hide subtotal lines. | Redaction planning added tax and total lines for totals context but skipped subtotal candidates. | Keep subtotal, tax, and total lines together whenever totals context is requested, while still hiding unselected item and payment lines. | `test/expense_receipt_parser_direct_parity_test.dart` | `closed` |
| `BUG-RECEIPT-0011` | `receipt_line_review_mode` | Multi-photo receipt handoff could downgrade detailed line review intent to prices-only when an earlier section reported `pricesOnly`. | Native review depth returned the first valid section diagnostic instead of preserving the most detailed requested review depth across sections. | Prefer `detailedLines` when any captured section requests it and expose review-depth counts in privacy-safe handoff metadata. | `test/receipt_camera_result_frozen_brain_install_test.dart` | `closed` |
| `BUG-RECEIPT-0012` | `business_personal_split` | Malformed in-memory split receipt percentages could produce impossible allocation totals such as more than 100% business or negative personal amounts. | Storage hydration clamped `businessPercent`, but direct model construction from parsers, adapters, or future sync could still feed unbounded values into derived totals and serialized maps. | Bound split percentages at derived math and serialization boundaries while keeping business/personal fixed at 100%/0%. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0013` | `business_personal_split` | The active receipt entry review model could preview impossible split allocation totals before saving. | The entry draft line model used raw `businessPercent` in review labels and subtotal allocation math even though editor input and saved ledger records were bounded elsewhere. | Added bounded split percentage math to receipt entry computed fields so preview labels, business totals, and personal totals stay in range. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0301` | `camera_review_state` | Backup scanner and phone-camera fallback captures reached review with only generic `review_if_readable` action and parser-risk buckets. | Saved-photo warning details detected fallback capture sources but did not map them to explicit crop/focus review actions or OCR risk families. | Added backup-scan crop and phone-backup focus action/risk mappings while keeping fallback-only captures from becoming the primary accepted warning profile. | `test/receipt_camera_result_native_quality_test.dart`; `test/helpers/receipt_native_quality_warning_expectations.dart` | `closed` |
| `BUG-RECEIPT-0302` | `ocr_handoff_contract` | Explicit backup scanner and phone-camera fallback risk tokens could stop at the photo-review result and collapse back to generic scanner-prep review in OCR diagnostics. | OCR source handoff review did not recognize the new fallback-source action/risk tokens as a source-quality review family or parser-task diagnostic bucket. | Added `backup_capture_review`, crop/focus/totals action mapping, and backup scan/phone backup parser task counts. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0303` | `ocr_handoff_contract` | Saved-photo shadow risk could surface as a source-quality review but still report the higher-level OCR handoff warning profile as `saved_photo_ok`. | The OCR handoff warning-profile priority list was missing `receipt_handoff_warning_saved_photo_shadow_risk` after the shadow warning family was added. | Added shadow risk to the warning-profile priority list and pinned the privacy-safe review cue with a focused regression. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0304` | `qa_harness` | Future saved-photo warning profiles could be added to photo review without being wired into OCR handoff warning-profile/review-cue status. | The suite had focused regressions for individual warning families but no class-level parity contract between `acceptedPhotoWarningProfile` and `warningProfileStatus`. | Added a source contract test that compares both warning-profile lists and fails if an accepted profile is missing from OCR handoff. | `test/receipt_warning_profile_parity_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0305` | `ocr_handoff_contract` | Saved-photo parser-risk codes could be present in photo-review metadata but absent from OCR source attachment risk flags. | The OCR source risk builders emitted each saved-photo warning code and action, but not `parserRiskCode`, forcing downstream diagnostics to infer parser risk from less precise action tokens. | Emit `ocr_source_parser_risk_*` flags from both source builders and classify those tokens in OCR handoff/admin diagnostics. | `test/receipt_capture_flow_handoff_contract_test.dart`; `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0306` | `qa_harness` | Future saved-photo parser-risk codes could be added without OCR source handoff and parser/admin diagnostic support. | Pass 820 added parser-risk handoff wiring, but no class-level contract forced future `parserRiskCode` additions to update OCR handoff and diagnostics. | Added a parity contract that checks every saved-photo `parserRiskCode` has matching `ocr_source_parser_risk_*` handling in OCR handoff and diagnostics helpers. | `test/receipt_warning_profile_parity_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0307` | `receipt_line_review_mode` | OCR source handoff stored receipt review depth only as a raw document signal, so downstream diagnostics could miss whether the camera was in price-only or detailed-line mode. | `ReceiptOcrSourceHandoffSummary` counted handoff, stitch, coverage, continuation, completion, and quality families but did not classify `receipt_review_depth_*` signals. | Added explicit review-depth counts/status to the OCR source handoff contract and pinned detailed-line handoff with a focused OCR service regression. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0308` | `qa_harness` | Focused receipt OCR service tests failed to compile because the PDF inspector referenced a missing private helper. | `ReceiptPdfInspector.detectRiskFlags` called `_containsPdfName` even though the available helper lives on `AppPdfSecurityPolicy`. | Routed trailer-name detection through `AppPdfSecurityPolicy.containsPdfName` and reran the focused OCR service/PDF inspector checks. | `test/receipt_ocr_service_test.dart`; `test/receipt_pdf_inspector_edge_cases_test.dart` | `closed` |
| `BUG-RECEIPT-0309` | `receipt_line_review_mode` | Receipt review depth was classified in OCR source handoff but not exposed as typed OCR diagnostics fields for downstream UI/admin/telemetry. | Diagnostics only carried review-depth data inside `ocrSourceHandoffContract`, forcing callers to scrape a loose map instead of stable fields. | Added typed `ocrSourceReviewDepthSignalCounts` and `ocrSourceReviewDepthStatus` diagnostics fields and pinned them with a focused OCR service regression. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0310` | `receipt_line_review_mode` | Price-only versus detailed-line review depth could disappear before expense telemetry/admin rollups. | Pass 823 exposed typed OCR diagnostics, but OCR completion telemetry, parser telemetry, privacy-safe receipt events, and Command One rollups did not forward those fields. | Forward review-depth signal counts/status through OCR and parser metadata, privacy-safe events, telemetry summary rollups, and Command One map output with focused regressions. | `test/expense_screen_telemetry_failure_source_workflow_test.dart`; `test/expense_screen_telemetry_camera_health_test.dart`; `test/receipt_camera_ocr_source_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0311` | `barcode_qr_scanning` | Compact customer/account QR identifiers could become inventory lookup candidates. | Sensitive barcode payload detection required a separator or end boundary after privacy tokens, so values like `CUSTOMER12345` or `receipt_account#A12345` were treated as generic text. | Treat digits and hash separators after sensitive payload tokens as private evidence, block lookup, and keep raw QR identifiers out of privacy-safe summaries. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0312` | `barcode_qr_scanning` | Multi-image barcode/QR scan summaries did not show aggregate format or value-type coverage. | Single-image summaries exposed privacy-safe format/type counts, but batch summaries only exposed total code/QR counts and lookup candidates, leaving long-receipt scanner QA blind to UPC versus QR mix. | Add aggregate `formatCounts` and `valueTypeCounts` to batch privacy-safe summaries and pin them with a focused barcode scanner regression. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0313` | `qa_harness` | Review-result barcode handoff QA did not prove UPC/QR batch summary evidence survived the OCR-source-first scan path. | Pass 826 tested batch summaries directly but the review-result handoff regression only checked source path order and purpose. | Extend the review-result handoff regression with UPC/QR fixtures and privacy-safe format/type count assertions. | `test/receipt_capture_flow_barcode_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0314` | `native_bridge` | Android native camera diagnostics could report `lastFocusStatus` as `not_used` after continuous autofocus was configured. | CameraX was configured with continuous-picture AF request options, but the Android bridge never updated the focus-status diagnostic after binding the camera. | Set `lastFocusStatus` from the effective continuous-focus policy after native bind and pin configured/unavailable/not-requested status tokens with Android bridge QA. | `test/receipt_native_android_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0315` | `barcode_qr_scanning` | Multi-image barcode/QR summaries could hide how many receipt image inputs were actually scanned versus rejected. | Batch summaries exposed total image result count and warning buckets, but not scanned, invalid, or warning image counts for mixed valid/invalid long-receipt input batches. | Add privacy-safe scanned/warning/invalid image counts to batch summaries and pin mixed valid/invalid image behavior without exposing raw paths. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0354` | `ghost_overlap_stitching` | A mismatched stitch/OCR source contract could still route an accepted long receipt straight into filled receipt details. | The OCR-source relationship marked manual review required, but accepted-photo route labels and must-open booleans only checked kept-for-later, partial receipt, and section-order blockers. | Treat manual-review-required OCR source handoffs as `needs_review_before_ocr`, route them to OCR source review, and block direct filled-review/details opening until reviewed. | `test/receipt_ocr_source_relationship_test.dart` | `closed` |
| `BUG-RECEIPT-0355` | `ocr_handoff_contract` | Scanner preparation review risks could still label the accepted photo path as a normal prepared source. | The scanner risk set manual review required, but the handoff path label did not distinguish scanner-preparation review from a clean prepared OCR source. | Add a scanner-preparation review handoff path and block direct receipt-details opening for scanner operator-review cases. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0356` | `ocr_handoff_contract` | Attachment-panel OCR source signals could claim the clear-source-before-proof path even when the result fell back to saved proof. | The shared attachment signal builder always added `ocr_reads_clear_source_before_saved_proof`, while the capture-flow builder only added it when the review result proved a clear OCR source. | Make attachment signals add the clear-source token only when `ocrReadsClearSourceBeforeSavedProof` is true and pin the source contract. | `test/receipt_capture_flow_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0357` | `ocr_handoff_contract` | OCR-source document signals could claim a prepared non-backup source even when OCR fell back to saved proof. | Both capture-flow and attachment-panel signal builders added `ocr_reads_prepared_source_not_saved_backup` before checking whether the result actually had a clear OCR source. | Gate the prepared-source token behind `ocrReadsClearSourceBeforeSavedProof` and add an explicit saved-proof fallback review token. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0358` | `qa_harness` | Photo-review UI copy regression inspected only the old controls source after the next/details copy moved into exit actions. | The source-contract test kept asserting labels against `receipt_photo_review_controls.dart`, even though the implementation now owns exit-copy labels in `receipt_photo_review_exit_actions.dart`. | Add exit-actions source to the UI-copy contract so the test follows the modularized review controls. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0359` | `qa_harness` | Photo-review UI sizing regression inspected preview sources but missed the modular mode-control file that owns the expected segmented-control minimum size. | The source-contract test continued checking `minimumSize: const Size(92, 36)` against preview controls after that size moved into `receipt_photo_review_mode_controls.dart`. | Add mode-controls source to the UI-copy contract so modularized review control sizing remains covered. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0360` | `qa_harness` | Photo-review UI wording regression looked for stale `Next opens receipt` copy after review guidance moved to clearer open-receipt-details wording. | The test pinned old copy instead of the active review-flow language used by exit actions and preview controls. | Update the contract to require active `open receipt details` guidance while still checking review/detail labels. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0361` | `qa_harness` | Photo-review UI wording regression expected an old business/personal sentence that no active preview source owned. | Review copy now describes readable store/date/total/item-price evidence in the preview status and keeps Business/Personal/Mixed in the handoff model. | Update the UI source contract to pin active readability copy while the model contract continues pinning Business/Personal/Mixed review language. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0362` | `qa_harness` | Photo-review UI copy regression forbade the active `Use this photo` review guidance. | The test still treated `Use this photo` as stale copy, even though the active review flow intentionally offers Use/Retake/Add Photo choices after capture. | Update the contract to allow the active review choice copy while continuing to reject stale `Read receipt` and `Saved copy` wording. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0363` | `camera_capture_quality` | iOS receipt guidance toggle parity could regress and re-enable experimental blur/glare/shadow/dirty-lens claims through the general guidance setting. | Android had a focused guidance-toggle source regression, but iOS only indirectly proved the experimental quality warnings were gated. | Add an iOS source regression proving receipt guidance toggles only framing/text-size guidance and cannot touch experimental quality-warning toggles. | `test/receipt_native_ios_guidance_warning_gate_test.dart` | `closed` |
| `BUG-RECEIPT-0364` | `camera_capture_quality` | Default camera guidance copy could drift back into unproven blur/glare/shadow/dirty-lens claims even with experimental quality warnings disabled. | Existing release-control tests checked docs and native warning gates but did not pin the neutral default guidance text blocks directly. | Add Android/iOS source regressions requiring neutral readable-receipt guidance and rejecting experimental quality-warning copy from the default guidance helpers. | `test/receipt_camera_release_control_priority_test.dart` | `closed` |
| `BUG-RECEIPT-0316` | `barcode_qr_scanning` | Invalid barcode/QR image paths could consume the multi-image scan limit and prevent valid receipt segments from being scanned. | `scanImageFiles` counted invalid warning results against `maxImageCount`, even though invalid paths never invoke ML Kit. | Track input count separately, cap decoder work by scanned valid images, and keep invalid-path warning evidence privacy-safe. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0317` | `native_bridge` | iOS native camera diagnostics could keep `lastFocusStatus` as `not_used` when continuous focus was intentionally not requested. | The iOS bridge set configured/unavailable focus statuses for continuous mode but left non-continuous sessions on the default placeholder token. | Add an explicit `continuous_focus_not_requested` status for non-continuous iOS sessions and pin it with native bridge QA. | `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0318` | `native_bridge` | Native focus-status diagnostics were captured but not classified into review/admin health outcomes. | Android and iOS now emit focus-status tokens, but the review result health classifier ignored `lastFocusStatus`, so configuration failures or stale `not_used` tokens could be hidden behind otherwise-ready controls. | Add focus-status health counts, risk outcomes, and attachment risk flags for configured, unavailable, not-requested, configuration-failed, and stale not-used diagnostics. | `test/receipt_camera_result_native_ui_ready_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0319` | `ghost_overlap_stitching` | Retaking a long-receipt segment could reopen the native camera with generic continuation guidance instead of the selected segment's retake-alignment context. | Retake flow sent the guide photo path but did not forward the retake reason code or human guidance into the native session handoff. | Forward retake alignment reason/guidance through capture picking into the native camera session while keeping coverage guidance as the fallback. | `test/receipt_photo_review_retake_order_test.dart`; `test/receipt_camera_long_receipt_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0320` | `ghost_overlap_stitching` | Retaking the top receipt section could pass the next section into the native previous-section ghost overlay slot. | Top-section retake context used the next photo as the preferred guide even though native ghost guidance only supports previous-section top overlays. | Add a previous-section-only native guide path and route retakes through it so next-section context stays review-only until a true next-guide overlay exists. | `test/receipt_photo_review_retake_order_test.dart`; `test/receipt_camera_long_receipt_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0321` | `multi_photo_ordering` | Malformed retake, insert, or manual reorder metadata could reach OCR handoff without a review-required action. | Section-order diagnostics only checked basic section-number ordering and did not validate offsets, alignment context flags, top-retake ghost mismatches, or action metadata. | Add invalid-order families, privacy-safe section-order review action codes/labels, and review-required handoff counts/metadata. | `test/receipt_camera_result_section_order_test.dart`; `test/receipt_camera_result_section_order_invalid_context_test.dart`; `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0322` | `multi_photo_ordering` | Invalid receipt section-order metadata could still allow the handoff route to open receipt details/OCR. | The review-required section-order action updated labels and counts, but the route/outcome booleans only blocked kept-for-later and missing-bottom continuation cases. | Make section-order review a `needs_review_before_ocr` outcome, pause receipt-details handoff, and emit the review-needed attachment signal. | `test/receipt_camera_result_section_order_invalid_context_test.dart`; `test/receipt_camera_result_section_order_test.dart`; `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0323` | `multi_photo_ordering` | Retake or inserted-section diagnostics with missing section numbers could be treated as ordinary unordered metadata. | The malformed-order helpers returned early when original/final or anchor/final section numbers were missing, even if other retake/insert metadata proved the operation existed. | Detect missing retake original/final and insert anchor/final section numbers as invalid order metadata and require review before OCR. | `test/receipt_camera_result_section_order_invalid_context_test.dart`; `test/receipt_camera_result_section_order_test.dart`; `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0324` | `ocr_handoff_contract` | Section-order review actions could stop at photo-review metadata without reaching OCR-source attachment signals or risk flags. | The attachment signal builders emitted the general handoff outcome but did not include section-order outcome/action evidence or an OCR-source risk flag for review-required ordering. | Emit section-order outcome/action document signals and review-required OCR source risk flags from both shared capture flow and attachment-panel paths. | `test/receipt_capture_flow_handoff_contract_test.dart`; `test/receipt_camera_result_section_order_invalid_context_test.dart` | `closed` |
| `BUG-RECEIPT-0325` | `ocr_handoff_contract` | Section-order document/risk signals could be present on OCR attachments but not classified as typed section-order evidence. | `ReceiptOcrSourceHandoffSummary` counted many OCR-source signal families but did not expose a section-order family, and generic OCR-source risk bucketing could mix section-order review flags with photo-quality risks. | Add `sectionOrderSignalCounts`, expose it through privacy-safe handoff diagnostics, classify review-required section order as `section_order_review_required`, and keep section-order risk flags out of generic photo-quality counts. | `test/receipt_ocr_source_section_order_handoff_test.dart`; `test/receipt_capture_flow_handoff_contract_test.dart`; `test/receipt_camera_result_section_order_invalid_context_test.dart` | `closed` |
| `BUG-RECEIPT-0326` | `native_bridge` | Stale native UI contract tags for retired controls could make the camera look like it was missing tap-focus or lock controls instead of flagging the stale contract as a regression. | Control-readiness health checks treated contract tags as expected controls even when product policy explicitly retired tap focus, focus lock, exposure lock, and white-balance lock. | Classify retired control contract tags as retirement regressions, keep them out of missing-control readiness counts, and surface the regression as native camera UI risk. | `test/receipt_camera_result_retired_control_contract_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0327` | `native_bridge` | Native diagnostics could report retired tap-focus or lock controls as active without any stale contract tag and still avoid a camera UI risk. | The retirement guard added for stale contract tags returned before normal missing-control checks, but controls with expected=false and no tag were skipped before inspecting active actual statuses. | Inspect retired control actual statuses even when no contract tag exists, classify active retired controls as retirement regressions, and keep them out of ready/missing control counts. | `test/receipt_camera_result_retired_control_contract_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0328` | `native_bridge` | A lock-only retired-control regression could be counted as risk evidence but still leave the native UI outcome looking ready. | Native UI health outcome priority only named tap-focus retirement regressions before the normal readiness checks, so focus-lock, exposure-lock, or white-balance-lock regressions could be hidden behind ready summaries. | Promote any positive `*_retirement_regressed` count ahead of normal readiness outcomes so the primary camera health label matches the real regression. | `test/receipt_camera_result_retired_control_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0329` | `native_bridge` | Native diagnostics could claim continuous autofocus readiness while the UI contract tags omitted continuous-focus or fallback-review evidence. | Focus/readability health checks trusted `continuousFocusExpected` and focus policy fields without verifying the matching contract tags that prove the camera UI exposes the intended autofocus/readability path. | Require `continuous_focus` when continuous focus is expected, require `focus_readability_review` when fallback review is required, and update ready fixtures to keep retired lock controls disabled. | `test/receipt_camera_result_focus_contract_test.dart`; `test/receipt_camera_result_native_ui_ready_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0330` | `native_bridge` | Auto-capture diagnostics could report impossible states such as auto-ready without manual shutter fallback, ready while blocked, or allowed without user opt-in. | Capture-readiness health counted auto-capture booleans independently and did not validate the product invariant that auto capture is optional, opt-in, and never replaces the manual shutter. | Add auto-capture consistency regressions and native UI risk outcomes for missing manual fallback, ready-while-blocked, and allowed-without-request states. | `test/receipt_camera_result_auto_capture_contract_test.dart`; `test/receipt_camera_result_native_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0331` | `native_bridge` | Auto-capture diagnostics could claim `auto_capture_ready` before the required stable-frame count was reached. | Capture-readiness health trusted the readiness code without cross-checking `stableFrameCount` against `requiredStableFrames`. | Validate stable-frame diagnostics when auto capture reports ready and surface `auto_capture_ready_before_stable_regressed` as a native UI risk. | `test/receipt_camera_result_auto_capture_contract_test.dart`; `test/receipt_camera_result_native_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0332` | `native_bridge` | Auto-capture diagnostics could claim `auto_capture_ready` without carrying stable-frame evidence at all. | The previous stable-frame guard only compared counts when both values were present, so missing `stableFrameCount` or `requiredStableFrames` left auto-ready looking trustworthy. | Require auto-ready diagnostics to include positive required-frame and zero-or-positive stable-frame evidence, otherwise surface `auto_capture_ready_missing_stability_evidence`. | `test/receipt_camera_result_auto_capture_contract_test.dart`; `test/receipt_camera_result_native_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0333` | `native_bridge` | Auto-capture diagnostics could keep reporting `auto_capture_waiting_for_stability` after the stable-frame target had already been reached. | Capture-readiness health only checked early-ready states and did not cross-check waiting states against stable-frame counts. | Add `auto_capture_waiting_after_stable_regressed` when waiting readiness conflicts with the stable-frame target. | `test/receipt_camera_result_auto_capture_contract_test.dart`; `test/receipt_camera_result_native_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0334` | `native_bridge` | Auto-capture diagnostics could report a waiting-for-stability state when auto capture was not requested or without carrying stability evidence. | Waiting readiness was treated as a harmless held-back state and did not validate `autoCaptureEnabled`, `stableFrameCount`, or `requiredStableFrames`. | Add waiting-state invariants for opt-in and stable-frame evidence, surfacing `auto_capture_waiting_without_request_regressed` and `auto_capture_waiting_missing_stability_evidence`. | `test/receipt_camera_result_auto_capture_contract_test.dart`; `test/receipt_camera_result_native_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0335` | `native_bridge` | Native focus diagnostics could claim continuous focus was expected while native reported it unavailable, or claim continuous focus configured when the session said it was not expected. | Focus health counted the expected flag and native focus status independently, so contradictory focus-state diagnostics could still collapse into a less specific health outcome. | Cross-check continuous-focus expectation against the native focus status and surface expectation/status mismatch outcomes before generic focus-status buckets. | `test/receipt_camera_result_focus_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0336` | `native_bridge` | Retired tap-focus, focus-lock, or white-balance controls could be reintroduced at the native session boundary while Dart still reported the product policy as retired. | Existing review-result guards caught bad diagnostics after capture, but Android/iOS source contracts did not directly pin that native session readers ignore retired-control arguments and actual-status helpers stay forced off. | Add Android and iOS source regressions proving retired focus controls are forced off and cannot be revived by platform arguments. | `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0337` | `source_preservation` | Native capture recovery safety diagnostics still described the staged OCR source as an original photo before the saved proof copy. | Earlier storage wording was fixed in camera copy and OCR handoff, but recovery manifests and staged-photo diagnostics kept the stale original-source policy token. | Rename recovery OCR-source policy tokens to temporary full-quality staged source wording and update recovery/index regression fixtures. | `test/receipt_native_capture_recovery_index_test.dart`; `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0338` | `qa_harness` | The real-device receipt camera script could lose required device-tier, lighting, interruption, or OCR-source checks while synthetic QA still passed. | Real-device readiness lived only as prose and was not included in a permanent quality gate. | Add a real-device matrix gate, wire it into the fast guard, and pin the active script against missing matrix coverage or retired tap/original-source wording. | `test/receipt_real_device_matrix_gate_test.dart`; `tool/receipt_real_device_matrix_gate.dart` | `closed` |
| `BUG-RECEIPT-0339` | `barcode_qr_scanning` | Work-supply barcode bridge regression expected a malformed private QR value type to become an inventory suggestion. | The bridge test predated the stricter scanner privacy policy that buckets private/customer value types as sensitive and blocks lookup candidates. | Update the regression to require zero suggestions, a `sensitiveOther` value-type bucket, and no raw QR payload in summaries. | `test/work_supply_barcode_scan_bridge_test.dart` | `closed` |
| `BUG-RECEIPT-0340` | `qa_harness` | Command1 camera health summaries could show auto-capture counts without the capture-readiness reason buckets that explain why capture was manual-only, waiting, or ready. | Native receipt camera telemetry recorded auto-capture booleans and status buckets, but the Command Center snapshot did not aggregate `captureReadinessCode` evidence. | Add privacy-safe native capture-readiness code counts/top reason to the telemetry snapshot, Command Center map, Firestore sanitizer, and command-summary fixtures. | `test/expense_screen_telemetry_command_summary_test.dart`; `test/expense_screen_telemetry_camera_health_test.dart`; `test/maintainiac_firestore_expense_telemetry_command_center_parity_test.dart` | `closed` |
| `BUG-RECEIPT-0341` | `privacy_redaction` | Admin OCR-quality diagnostics could allow a receipt image preview without an explicit machine-only viewer policy. | The artifact contract required consent, redaction, and quality-only purpose, but did not encode the owner-never-sees-receipts rule as a testable field. | Add machine-quality-review-only viewer metadata and block upload eligibility whenever owner image preview is allowed. | `test/expense_admin_diagnostic_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0342` | `privacy_redaction` | Receipt camera diagnostics could be enabled without a screen-local opt-in that explains owner-visible diagnostics stay metadata-only. | Shared receipt camera settings had capture guidance and data-saver controls but no per-camera help-improve switch defaulting off. | Add a default-off receipt camera improvement opt-in setting, reset it to off, and pin settings copy against receipt-image/text upload drift. | `test/receipt_capture_settings_store_test.dart` | `closed` |
| `BUG-RECEIPT-0343` | `privacy_redaction` | Receipt capture diagnostics could still publish to the expense telemetry callback when the Help Improve Receipt Camera setting was off. | The shared attachment panel emitted native capture diagnostics directly without checking the new opt-in boundary. | Route all receipt capture diagnostics through one opt-in publisher and stamp allowed events with metadata proving owner image preview remains disabled. | `test/receipt_capture_settings_store_test.dart`; `test/expense_admin_diagnostic_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0344` | `qa_harness` | The receipt camera diagnostics opt-in boundary was pinned mostly by source-text checks instead of a direct behavior contract. | The first opt-in hardening pass routed diagnostics through a helper but did not expose the publish policy as a focused testable unit. | Add a dedicated `ReceiptCaptureDiagnosticPublishPolicy` with direct regressions for opt-out blocking and machine-only privacy envelope fields. | `test/receipt_capture_diagnostics_policy_test.dart`; `test/receipt_capture_settings_store_test.dart` | `closed` |
| `BUG-RECEIPT-0345` | `qa_harness` | Real-device camera QA could skip the new Help Improve Receipt Camera privacy boundary while synthetic tests passed. | The real-device matrix gate did not require testers to verify default-off diagnostics or owner-visible metadata-only behavior. | Add privacy diagnostics checkpoints to the real-device script and gate required phrases for default-off, metadata-only, and no owner-visible receipt image/text rules. | `test/receipt_real_device_matrix_gate_test.dart`; `tool/receipt_real_device_matrix_gate.dart` | `closed` |
| `BUG-RECEIPT-0346` | `privacy_redaction` | Opted-in receipt camera diagnostics could still forward raw paths, receipt text, or device identifiers if a future diagnostic map added them. | The publish boundary stamped privacy flags but passed the diagnostic payload through without a local allow/sanitize step. | Sanitize diagnostic keys and values before publishing, dropping receipt content, paths, raw OCR text, and device identifiers while preserving safe machine tokens/counts. | `test/receipt_capture_diagnostics_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0347` | `source_preservation` | Production camera directives could still tell future agents to preserve original receipt captures even though saved proof defaults to compressed/user-selected proof size. | The operating directive and active handoff kept old full-original retention wording after the camera policy moved to temporary full-quality OCR source plus explicit original-proof opt-in. | Reword active directives to temporary full-quality OCR source before saved-proof compression and add regressions rejecting default original-retention language. | `test/maintainiac_production_operating_directive_test.dart`; `test/receipt_active_camera_docs_focus_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0348` | `privacy_redaction` | Native camera capture diagnostics could preserve raw receipt text, file paths, or device identifiers before review handoff. | The native diagnostics sanitizer removed malformed numbers but did not reject private-looking keys or receipt/path-like string values. | Drop unsafe diagnostic keys and private-looking text values at the native camera service boundary before review receives the diagnostics. | `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0349` | `privacy_redaction` | Opted-in receipt camera diagnostics could leak tokenized device model or device name values if they contained no spaces. | The publish policy blocked `deviceId` and raw-device keys, but missed safe-token-looking `deviceModel` and `deviceName` keys. | Add device model/name keys to the camera diagnostic publish denylist and pin tokenized model/name fixtures. | `test/receipt_capture_diagnostics_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0350` | `native_bridge` | Legacy manual-focus camera settings could make a continuous-focus-capable phone enter the fallback review path instead of the release continuous-autofocus path. | Session focus enablement still depended on the stored focus mode even after tap/manual focus controls were retired from the product UI. | Force the release session to use continuous autofocus whenever the native device supports it, regardless of stale manual/tap focus settings. | `test/receipt_native_camera_session_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0351` | `native_bridge` | Active camera docs were not explicit enough that preview tap focus is forbidden and future manual focus must be explicit and reversible. | Prior hardening treated tap focus as retired/disabled in code but left room for future agents to preserve compatibility vocabulary or reintroduce screen-tap focus. | Add active documentation and doc-regression coverage banning preview tap focus while allowing only explicit reversible focus controls such as plus/minus buttons or a slider. | `test/receipt_active_camera_docs_focus_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0352` | `camera_review_state` | Native receipt camera shell tests failed because the live bottom-bar copy drifted from the expected review/photo-only labels. | The shell detail labels were shortened to `Review next` and `Photo only`, making the camera flow less explicit and breaking the existing UI contract regression. | Restore explicit `Next reviews text` and `Save photo only` copy in the camera shell bottom bar. | `test/receipt_native_camera_shell_test.dart` | `closed` |
| `BUG-RECEIPT-0353` | `camera_review_state` | Compact-phone receipt camera shell could throw when brightness controls were visible. | The exposure slider was positioned directly in the shell stack and could render without a Material ancestor in routes that embed the shell without a Scaffold. | Wrap the exposure control in a transparent Material and add compact-phone shell layout regression coverage. | `test/receipt_native_camera_shell_test.dart` | `closed` |
| `BUG-RECEIPT-0014` | `business_personal_split` | A negative split percentage typed into the receipt line editor could be treated as a positive value. | The editor stripped every non-digit/non-decimal character before parsing, which removed the minus sign before range checks ran. | Preserve the sign while normalizing percent text, then clamp negative percentages to 0 and percentages above 100 to 100. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0015` | `camera_capture_quality` | Bogus non-finite native camera diagnostics could suppress bottom-of-receipt quality warnings. | Saved-photo warning diagnostics accepted `Infinity`/non-finite numeric values as readable luma or edge scores. | Treat non-finite native diagnostic numbers as missing evidence so bottom-dark and other quality warnings remain conservative. | `test/receipt_camera_saved_photo_warning_diagnostics_test.dart` | `closed` |
| `BUG-RECEIPT-0016` | `ocr_handoff_contract` | Saved-photo glare warnings could fall through to generic scanner-prep review instead of a specific glare action. | OCR source handoff review classified bottom, dark, and blur risks but did not map glare/washed-out saved-photo flags to a source-quality review status/action. | Added saved glare review detection and `reduce_glare_or_retake` handoff action. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0017` | `multi_photo_ordering` | Retake replacement paths with leading/trailing whitespace could bypass duplicate/current-section checks. | Replacement validation checked blank strings with `trim()` but compared raw paths for duplicate and current-photo collisions. | Reject unnormalized replacement paths before mutating receipt section order. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0018` | `ghost_overlap_stitching` | Non-finite manual overlap fractions could turn an unsafe stitch request into a generic stitch exception. | Manual overlap fraction conversion multiplied image height by untrusted non-finite values before the safe-range fallback check. | Treat non-finite manual overlap fractions as unsafe overlap input so stitching returns `manual_overlap_unsafe` and preserves ordered OCR sources. | `test/receipt_stitching_manual_overlap_test.dart` | `closed` |
| `BUG-RECEIPT-0019` | `source_preservation` | Stitched receipt handoff metadata said original sections were preserved but did not expose privacy-safe source counts to prove the stitched OCR artifact came from the expected section set. | The privacy-safe handoff metadata included source-preservation labels and pair counts, but omitted original-input count, OCR-source count, and overlap pixel totals. | Added privacy-safe stitch source counts and overlap totals without leaking raw file paths. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0020` | `privacy_redaction` | Privacy-safe receipt line review/proof metadata could leak generated line IDs that contain item description text. | The line editor can build source IDs from typed descriptions, and privacy-safe contracts reused the raw source-of-truth `id`. | Use the deterministic redaction anchor as the privacy-safe line identifier while leaving the saved record ID unchanged. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0021` | `native_bridge` | Malformed native review-depth diagnostics were silently ignored, making a bad bridge value look like a clean price-only fallback. | Review-depth counts only recorded the two expected enum values and dropped any other non-empty value. | Count invalid review-depth values with privacy-safe `invalid_*` tokens while keeping the safe `pricesOnly` fallback. | `test/receipt_camera_result_frozen_brain_install_test.dart` | `closed` |
| `BUG-RECEIPT-0022` | `ocr_handoff_contract` | Blank native continuation/ghost-guide values could hide valid phone-camera backup evidence from the receipt OCR handoff. | Continuation diagnostics used null-coalescing fallback, so an empty primary string prevented reading the populated backup reason, ghost policy, repeat target, placement, or match target. | Read the first non-empty continuation diagnostic value across primary and backup sources. | `test/receipt_camera_result_continuation_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0023` | `ocr_handoff_contract` | Per-source receipt attachment document/risk signals could miss phone-camera backup continuation evidence even after the summary handoff counted it. | Attachment signal builders read only primary continuation fields for reason and ghost policy, and missing-bottom risk flags ignored backup booleans. | Use first non-empty primary/backup continuation values and include backup missing-bottom evidence in attachment risk signals. | `test/receipt_camera_result_continuation_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0024` | `business_personal_split` | Marking a single receipt line as split could save a parser review reason that always said the split started at 50% business. | The code asked the user for a custom split percent but used hardcoded review copy after applying the selected percent. | Build the review reason from the selected bounded business percent. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0025` | `business_personal_split` | The quick price-only split line sheet rejected pasted values like `80%` even though the field is labeled Business %. | The quick sheet parsed custom split text with direct `double.tryParse(text.trim())` instead of normalizing a percent sign like the full line editor. | Added a quick split percent parser that strips `%`, rejects non-finite values, and bounds the result to 0..1. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0026` | `business_personal_split` | The main line split-percent sheet rejected pasted values like `80%` even after the quick split path accepted them. | The custom split sheet still parsed the controller text directly instead of normalizing the percent sign first. | Added a custom split percent parser that strips `%`, rejects non-finite values, and returns the bounded 0..1 share. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0027` | `camera_capture_quality` | Malformed native camera diagnostics such as `Infinity` could crash integer telemetry or look like real bottom-luma/edge evidence. | Diagnostic bucket helpers rounded raw `num` values and accepted non-finite doubles instead of treating impossible camera values as missing evidence. | Added finite-value guards for diagnostic integer totals, maximums, and double quality buckets. | `test/receipt_camera_ocr_source_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0028` | `camera_capture_quality` | Native recovery telemetry counts could crash when malformed diagnostics supplied non-finite numeric values. | The recovery telemetry helper called `toInt()` on any `num` without checking whether it was finite. | Require finite numeric recovery telemetry values and treat malformed counts as zero. | `test/receipt_camera_ocr_source_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0029` | `native_bridge` | Malformed platform camera capability numbers could become fake camera counts, zoom ranges, exposure ranges, or still sizes. | Native capability map parsing accepted any `num`, including `NaN` and `Infinity`, before building the Dart camera session contract. | Require finite native capability numbers and fall back to conservative defaults. | `test/receipt_native_camera_session_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0030` | `camera_capture_quality` | Non-finite receipt coverage diagnostics could fake bottom-edge/totals completion evidence or crash count parsing. | Coverage helpers accepted raw `num` values for edge scores and candidate counts without checking that the numbers were finite. | Treat non-finite coverage diagnostic numbers as missing evidence and keep missing-bottom continuation conservative. | `test/receipt_camera_result_coverage_totals_test.dart` | `closed` |
| `BUG-RECEIPT-0031` | `camera_capture_quality` | Native capture staging could treat unusable bottom-edge numbers as present evidence even when native framing said the receipt may be cut off. | Staging cleanup accepted non-finite edge scores and defaulted bottom-edge evidence to present when cut-off risk had no usable numeric support. | Reject non-finite staging edge numbers and mark cut-off framing with unusable edge evidence as possibly cut off. | `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0032` | `privacy_redaction` | Manual receipt line redaction anchors could leak typed item descriptions when generated line IDs included private text. | The redaction anchor fallback tokenized raw `ocrSourceLineId` or saved line `id` when no OCR line number was available. | Replace raw-ID fallback anchors with deterministic private-safe fingerprints. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0033` | `privacy_redaction` | In-entry draft receipt line redaction anchors could leak typed item descriptions before the line was saved. | The draft line model still used the raw `ocrSourceLineId` or draft `id` fallback after the saved ledger model was hardened. | Use deterministic private-safe draft line tokens while preserving safe OCR line IDs. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0034` | `privacy_redaction` | In-entry receipt proof line labels could leak generated manual IDs containing typed item descriptions before save. | The draft proof-line label fallback returned raw `ocrSourceLineId` or draft `id` when no OCR line number was available. | Use safe OCR source IDs only, otherwise show a generic receipt-line label. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0035` | `multi_photo_ordering` | Retaking a receipt section could replace the wrong slot when the current section path list contained duplicates. | Retake alignment used `indexOf(targetPhotoPath)` without first proving current section paths were unique and normalized. | Reject duplicate or unnormalized current section paths before building retake alignment or replacement plans. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0036` | `multi_photo_ordering` | Adding or removing a long-receipt section after an async camera/confirm step could mutate the wrong slot when duplicate or stale paths were present. | Review actions re-found the selected receipt section with path lookup after async return instead of validating the original selected index and path. | Route add-photo and remove-photo mutations through stable slot order plans that reject duplicate current paths and stale anchors. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0037` | `qa_harness` | A lifecycle source regression still required the old unsafe receipt-photo removal path lookup after the add/remove ordering fix. | The source-level guard was not updated when removal moved from `indexOf(targetPhotoPath)` to a stable slot order plan. | Update the lifecycle regression to require insert/remove order plans and reject the old path lookup. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0038` | `camera_capture_quality` | Duplicate camera result paths could attach first-section quality diagnostics to the wrong long-receipt section. | Per-photo diagnostics and review quality handoff used `indexOf(path)` even though duplicate path keys cannot safely identify receipt sections. | Reject duplicate result/request path lists before emitting per-path diagnostics or quality checks. | `test/receipt_camera_result_best_shot_ocr_test.dart`; `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0039` | `source_preservation` | Blank or untrimmed camera result paths could still be treated as real receipt-section evidence. | Path uniqueness guards checked duplicate raw strings but did not require normalized nonblank source paths. | Require nonblank trimmed-equal paths before emitting per-photo diagnostics or review quality checks. | `test/receipt_camera_result_best_shot_ocr_test.dart`; `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0040` | `source_preservation` | Kept-for-later review results could expose normalized public paths but duplicate raw stitch input paths. | The kept-for-later factory normalized public photo paths in the constructor but built stitch input metadata from only nonblank raw paths. | Use the same unique normalized path list for kept-for-later photo paths, stitch input paths, and review-exit diagnostics. | `test/receipt_camera_result_test.dart` | `closed` |
| `BUG-RECEIPT-0041` | `camera_capture_quality` | Fractional subtotal/total candidate counts could suppress the long-receipt bottom-section prompt. | Coverage count parsing rounded malformed numeric diagnostics such as `0.6` into positive receipt evidence. | Require finite integer candidate counts before treating subtotal/total count diagnostics as evidence. | `test/receipt_camera_result_coverage_totals_test.dart` | `closed` |
| `BUG-RECEIPT-0042` | `camera_capture_quality` | Native `bottom_soft_or_missing` status without a boolean edge flag could fail to trigger the bottom-section prompt. | Coverage bottom-edge status mapping did not include the status emitted by the native camera bridges. | Treat `bottom_soft_or_missing` and equivalent bottom-missing statuses as missing bottom-edge evidence. | `test/receipt_camera_result_coverage_totals_test.dart` | `closed` |
| `BUG-RECEIPT-0043` | `ghost_overlap_stitching` | Uppercase native previous-section reason codes could use generic overlap guidance instead of bottom/totals ghost guidance. | Previous-section guide reason codes were trimmed but not normalized before matching `missing_bottom_edge_and_totals`. | Normalize previous-section reason codes to lowercase before choosing ghost policy and match target. | `test/receipt_native_camera_session_limits_test.dart` | `closed` |
| `BUG-RECEIPT-0044` | `receipt_line_numbering` | Duplicate OCR stable line IDs could silently overwrite the first receipt line number and proof label in lookup maps. | Parser handoff maps used map literals keyed by `stableLineId`, so later duplicate IDs replaced earlier source-line references. | Build line-id maps with first-occurrence wins semantics while preserving the ordered line list. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0045` | `receipt_line_numbering` | Duplicate OCR stable line IDs could still overwrite role, parser bucket, family, hint, or customer-proof visibility metadata. | Adjacent line-id metadata maps still used map literals keyed by `stableLineId` after line number maps were hardened. | Apply first-occurrence wins semantics to parser metadata and customer-proof visibility maps. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0046` | `receipt_line_numbering` | Duplicate OCR stable line IDs could appear twice in parser task or customer-proof selectable line-ID lists. | Category/review/redaction lists mapped source rows directly to `stableLineId` without de-duplicating actionable ID lists. | Preserve raw ordered line IDs for audit, but de-duplicate parser category and customer-proof line-ID lists. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0047` | `multi_photo_ordering` | Malformed retake metadata could still summarize the receipt section order as preserved. | Retake outcome selection checked preserved-slot evidence before invalid retake-order flags. | Give invalid retake-order diagnostics priority and expose an invalid evidence label. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0048` | `multi_photo_ordering` | Stale replacement paths could receive retake order diagnostics from a different accepted retake plan. | The diagnostic builder accepted a free-form replacement path list after plan creation. | Store the accepted replacement path list on the retake plan and reject diagnostic requests for mismatched lists. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0049` | `ocr_handoff_contract` | Original OCR source quality-guard review could be hidden behind the normal data-saver proof storage outcome. | Receipt proof storage outcome checked the ordinary separate OCR source plus data-saver proof path before the original-quality guard. | Prioritize original-quality guard review after saved-proof fallback and before normal data-saver proof storage. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0050` | `camera_capture_quality` | Native bottom-missing coverage statuses could let a final receipt section continue to details without another photo. | Receipt completion logic only recognized exact Flutter enum status names or an explicit needs-more boolean. | Normalize coverage status tokens and treat native bottom-missing statuses as requiring another receipt section. | `test/receipt_camera_result_completion_coverage_test.dart` | `closed` |
| `BUG-RECEIPT-0051` | `camera_capture_quality` | Attachment panel photo risk flags could miss native bottom-missing coverage statuses. | Panel photo risk logic only recognized exact Flutter enum status names after the needs-more boolean check. | Add a local normalized coverage-status helper for attachment photo risk flags. | `test/receipt_camera_ocr_source_attachment_read_test.dart` | `closed` |
| `BUG-RECEIPT-0052` | `camera_capture_quality` | Native bottom-missing attachment risk flags could omit the add-more-photos recommendation. | The panel added `add_more_photos_recommended` only for the explicit needs-more boolean, not for normalized coverage statuses. | Emit both partial-receipt and add-more-photos flags when normalized coverage status needs another receipt section. | `test/receipt_camera_ocr_source_attachment_read_test.dart` | `closed` |
| `BUG-RECEIPT-0053` | `business_personal_split` | Non-finite receipt line numeric payloads could poison split percentages, line totals, or parser confidence after map restore. | Expense ledger numeric helpers accepted `NaN` and infinity values from maps/strings. | Reject non-finite ints/doubles at the expense ledger conversion boundary and ignore non-finite percent values. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0054` | `receipt_line_numbering` | Malformed receipt section numbers could leak into proof redaction anchor IDs. | Section-line anchors used the raw section number when a section line was present, even if the section number was nonpositive. | Sanitize section anchor numbers to section 1 unless the parsed section number is positive. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0055` | `receipt_line_numbering` | Duplicate or replayed OCR line-number evidence could attach the wrong source section to a parsed receipt line. | Parser enrichment keyed OCR drafts only by line number, so a later duplicate line number could replace the first source line. | Prefer stable OCR line IDs for enrichment and use first-occurrence wins when falling back to line numbers. | `test/expense_receipt_parser_ocr_diagnostics_test.dart` | `closed` |
| `BUG-RECEIPT-0056` | `source_preservation` | Stale or foreign requested photo paths could inherit camera capture diagnostics from a different receipt result. | Capture diagnostics fell back to the request index when a requested path was not present in the source camera result. | Reject the full diagnostics handoff when requested paths are not exact members of the camera result. | `test/receipt_camera_result_best_shot_ocr_test.dart` | `closed` |
| `BUG-RECEIPT-0057` | `multi_photo_ordering` | A stale native recovery manifest could reopen duplicate receipt sections while diagnostics collapsed to one path. | Recovery review used raw existing manifest paths directly and built diagnostics with a path-keyed map literal. | Normalize and de-duplicate existing recovery photo paths before review and build immutable per-path diagnostics from that list. | `test/receipt_attachment_panel_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0058` | `source_preservation` | Stale quality or diagnostics entries could survive after receipt review result paths were normalized. | `ReceiptPhotoReviewResult` normalized photo and OCR source path lists but kept per-path evidence maps unchanged. | Filter quality, capture diagnostics, and OCR preparation diagnostics to the normalized path lists at the result boundary. | `test/receipt_camera_result_test.dart` | `closed` |
| `BUG-RECEIPT-0059` | `source_preservation` | Duplicate or whitespace-padded picked camera paths could enter receipt review before diagnostics were normalized. | `_PickedReceiptPhotos` factories kept raw picker/native/backup paths and built diagnostics from those raw lists. | Normalize, de-duplicate, and filter picked photo paths and diagnostics before adding them to review state. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0060` | `source_preservation` | Padded stored receipt attachment enum names could restore to defaults instead of the saved proof state. | Receipt attachment enum `fromName` helpers compared raw strings without trimming storage values. | Trim stored enum names before matching receipt attachment kind, data-saver, PDF, storage, and read-state values. | `test/receipt_attachment_record_metadata_test.dart` | `closed` |
| `BUG-RECEIPT-0061` | `native_bridge` | Padded native camera engine names could restore as unavailable and hide which native camera captured a receipt. | Native capability, manifest, and Hive recovery index restore paths compared raw engine names without trimming persisted bridge values. | Add a shared trim-aware native camera engine restore helper and use it at capability, manifest, and Hive index restore boundaries. | `test/receipt_native_camera_contract_test.dart`; `test/receipt_native_capture_recovery_index_test.dart` | `closed` |
| `BUG-RECEIPT-0062` | `camera_capture_quality` | Padded saved receipt performance mode values could restore as automatic and change the camera workload profile. | `ReceiptPerformanceMode.fromName` compared raw Hive strings without trimming persisted settings values. | Trim saved performance mode names before matching camera workload modes. | `test/receipt_capture_settings_store_test.dart` | `closed` |
| `BUG-RECEIPT-0063` | `source_preservation` | Edited-photo source-selection metadata duplicated the edit action and could not distinguish which receipt source was selected. | Review metadata, OCR handoff signals, and OCR source risk flags tokenized `photoEditAction` instead of the selected source outcome. | Count selected edited copies as `edited_copy_selected` while leaving edit action and replaced-original counts separate across metadata, document signals, and risk flags. | `test/receipt_camera_result_recovery_metadata_test.dart`; `test/receipt_camera_result_recovery_handoff_test.dart`; `test/receipt_photo_review_quality_handoff_test.dart`; `test/receipt_capture_flow_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0064` | `source_preservation` | Mixed current/stale picked camera paths could leave partial photo quality evidence even when diagnostics rejected the batch. | `_PickedReceiptPhotos.fromCameraResult` checked path uniqueness before quality handoff but did not prove every picked path belonged to the source camera result. | Require picked paths to be exact camera-result members before attaching quality checks. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0065` | `source_preservation` | Derived OCR source paths could inherit original-photo diagnostics or quality evidence by sharing the same list index. | Attachment and capture-flow OCR source helpers fell back to `photoPaths[index]` without proving it matched `ocrSourcePhotoPaths[index]`. | Prefer direct OCR-source path evidence and allow original-photo fallback only when the indexed original path equals the OCR source path. | `test/receipt_camera_ocr_source_attachment_read_test.dart`; `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0066` | `source_preservation` | Document-scanner backup quality checks could overwrite or trust ambiguous receipt photo paths. | `qualityChecksByPathForCameraResult` keyed quality checks by raw camera-result paths without rejecting duplicate, blank, or whitespace-padded paths. | Require camera-result photo paths to be unique and normalized before attaching backup quality checks. | `test/receipt_camera_ocr_source_attachment_read_test.dart` | `closed` |
| `BUG-RECEIPT-0067` | `source_preservation` | Persisted receipt attachments with padded or duplicate photo paths could restore duplicate raw source photos and miskey IDs, quality state, or read-state maps. | Attachment panel initial restore filtered blank photo paths but kept raw path values and duplicate entries when rebuilding receipt source state. | Normalize, trim, and de-duplicate initial photo attachments before restoring `_photoPaths`, IDs, quality checks, and read states. | `test/receipt_attachment_panel_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0068` | `camera_capture_quality` | Malformed saved attachment numbers such as `NaN` or infinity could crash receipt attachment restore or appear as impossible photo quality evidence. | `ReceiptAttachmentRecord.fromMap` converted raw numeric map values directly for byte size, page count, and photo quality fields without finite-value guards. | Require finite numeric attachment metadata before restoring integer or double camera quality fields. | `test/receipt_attachment_record_metadata_test.dart` | `closed` |
| `BUG-RECEIPT-0069` | `source_preservation` | Padded stored receipt attachment IDs or source paths could restore as distinct records and miskey source state, duplicate checks, or recovery maps. | `ReceiptAttachmentRecord.fromMap` restored raw `id` and `path` strings without normalizing storage whitespace. | Trim persisted attachment identity and source path values at the map restore boundary while leaving imported receipt text untouched. | `test/receipt_attachment_record_metadata_test.dart` | `closed` |
| `BUG-RECEIPT-0070` | `native_bridge` | Malformed native camera diagnostics such as `NaN`, infinity, nested unsafe values, or non-string keys could reach live receipt review state. | `ReceiptNativeCameraService.captureReceipt` copied the platform `captureDiagnostics` map directly before adding review and policy metadata. | Sanitize native capture diagnostics at the service boundary, keeping only string keys and JSON-safe finite values. | `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0071` | `native_bridge` | Old persisted native recovery diagnostics could restore malformed `NaN`, infinity, nested unsafe values, or non-string keys into interrupted receipt review. | Recovery index and manifest restore paths used raw `Map.from` diagnostics even after live native capture diagnostics were sanitized. | Reuse the native diagnostic sanitizer for service results, Hive recovery restore, manifest restore, and diagnostic updates. | `test/receipt_native_capture_recovery_index_test.dart`; `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0072` | `multi_photo_ordering` | Duplicate native receipt photo paths could enter review and collapse long-receipt section identity after path normalization. | `ReceiptNativeCameraService.captureReceipt` trimmed blank native photo paths but accepted duplicate source paths from the platform bridge. | Reject duplicate native original photo paths before returning a capture result to review. | `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0073` | `privacy_redaction` | Old private-looking manifest diagnostics could survive a native recovery stage update and remain in interrupted receipt recovery metadata. | Recovery-stage manifest updates sanitized new stage metadata but merged existing manifest `captureDiagnostics` back raw. | Filter existing and merged manifest diagnostics through the staging safe-key whitelist before writing recovery-stage updates. | `test/receipt_native_capture_staging_cleanup_test.dart` | `closed` |
| `BUG-RECEIPT-0074` | `multi_photo_ordering` | Stale picked diagnostics could be re-added for receipt paths outside the accepted retake order plan. | Retake diagnostics merge emitted accepted retake keys first, then appended any picked diagnostics whose keys were not in the retake map. | Return retake diagnostics only for accepted retake-plan paths so stale picked paths cannot affect section ordering metadata. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0075` | `ghost_overlap_stitching` | Duplicate long-receipt stitch input paths could be treated as adjacent receipt sections and create a bogus combined OCR image. | Stitch input normalization trimmed blanks but did not reject duplicate paths before overlap matching. | Fall back to ordered section review when stitch inputs contain duplicate receipt section paths. | `test/receipt_stitching_test.dart` | `closed` |
| `BUG-RECEIPT-0076` | `ghost_overlap_stitching` | Stitch result metadata could claim a manual match was used when the manual overlap list only contained zero placeholders. | `usedManualAdjustment` was derived from whether a manual overlap list was non-empty, not whether any pair actually used manual overlap. | Derive manual stitch usage from pair results that actually used manual overlap. | `test/receipt_stitching_manual_overlap_test.dart` | `closed` |
| `BUG-RECEIPT-0077` | `business_personal_split` | Padded stored receipt line use names could restore personal or split receipt lines as business lines. | `ExpenseLineUse.fromName` compared raw saved strings without trimming persisted values. | Trim stored business-use enum names before matching receipt line use. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0078` | `source_preservation` | Padded duplicate receipt status or confidence names could restore override-saved receipts as unchecked or exact file matches as only possible duplicates. | Duplicate receipt enum restore helpers compared raw saved strings without trimming persisted values. | Trim stored duplicate status and confidence enum names before matching receipt duplicate review state. | `test/expense_duplicate_detection_test.dart` | `closed` |
| `BUG-RECEIPT-0079` | `source_preservation` | Padded receipt-proof media purpose or backup-policy metadata could restore as a company logo/local-only asset and disappear from receipt cloud manifests. | `AppMediaAsset.fromMap` compared raw saved enum strings and cast byte size without safe parsing. | Trim restored media enum names and parse byte size from finite numeric or formatted string values. | `test/cloud_backup_manifest_test.dart` | `closed` |
| `BUG-RECEIPT-0080` | `source_preservation` | Padded app-document kind metadata could restore maintenance/job receipt PDFs as generic documents. | `AppDocumentKind.fromName` compared raw saved kind strings without trimming persisted values. | Trim stored app-document kind names before matching document receipt routing. | `test/app_document_store_test.dart` | `closed` |
| `BUG-RECEIPT-0081` | `source_preservation` | Existing PDF receipt proofs with padded hashes or paths could be missed by current-form duplicate checks during import. | `_hasDuplicateAttachment` trimmed only the incoming hash and only checked existing path blankness after trim while comparing raw values. | Compare normalized current and incoming PDF proof hashes and paths before accepting a staged import. | `test/receipt_pdf_import_copy_test.dart` | `closed` |
| `BUG-RECEIPT-0082` | `multi_photo_ordering` | Native recovery manifests with padded or duplicate staged paths could inflate recovered receipt section counts after interrupted capture. | Recovery manifest restore trimmed staged paths but kept duplicates, and attachment fallback returned raw padded paths. | De-duplicate normalized staged recovery paths and return trimmed attachment fallback paths. | `test/receipt_native_capture_recovery_record_test.dart` | `closed` |
| `BUG-RECEIPT-0083` | `multi_photo_ordering` | Native recovery Hive index entries with padded identity or duplicate staged paths could restore unstable interrupted-capture section counts. | Recovery index restore kept raw session, manifest, engine, and data-saver strings and did not de-duplicate staged photo paths. | Trim saved recovery index identity fields and de-duplicate normalized staged photo paths. | `test/receipt_native_capture_recovery_index_test.dart` | `closed` |
| `BUG-RECEIPT-0084` | `native_bridge` | Non-finite native close diagnostic counts could create false positive close-request, deferred-capture, retry, or no-photo-cancel health flags. | Shared diagnostic positive-number helpers treated any numeric value greater than zero, including infinity, as positive evidence. | Require finite positive numeric diagnostics before creating native close health evidence or positive integer section indexes. | `test/receipt_camera_result_native_close_settings_test.dart` | `closed` |
| `BUG-RECEIPT-0085` | `camera_capture_quality` | Non-finite photo quality metrics could leak into receipt review scoring or avoid conservative retake guidance. | `ReceiptPhotoQualityCheck` used raw focus, brightness, contrast, crop, and text-band values in score and label calculations. | Treat non-finite quality metrics as zero-quality evidence before calculating review score, labels, and warnings. | `test/receipt_camera_result_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0086` | `native_bridge` | Non-finite native recovery recovered-photo counts could create false OCR-source recovered-photo document signals. | OCR source signal builders checked only numeric type and greater-than-zero before emitting recovered-photo signals. | Require recovered-photo counts to be finite and positive in both capture-flow and attachment-panel OCR source signal builders. | `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0087` | `native_bridge` | Non-finite native settings-open counts could create false settings-opened UI health evidence. | Native settings health checked only numeric type and greater-than-zero before emitting settings-opened, and treated all other numeric values as settings-not-opened. | Require settings-open counts to be finite before emitting opened or not-opened settings evidence. | `test/receipt_camera_result_native_close_settings_test.dart` | `closed` |
| `BUG-RECEIPT-0088` | `ghost_overlap_stitching` | Non-finite manual stitch-overlap values could enter receipt review state before the stitch processor rejected them. | Manual overlap review controls clamped incoming values without first requiring a finite number. | Ignore non-finite manual overlap values before mutating selected stitch-pair overlap state or preview cache keys. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0089` | `native_bridge` | Non-finite native session zoom, exposure, or auto-capture threshold values could reach camera controls before capability clamping. | Android and iOS native session readers accepted raw double bridge values for zoom/exposure and auto-capture thresholds. | Route native session double values through finite-value readers before applying zoom, exposure, or auto-capture bounds. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0090` | `camera_capture_quality` | Non-finite captured-photo quality diagnostics could reach receipt review as unsafe native evidence. | Android and iOS native diagnostic rounding rejected negative values but allowed `NaN` or infinity through. | Treat non-finite native captured-photo quality values as unknown diagnostic evidence before rounding. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0091` | `camera_capture_quality` | Non-finite live preview brightness could leak into camera-result diagnostics and preview parity handoff metadata. | Dart camera-result diagnostics rounded live-to-saved brightness deltas and emitted live brightness without a finite-value guard. | Treat non-finite live preview brightness as unknown before building capture diagnostics, delta buckets, and preview parity signals. | `test/receipt_camera_result_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0092` | `camera_capture_quality` | Non-finite live brightness or exposure offsets could be classified as real glare or bracketed exposure evidence. | `ReceiptCameraCaptureEvidence` helper getters compared raw nullable doubles without rejecting infinity or `NaN`. | Treat non-finite live brightness and exposure offsets as missing evidence before computing camera quality flags and labels. | `test/receipt_camera_result_best_shot_ocr_test.dart` | `closed` |
| `BUG-RECEIPT-0093` | `camera_capture_quality` | Zero-size or non-finite manual crop rectangles could create unsafe receipt crop scaling before OCR source preparation. | `ReceiptImageProcessor.cropFile` decoded the source image before trusting raw display and crop rectangles for scale math. | Reject unusable display or crop rectangles before deriving crop pixels. | `test/receipt_image_rotation_test.dart` | `closed` |
| `BUG-RECEIPT-0094` | `camera_capture_quality` | Non-finite receipt straightening angles could reach image rotation and create unsafe derived OCR images. | `ReceiptImageProcessor.rotateFile` trusted raw numeric rotation values before calling image rotation. | Reject non-finite rotation angles before decoding and rotating receipt images. | `test/receipt_image_rotation_test.dart` | `closed` |
| `BUG-RECEIPT-0095` | `native_bridge` | Non-finite values inside native diagnostic lists could leak into staged receipt recovery manifests as `NaN` or infinity strings. | Native capture staging sanitized top-level numbers but converted iterable diagnostic entries with raw `toString()` values. | Filter non-finite numeric and exact non-finite string tokens out of staged diagnostic lists before writing recovery state. | `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0096` | `native_bridge` | Stringified non-finite native diagnostics could survive service and recovery restore sanitization as trusted camera metadata. | The shared native diagnostic sanitizer rejected numeric `NaN`/infinity values but allowed exact string tokens from platform channels. | Drop exact `NaN`, `Infinity`, and `-Infinity` string diagnostics in native service and recovery restore paths. | `test/receipt_native_camera_result_rejection_test.dart`; `test/receipt_native_capture_recovery_index_test.dart` | `closed` |
| `BUG-RECEIPT-0097` | `multi_photo_ordering` | Long-receipt retake planning could treat path aliases as different receipt sections and preserve duplicate source images in order state. | Retake/insert guards compared raw path strings and did not reject normalized aliases such as `../` segments. | Require normalized receipt photo paths and compare normalized path identity for current and replacement section lists. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0098` | `multi_photo_ordering` | Picked native or backup receipt photos could enter review with path aliases before retake/order guards ran. | Picked-photo intake normalized only whitespace and exact duplicate strings, not filesystem path aliases. | Route picked-photo intake and retake/order guards through one shared normalized receipt photo path identity helper. | `test/receipt_photo_path_identity_test.dart`; `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0099` | `multi_photo_ordering` | Receipt review cleanup and recovery checks could compare photo paths by raw string identity after normalized intake hardened ordering. | Exit/recovery helpers still used raw `contains` checks for current and initial receipt photo paths. | Use normalized receipt photo path membership for review cleanup, recoverable-photo checks, and camera-result membership. | `test/receipt_photo_path_identity_test.dart`; `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0100` | `camera_capture_quality` | Malformed quality metrics could poison OCR cleanup candidate ranking with non-finite enhancement scores. | OCR-source enhancement scoring used raw text-band, contrast, and focus metrics after broader quality scoring had finite guards. | Route cleanup enhancement score metrics through a finite-value helper before ranking OCR-source candidates. | `test/receipt_camera_ocr_source_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0101` | `receipt_line_numbering` | Malformed OCR source locations could show impossible receipt proof labels such as source line 0. | `ReceiptOcrParserLineLocation` exposed raw section and line numbers in labels and review maps. | Clamp source section and line numbers to at least 1 before building user-facing labels or maps. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0102` | `receipt_line_numbering` | Privacy-safe line summaries could disagree with clamped receipt proof labels by exposing raw impossible source numbers. | `toPrivacySafeSummaryMap` still wrote raw source section and line numbers after source labels were clamped. | Emit safe source section and line numbers in privacy-safe line summaries. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0103` | `receipt_line_review_mode` | Malformed review-depth diagnostics could create oversized invalid metadata keys in receipt review handoff summaries. | Shared diagnostic token normalization did not bound token length before review-depth counts were emitted. | Cap diagnostic tokens before building native review-depth count keys. | `test/receipt_camera_result_frozen_brain_install_test.dart` | `closed` |
| `BUG-RECEIPT-0104` | `receipt_line_review_mode` | Native camera session arguments could accept malformed receipt review-depth values before UI labels and diagnostics were built. | Android and iOS session readers assigned raw `reviewDepth` strings from the platform argument bundle. | Normalize native review-depth arguments to `pricesOnly` or `detailedLines` before use. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0105` | `ocr_handoff_contract` | Derived OCR-source attachments could lose phone-camera continuation and ghost-guide diagnostics when the OCR-ready source path differed from the original proof-photo path. | OCR-source diagnostic lookup required direct OCR-source path evidence and did not fall back to the aligned original proof-photo diagnostics for derived OCR artifacts. | Fall back by source index to the original proof-photo diagnostics when OCR-source diagnostics are absent, preserving continuation reason, ghost policy, repeat target, placement, and bottom-edge risk for derived OCR artifacts. | `test/receipt_camera_result_continuation_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0106` | `multi_photo_ordering` | Add Another Photo could drop picked native/backup camera diagnostics when no insert-order metadata existed for the picked paths. | The shared order-diagnostic merge helper emitted only order-diagnostic keys, so an empty order map erased otherwise valid picked diagnostics. | Start the merge from picked diagnostics and overlay retake/insert ordering metadata only for matching accepted paths. | `test/receipt_camera_long_receipt_guidance_test.dart`; `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0107` | `multi_photo_ordering` | Malformed insert-after metadata could be summarized as preserved order in receipt handoff evidence. | The section-order evidence label appended the insert-preserved suffix before giving invalid insert-order outcomes their own evidence marker. | Give invalid insert-order outcomes precedence and emit `insert_invalid` instead of preserved-order evidence. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0108` | `multi_photo_ordering` | Manual move-earlier/move-later actions could change long-receipt section order without privacy-safe evidence that the user intentionally reordered sections. | The review action swapped paths directly and did not stamp moved-section diagnostics or result-level handoff counts. | Route manual moves through a validated move-order plan, preserve path identity, and summarize original/final section, direction, and policy in receipt handoff counts. | `test/receipt_photo_review_retake_order_test.dart`; `test/receipt_camera_result_stitch_scanner_test.dart`; `test/receipt_camera_long_receipt_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0109` | `multi_photo_ordering` | A future caller could skip receipt sections by passing a non-adjacent manual reorder direction into the move-order plan. | Manual reorder planning rejected zero and out-of-bounds moves but did not require the direction to be exactly one section earlier or later. | Reject non-adjacent manual reorder directions at the plan boundary so section moves remain one-step, reviewable user actions. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0110` | `receipt_line_review_mode` | Expense full-item-detail preference and inventory/maintenance detailed-line needs could be lost before opening the native receipt camera. | Shared capture-flow options had no review-depth override, so native camera settings always fell back to `pricesOnly` even when the caller needed detailed receipt lines. | Add an explicit review-depth option, preserve it through continuation capture, and map expense settings plus inventory/maintenance capture areas to native review depth at the attachment UI boundary. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0111` | `camera_capture_quality` | Camera quality diagnostics could treat normalized path aliases as distinct receipt photos. | `ReceiptCameraResult.captureDiagnosticsByPhotoPath` checked raw trimmed strings instead of the shared normalized receipt photo path identity guard. | Reuse the shared normalized path identity guard before publishing per-photo camera quality diagnostics. | `test/receipt_camera_result_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0112` | `barcode_qr_scanning` | Inventory, maintenance, or receipt flows had no shared local ML Kit barcode/QR scan boundary to reuse safely. | Barcode and QR code handling existed only as manual text fields or parser hints, not as a tested local scanner service with privacy-safe summaries. | Add a shared Google ML Kit barcode scanner wrapper with image-file scanning, barcode/QR format coverage, normalized deduped lookup values, sensitive payload blocking, and privacy-safe diagnostics. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0113` | `barcode_qr_scanning` | Inventory barcode aliases had no tested bridge from shared barcode/QR scan results to inventory package suggestions. | The scanner service returned shared scan results, but work-supply alias code still expected manually typed text and private helper format guessing. | Add a work-supply scan bridge that converts safe scanner lookup values into deduped alias suggestions with privacy-safe summaries and explicit barcode format mapping. | `test/work_supply_barcode_scan_bridge_test.dart`; `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0114` | `camera_capture_quality` | Receipt camera copy and diagnostics could make tap-to-focus look like the primary sharpness solution instead of continuous autofocus with readability guidance. | The native defaults already preferred continuous focus, but shared labels, quality guidance, and contract tags still promoted tap focus as the main user action. | Make continuous focus the explicit primary policy, rename the user-facing control to focus assist, add focus/readability policy diagnostics, and preserve legacy bridge keys through a health alias. | `test/receipt_native_camera_contract_test.dart`; `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_camera_quality_guidance_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0115` | `native_bridge` | Blocked native auto-capture could report nonzero stable-frame and cooldown thresholds even when Dart intentionally sent zero because auto-capture was unavailable. | Android and iOS native session readers clamped `autoCaptureStableFrameTarget` and `autoCaptureCooldownMs` to runtime minimums without first checking whether auto-capture was allowed. | Preserve zero stable-frame and cooldown diagnostics when auto-capture is blocked; clamp only when auto-capture is allowed. | `test/receipt_native_android_bridge_auto_capture_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0116` | `source_preservation` | Generated edit or best-shot cleanup could decide whether to delete app-created receipt artifacts using raw path set membership instead of the receipt camera path-identity guard. | Cleanup compared `keptPaths.contains(path)` even though receipt review intake and retake/order guards normalize receipt photo identity. | Route generated cleanup keep checks through `receiptPhotoPathSetContains` so source-preservation decisions share the same normalized path identity contract. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0117` | `camera_capture_quality` | Tap-to-focus could still reappear as the default receipt-camera sharpness behavior even after continuous focus was declared primary. | Shared settings and native bridge fallbacks defaulted `tapFocusEnabled` to true, and the camera shell exposed "Tap text to focus" whenever the device supported it. | Make continuous autofocus/readability guidance the default, disable tap-focus by default across Dart/Android/iOS fallbacks, and remove tap-focus copy from the standard camera shell. | `test/receipt_native_camera_contract_test.dart`; `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_camera_shell_test.dart`; `test/receipt_native_android_bridge_close_controls_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0118` | `native_bridge` | Saved native receipt captures ignored the configured auto-capture cooldown after a photo was written. | Android and iOS saved-photo paths used hard-coded 2600ms cooldowns even though session arguments already carry `autoCaptureCooldownMs`. | Use `autoCaptureCooldownMs` when scheduling the post-save auto-capture cooldown on both native platforms and reject hard-coded saved-photo cooldowns in source regressions. | `test/receipt_native_android_bridge_auto_capture_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0119` | `native_bridge` | iOS could leave receipt capture stuck in-flight if the camera UI disappeared during pre-capture exposure adjustment. | The AVFoundation exposure callback used `guard let self, self.isCameraUiUsable else { return }`, which abandoned the callback without clearing capture state or recording abort diagnostics. | Clear `captureInFlight` and `pendingCloseAfterCapture`, record pre-capture exposure abort count/reason, and regress against the silent early return. | `test/receipt_native_ios_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0120` | `native_bridge` | Pre-capture exposure aborts could be recorded but still summarized as `not_evaluated` in native camera diagnostics. | Android and iOS set `lastPreCaptureExposureDecision` to `aborted_camera_closing`, but `preCaptureExposureOutcome()` did not classify that decision. | Map `aborted_camera_closing` to a stable abort outcome on both native platforms so review diagnostics preserve the abort state. | `test/receipt_native_android_bridge_diagnostics_storage_test.dart`; `test/receipt_native_ios_bridge_storage_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0121` | `native_bridge` | A failed latest receipt section could be reported as a generic close-after-capture failure even when existing captured sections were safely returned for review. | Native close outcome classifiers checked generic `capture_failed` before the explicit `back_capture_failed_returned_existing_sections` action, losing the partial-success review path. | Add a distinct `capture_failed_returned_existing_sections` outcome and surface it through native diagnostics, review handoff counts, labels, document signals, and risk flags. | `test/receipt_native_android_bridge_diagnostics_storage_test.dart`; `test/receipt_native_ios_bridge_storage_contract_test.dart`; `test/receipt_camera_result_native_close_settings_test.dart` | `closed` |
| `BUG-RECEIPT-0122` | `receipt_line_review_mode` | Padded or differently cased review-depth diagnostics from recovery/staging could be treated as invalid and downgrade detailed-line review intent. | `nativeReceiptReviewDepth` and its counts compared only exact `detailedLines`/`pricesOnly` strings after trimming. | Normalize review-depth diagnostics case-insensitively before aggregating review mode while keeping genuinely malformed values visible as bounded invalid tokens. | `test/receipt_camera_result_frozen_brain_install_test.dart` | `closed` |
| `BUG-RECEIPT-0123` | `business_personal_split` | Price-only/split receipt review contracts exposed the split label but not the clamped business and personal percentages downstream redaction/review needs. | Privacy-safe receipt line contracts included `businessUse` and label text but omitted explicit allocation percentages, making later line redaction or totals review infer from display copy. | Add clamped `businessPercent` and `personalPercent` to privacy-safe line review and proof contracts without exposing receipt item text. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0124` | `camera_capture_quality` | Receipt camera diagnostics could claim continuous autofocus was primary on devices that do not report continuous-focus support. | Native capability discovery exposed `supportsContinuousFocus`, but the session config did not use it when building focus policy, control tags, or native arguments; iOS also did not configure continuous autofocus at session startup. | Add an explicit `continuousFocusEnabled` session flag, downgrade unsupported devices to readability review, surface continuous-focus expectations in native arguments, and configure iOS continuous autofocus/exposure on session open when supported. | `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_camera_contract_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0125` | `camera_capture_quality` | Android CameraX startup could rely on default autofocus behavior even when the session policy explicitly enabled continuous focus. | The Android native bridge received focus mode policy strings but did not consume the new `continuousFocusEnabled` session flag or apply Camera2 continuous picture AF to Preview/ImageCapture builders. | Read `continuousFocusEnabled`, include it in diagnostics, and apply Camera2Interop continuous picture AF plus normal AE to Android Preview and ImageCapture when continuous focus is enabled. | `test/receipt_native_android_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0126` | `camera_capture_quality` | iOS native receipt diagnostics could omit whether the session actually enabled continuous autofocus. | Dart sent `continuousFocusEnabled` after focus capability hardening, but the iOS bridge did not read, store, or report that flag, and startup continuous autofocus configuration did not check it. | Add `continuousFocusEnabled` to iOS session state, argument restore, diagnostics, and startup focus configuration so iOS matches the shared session policy. | `test/receipt_native_ios_bridge_ui_session_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0127` | `ghost_overlap_stitching` | Uppercase or padded continuation reason codes could lose missing-bottom/totals semantics in the Flutter capture-flow handoff. | Native session guide normalization lowercased `missing_bottom_edge_and_totals`, but `ReceiptCaptureContinuationGuide` and `_previousSectionGuideDiagnostics` only trimmed the reason before classifying the bottom/totals continuation. | Normalize continuation reason codes to lowercase at the guide factory and flow diagnostics boundary before choosing ghost fractions, continuation source, and bottom/totals flags. | `test/receipt_camera_result_continuation_handoff_test.dart`; `test/receipt_capture_flow_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0128` | `ghost_overlap_stitching` | Native Android/iOS ghost-guide titles and instructions could miss the bottom/totals mode when a reason code arrived uppercase or padded. | Android and iOS native session argument readers trimmed `previousSectionReasonCode` but did not lowercase it before title, instruction, and missing-bottom/totals checks compared exact `missing_bottom_edge_and_totals`. | Lowercase native `previousSectionReasonCode` on Android and iOS argument restore and add source regressions requiring the guard. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0129` | `camera_capture_quality` | Legacy tap-focus settings could still enable a receipt-camera focus path even after continuous autofocus became the product default. | Tap-focus remained available as a descriptor/session pathway and the shared camera shell could route preview taps to focus callbacks when a legacy caller set `tapFocusEnabled`. | Retire the tap-focus descriptor, force session tap focus off, keep focus-assist tags out of the native contract, and block shell tap-focus callbacks while preserving continuous autofocus/readability guidance. | `test/receipt_native_camera_contract_test.dart`; `test/receipt_native_camera_shell_test.dart` | `closed` |
| `BUG-RECEIPT-0130` | `camera_capture_quality` | Flutter fallback capture evidence could under-warn dark or glare saved photos during review. | Shared capture evidence emitted saved-photo brightness buckets as `too_dark`/`dim`/`glare_risk`, while review warnings and native bridges use `captured_too_dark`/`captured_dim`/`captured_glare_risk`; direct glare evidence also lost precedence to generic preview-parity wording. | Emit native-style `captured_*` saved-photo brightness buckets from shared capture diagnostics and let direct glare evidence surface before generic brighter-than-preview guidance. | `test/receipt_camera_result_best_shot_ocr_test.dart`; `test/receipt_camera_saved_photo_warning_diagnostics_test.dart`; `test/receipt_camera_result_native_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0131` | `ocr_handoff_contract` | Dim saved-photo warnings could reach OCR handoff risk counts but still leave the source-quality contract looking ready. | OCR handoff dark/exposure review logic recognized darker-than-preview and failed brightness assist but not `brightness_assist_still_dim`, `dimmer_than_preview`, or their review/add-light action tokens. | Include marginal dim saved-photo warnings in the dark/exposure review family for source handoff contracts and OCR diagnostic buckets. | `test/receipt_ocr_service_test.dart`; `test/receipt_ocr_service_source_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0132` | `business_personal_split` | Padded business-use values or non-finite/overrange split percentages could misclassify receipt lines and leak impossible allocation values into proof references. | Receipt line drafts read raw `businessUse` and `businessPercent` in labels, proof references, and selected line references. | Normalize business use, clamp split percentages, default malformed split percentages to 50/50, and expose privacy-safe business/personal percentages in proof and selection contracts. | `test/receipt_line_models_test.dart` | `closed` |
| `BUG-RECEIPT-0133` | `receipt_line_numbering` | Malformed zero or negative receipt layout line numbers could create impossible proof redaction anchors and privacy-safe line references. | Receipt layout intelligence used raw `lineNumber` values for stable line IDs, redaction anchor codes, parser-visible line lists, and client-proof redaction plans. | Clamp layout line numbers to a positive safe value before building IDs, anchors, visible/default line lists, diagnostics, and redaction plan membership. | `test/expense_receipt_parser_direct_parity_test.dart` | `closed` |
| `BUG-RECEIPT-0134` | `camera_capture_quality` | Damaged receipt QA fixtures could pass while the camera guidance drifted away from sharpness, brightness, glare, or readable-text advice. | The receipt QA runner checked photo-quality action gates but did not pin light labels, focus labels, warning text, or guidance wording for damaged receipt sources. | Extend fixture expectations and QA checks for light/focus labels plus warning/guidance text needles on blur, glare, crop, and low-contrast receipt cases. | `test/receipt_qa_runner_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0135` | `camera_capture_quality` | Active docs and camera-health action copy could reintroduce tap-focus-first behavior after the receipt camera retired tap focus. | Several current handoff/spec/test-script docs still named tap focus as expected behavior, and the soft-blur recovery action told Command Center to ask the user to tap receipt text. | Replace active product guidance with continuous autofocus/readability guidance and add a telemetry regression requiring soft-blur actions to avoid tap-focus wording. | `test/expense_screen_telemetry_photo_recovery_actions_test.dart` | `closed` |
| `BUG-RECEIPT-0136` | `camera_capture_quality` | Optional auto capture could fire on stable frames that still needed manual photo-quality review. | `ReceiptCaptureReadinessDecision` blocked auto capture for critical issues and missing text bands, but not for soft focus, low contrast, dim readable frames, or other noncritical `needsReview` states. | Add a manual-only quality-review readiness state before stability can enable auto capture while keeping manual shutter available. | `test/receipt_camera_quality_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0137` | `camera_capture_quality` | The manual-only quality-review readiness state could disappear from review copy and admin handoff diagnostics. | Review context copy and native camera result diagnostics only pinned older readiness codes after `manual_only_quality_review` was added. | Add review copy for manual quality review and extend handoff regressions so health counts, reader handoff counts, and UI source copy preserve the state. | `test/receipt_photo_review_quality_handoff_test.dart`; `test/receipt_camera_result_native_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0138` | `camera_capture_quality` | Native Android/iOS auto capture could still fire when live readability guidance showed shadow or dirty-lens review risk. | Native auto-capture gating checked edges, motion, and light but did not hold back for noncritical readability review signals that Dart now treats as manual-only quality review. | Add a native `waiting_for_quality_review` holdback for shadow/haze signals and map it to `manual_only_quality_review` diagnostics on Android and iOS. | `test/receipt_native_android_bridge_auto_capture_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0139` | `multi_photo_ordering` | Long-receipt retake diagnostics could prove previous/next context existed without saying which privacy-safe section numbers should guide alignment. | Retake replacement diagnostics exposed context booleans and guidance code, but downstream review/admin tooling could not distinguish top/middle/bottom alignment context without looking at paths. | Add previous/next context section numbers to retake diagnostics while continuing to exclude receipt photo paths. | `test/receipt_photo_review_retake_order_test.dart` | `closed` |
| `BUG-RECEIPT-0140` | `ghost_overlap_stitching` | Stitch fallback metadata could omit the safe reason and failed section pair needed for admin review. | Privacy-safe receipt handoff metadata exposed overlap coverage and counts, but not the fallback reason code/label or failed adjacent section numbers. | Add stitch fallback reason and failed pair section numbers to privacy-safe handoff metadata without exposing receipt photo paths. | `test/receipt_stitch_fallback_metadata_test.dart` | `closed` |
| `BUG-RECEIPT-0141` | `ocr_handoff_contract` | OCR/proof handoff summaries could make accepted-source reuse look like a saved-proof fallback risk. | The handoff exposed decision/outcome fields, but not one privacy-safe relationship key that downstream admin QA could use to distinguish same accepted source, separate clear source, and fallback saved proof. | Add `ocrSourceProofRelationship` with explicit same-source, clear-source, missing-source, and fallback classifications and pin it with focused regressions. | `test/receipt_ocr_source_relationship_test.dart` | `closed` |
| `BUG-RECEIPT-0142` | `camera_capture_quality` | Native diagnostics could still advertise a tap-focus coordinate space after tap focus was retired from the receipt camera path. | Android and iOS capture diagnostics exposed `tapFocusCoordinateSpace`, and source-contract tests required it, making tap focus look like a supported product behavior instead of retired legacy evidence. | Replace the coordinate key with `readabilityGuidanceCoordinateSpace`, update the safe staging key list, and add negative regressions rejecting `tapFocusCoordinateSpace`. | `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0143` | `camera_capture_quality` | Active receipt camera docs could still instruct tap-focus behavior after the product moved to continuous autofocus/readability guidance. | Product standard, real-device script, state-of-art spec, and handoff report retained active tap-focus wording from older camera passes. | Replace active doc guidance with continuous autofocus/readability wording and add a doc regression rejecting tap-focus-first copy. | `test/receipt_active_camera_docs_focus_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0144` | `camera_capture_quality` | Native helper expectations could still require tap focus as an expected control after the receipt camera retired it. | Previous-section channel and staging helper assertions expected `tapFocusControlExpected` to be true even though the shared fixture and native session policy set it false. | Update helper expectations to require `tapFocusControlExpected == false` while preserving pinch zoom, exposure, lock, and manual shutter expectations. | `test/receipt_native_camera_previous_section_channel_test.dart`; `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0145` | `camera_capture_quality` | Expense Command Center telemetry could still report tap focus as the expected native camera control instead of continuous focus. | The telemetry tail and health snapshot counted `tapFocusControlExpected` but did not expose `continuousFocusExpected`, and fixtures still expected tap focus count 1. | Add continuous-focus expected counts to receipt telemetry and Command Center maps, keep retired tap-focus counts at 0, and update sanitizer/summary regressions. | `test/expense_screen_telemetry_command_summary_test.dart`; `test/expense_screen_telemetry_camera_health_test.dart`; `test/maintainiac_firestore_expense_telemetry_sanitizer_test.dart` | `closed` |
| `BUG-RECEIPT-0146` | `camera_capture_quality` | Native UI health helpers could still treat legacy `focus_assist` tags as tap-focus expectations after tap focus was retired. | The control-health helper kept a `focus_assist` alias for the `tap_focus` check even though native contracts now reject focus-assist tags. | Remove the alias and add a source-contract regression so focus-assist tags cannot re-enable tap-focus health checks. | `test/receipt_capture_flow_native_handoff_health_test.dart` | `closed` |
| `BUG-RECEIPT-0147` | `camera_capture_quality` | Receipt telemetry could still expose tap-focus usage counters as active camera workflow metrics after tap focus was retired. | The expense telemetry tail emitted `tapFocusTotal` and `tapFocusSuppressedAfterZoomTotal`, making legacy native counters look like first-class camera behavior. | Rename the counters to `legacyTapFocusTotal` and `legacyTapFocusSuppressedAfterZoomTotal` and add source regressions rejecting the old active names. | `test/expense_screen_telemetry_camera_health_test.dart`; `test/expense_receipt_assisted_review_handoff_native_test.dart`; `test/receipt_camera_ocr_source_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0148` | `camera_capture_quality` | Receipt telemetry could still expose `tapFocusEnabledCount` as an active camera setting after tap focus was retired. | The telemetry tail and metadata allowlist kept the active-sounding tap-focus enabled counter even though tap focus is now legacy-only evidence. | Rename the counter to `legacyTapFocusEnabledCount` and add a source regression rejecting the old active key. | `test/receipt_camera_ocr_source_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0149` | `camera_capture_quality` | The shared receipt camera shell could still expose an unused tap-focus callback hook after tap focus was retired. | The UI path returned false for tap focus availability, but the public shell constructor and preview controls still carried `onTapFocus` plumbing that a future edit could accidentally reconnect. | Remove the callback hook from the shell and preview controls and add a source regression proving the tap-focus hook stays absent. | `test/receipt_native_camera_shell_test.dart` | `closed` |
| `BUG-RECEIPT-0150` | `ghost_overlap_stitching` | Privacy-safe continuation handoff summaries did not prove the ghost overlap slice percent, so UI/native changes could quietly lose the intended long-receipt guide contract. | Continuation summaries counted reason, policy, placement, and match target, but omitted the numeric slice-size contract already sent to native camera surfaces. | Add sanitized ghost slice percent continuation signals and regression coverage that rejects malformed/non-integer diagnostics and receipt-text leakage. | `test/receipt_camera_result_continuation_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0151` | `ghost_overlap_stitching` | Direct native ghost-guide session config values could leak `NaN` or infinity through fallback getters if a future caller bypassed settings sanitization. | The settings builder sanitized non-finite fractions, but the public session config getters bounded only finite comparisons and could return malformed values directly. | Make the ghost-guide getters reject non-finite values and fall back to the safe long-receipt defaults, with behavior regression coverage. | `test/receipt_native_camera_session_limits_test.dart` | `closed` |
| `BUG-RECEIPT-0152` | `receipt_line_numbering` | Malformed parser signal indexes or direct draft line numbers could publish `Line 0` or negative proof/redaction anchors. | Source-section line numbers were clamped, but `ReceiptOcrParserLineDraft.lineNumber` was used directly in labels, local review maps, privacy-safe summaries, and parser handoff maps. | Add a sanitized draft line number and route labels, maps, and handoff line-number contracts through it. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0153` | `receipt_line_numbering` | Malformed negative OCR signal indexes could publish negative-looking stable line IDs even after review labels were clamped. | `ReceiptOcrParserLineSignal.stableLineId` formatted the raw index directly with `padLeft`, so negative indexes created IDs such as `ocr_line_-03_item`. | Clamp the signal index before stable ID formatting and pin the parser handoff regression to the sanitized ID. | `test/receipt_ocr_service_parser_handoff_structure_test.dart` | `closed` |
| `BUG-RECEIPT-0154` | `privacy_redaction` | Malformed native review-depth diagnostics could leak raw diagnostic text into privacy-safe receipt metadata keys. | Invalid review-depth values were tokenized directly into `invalid_*` count keys, and the raw string could contain receipt text. | Use a generic `invalid_review_depth` bucket while still surfacing the malformed diagnostic count. | `test/receipt_camera_result_frozen_brain_install_test.dart` | `closed` |
| `BUG-RECEIPT-0155` | `privacy_redaction` | Malformed edited-photo action diagnostics could leak raw diagnostic text into receipt-reader handoff count keys. | Edited-photo action counts tokenized `photoEditAction` directly, and those count keys feed privacy-safe metadata and handoff telemetry. | Whitelist known app edit actions and bucket malformed action values as `invalid_photo_edit_action`. | `test/receipt_camera_result_recovery_metadata_test.dart` | `closed` |
| `BUG-RECEIPT-0156` | `privacy_redaction` | Malformed edited-photo action diagnostics could still leak raw diagnostic text into attachment document signals and OCR-source risk flags. | Metadata counts were sanitized, but attachment and capture-flow handoff signal builders still tokenized `photoEditAction` directly. | Route attachment and capture-flow edit-action signal/risk emissions through the same known-action whitelist and generic invalid bucket. | `test/receipt_camera_result_recovery_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0157` | `privacy_redaction` | Malformed source section labels could leak private store/customer text into client-proof redaction privacy maps. | Selected-line references, redaction lines, and image section summaries emitted `sourceReceiptSectionLabel` verbatim in privacy-safe maps. | Preserve normal generic labels such as `Photo 1`, but bucket unsafe section labels as `source_section` in privacy-safe output. | `test/receipt_processing_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0158` | `privacy_redaction` | Malformed receipt line IDs or proof line reference labels could leak receipt text into client-proof privacy maps. | Line proof references and selected/redaction line maps emitted `receiptLineId` and `proofLineReferenceLabel` verbatim even when those fields came from malformed imported data. | Add privacy-safe line ID and proof-line label guards and route proof references, selected-line maps, and redaction-line maps through them. | `test/receipt_processing_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0159` | `camera_capture_quality` | Native receipt camera UI health could look ready even if continuous focus or live readability guidance had disappeared. | The native UI health summary checked visible controls and readiness, but did not treat missing continuous-focus/readability contracts as a camera-health failure. | Add focus/readability health codes and make missing continuous focus, missing continuous-focus policy, missing live readability guidance, and missing camera quality baseline outrank ready UI controls. | `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0160` | `camera_capture_quality` | A tap-focus regression could be counted in native UI health without becoming an attachment or capture-flow risk flag. | Native UI risk classification treated missing/incomplete/unknown states as risky, but did not classify `tap_focus_retirement_regressed` because it lacks those words. | Treat regressed native UI health codes as risk flags in both shared capture-flow and attachment-import signal builders. | `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0161` | `multi_photo_ordering` | Long-receipt retake handoff summaries could prove previous/next alignment context existed without preserving which safe section numbers framed the retake. | Retake diagnostics included `receiptRetakePreviousContextSectionNumber` and `receiptRetakeNextContextSectionNumber`, but review-result section-order counts only emitted boolean previous/next context codes. | Add privacy-safe previous/next retake context section buckets and pin them through receipt-reader handoff counts. | `test/receipt_camera_result_section_order_test.dart` | `closed` |
| `BUG-RECEIPT-0162` | `multi_photo_ordering` | Malformed manual reorder diagnostics could report a non-adjacent section jump as preserved order. | Retake and insert summaries had invalid-order guards, but manual reorder summaries trusted original/final section numbers and direction without checking that the move was exactly one section earlier or later. | Add manual-reorder invalid codes for unknown direction, non-adjacent moves, and missing preserved-path evidence before preserved-order outcomes are chosen. | `test/receipt_camera_result_section_order_test.dart` | `closed` |
| `BUG-RECEIPT-0163` | `ghost_overlap_stitching` | Result-level continuation handoff risks could collapse bottom-section ghost guidance into one generic review flag. | Per-source continuation risks exposed missing bottom/totals and ghost policy details, but receipt continuation handoff risk builders emitted only `ocr_requested_bottom_section_review`. | Emit actionable continuation handoff risk flags for missing bottom/totals, ready ghost guide, missing prior photo, and bottom-overlap ghost policy in both capture-flow and attachment-import builders. | `test/receipt_camera_result_continuation_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0164` | `source_preservation` | Late failed-prep cleanup could still retain accepted proof/OCR artifacts in its deletion candidate set after receipt review had accepted them. | Generated prep artifacts were tracked for failure cleanup, but successful save did not explicitly remove saved proof, OCR source, or final stitched OCR artifacts from the cleanup set before closing review. | Forget accepted proof/OCR/stitch artifacts from failed-prep cleanup candidates with normalized receipt path identity before closing the review. | `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0165` | `camera_review_state` | Future UI changes could pass a negative or overflow stitch-pair index into long-receipt review state. | The normal stitch controls emitted bounded previous/next values, but the callback stored raw indexes and the recovery clamp only repaired overflow values. | Route stitch-pair selection through a bounded helper and clamp negative recovery state to zero before manual overlap arrays are indexed. | `test/receipt_photo_review_async_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0166` | `camera_capture_quality` | Privacy-safe capability copy could still present tap-focus support as the receipt camera focus feature. | Hardware profiles tracked only legacy tap-focus support, so labels and scoring could treat tap focus as the visible focus assist even after continuous focus became primary. | Add continuous-focus support to hardware profiles, wire it from native capabilities, prefer continuous-focus copy, and keep tap focus as legacy diagnostic evidence. | `test/receipt_native_camera_privacy_diagnostics_test.dart`; `test/receipt_assistance_policy_diagnostics_test.dart`; `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0167` | `native_bridge` | Native auto-capture readability holdback used direct raw-signal equality checks that the privacy diagnostic guard rejects. | Android and iOS compared `latestReadabilitySignal` directly for shadow/haze review states instead of using a named helper/set policy. | Route native quality-review readability checks through helper/set membership on Android and iOS, preserving conservative auto-capture holdback without direct equality chains. | `test/receipt_native_camera_privacy_diagnostics_test.dart` | `closed` |
| `BUG-RECEIPT-0168` | `barcode_qr_scanning` | Privacy-safe barcode/QR summaries could leak a raw scanner value-type token. | The shared scanner added safe format/count summaries but still exposed `valueType` verbatim, and sensitive payload checks compared raw type text case-sensitively. | Sanitize barcode value-type buckets, make sensitive payload blocking case-insensitive, remove raw value-type from privacy summaries, and pass only safe buckets into inventory bridge suggestions. | `test/receipt_barcode_scanner_service_test.dart`; `test/work_supply_barcode_scan_bridge_test.dart` | `closed` |
| `BUG-RECEIPT-0169` | `barcode_qr_scanning` | Barcode/QR privacy summaries could leak raw warning text into admin diagnostics. | `ReceiptBarcodeScanResult.privacySafeSummaryMap` returned `warnings` verbatim even though result objects can be constructed outside the scanner service. | Replace raw barcode warning summaries with whitelisted warning buckets and collapse unknown warning text into `barcode_scan_warning`. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0170` | `receipt_line_numbering` | Malformed OCR source section or line numbers could survive into expense receipt line maps and parser handoff enrichment even though display labels and redaction anchors were clamped. | Expense line serialization and privacy-safe maps emitted raw OCR source metadata, and parser handoff enrichment copied raw source-location values from OCR drafts. | Added safe OCR source number getters, routed serialized/privacy-safe line maps through them, and normalized parser handoff enrichment to safe line and source-location numbers. | `test/expense_receipt_line_record_test.dart`; `test/expense_receipt_parser_ocr_diagnostics_test.dart` | `closed` |
| `BUG-RECEIPT-0171` | `business_personal_split` | Saved receipt line maps using display labels or different casing such as `Split`, `PERSONAL`, or `Business` could hydrate as business instead of preserving the user's classification. | `ExpenseLineUse.fromName` trimmed stored values but compared only exact enum names, so label-style or case-varied values fell through to the business default. | Normalize stored use names case-insensitively and accept both enum names and display labels before falling back to business. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0172` | `receipt_line_numbering` | Huge malformed OCR receipt row or section numbers could publish impossible proof labels and privacy-safe handoff maps. | Existing receipt row guards clamped negative and zero values but did not cap oversized OCR row/section metadata before labels, line IDs, or saved expense maps were built. | Cap OCR receipt row numbers at four display digits and section numbers at three display digits across parser handoff and expense receipt line maps. | `test/receipt_ocr_service_parser_handoff_structure_test.dart`; `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0173` | `qa_harness` | The receipt bug ledger gate rejected already-recorded barcode/QR and camera review-state regressions. | The human ledger and Dart gate allowed-category lists drifted behind newer closed bug categories. | Add the missing categories to both the ledger documentation and gate source so future ledger checks accept the recorded regression families. | `dart tool/receipt_bug_regression_ledger_gate.dart` | `closed` |
| `BUG-RECEIPT-0174` | `camera_capture_quality` | Blurry receipt review guidance still told users to use focus assist even though the camera direction is continuous autofocus without tap-focus-first UI. | Soft-photo guidance had older focus-assist copy while native camera policy and docs had already retired tap focus. | Replace the copy with continuous autofocus guidance and add regression assertions that focus-assist and tap-focus wording stay out of the quality guidance. | `test/receipt_camera_quality_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0175` | `business_personal_split` | Downstream receipt line selection bundles had raw totals but no explicit business/personal allocated totals for split lines. | `ReceiptSelectedLineReference` preserved receipt subtotal, tax, and total but did not expose allocated totals derived from the confirmed business percentage. | Keep raw receipt totals for audit and add business/personal allocated subtotal, tax, and total values to selected line references and bundle summaries. | `test/receipt_processing_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0176` | `privacy_redaction` | Client-proof redaction plans could hide excluded receipt lines only as synthetic placeholders instead of actual receipt line references. | `ReceiptLineSelectionBundle` stored selected lines but discarded the excluded line references, so redaction plans could not target real receipt line IDs or proof labels for black-out overlays. | Preserve excluded receipt line references in selection bundles and feed them into client-proof redaction plans while keeping legacy synthetic placeholders as fallback. | `test/receipt_processing_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0177` | `business_personal_split` | Directly constructed selected receipt line references could publish malformed split percentages or overallocated business/personal amounts. | `ReceiptSelectedLineReference` trusted raw `businessPercent` and `personalPercent` constructor values when building maps and allocated totals. | Normalize selected-line allocation at the reference boundary using business use as the source of truth and a clamped split business percentage with complementary personal percentage. | `test/receipt_processing_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0178` | `privacy_redaction` | Receipt layout redaction plans could count unknown positive line numbers as visible even though no OCR/layout line anchor existed. | `redactionPlanForLineNumbers` accepted any positive selected line number before comparing against the current layout map. | Restrict visible redaction lines to known layout line numbers and expose ignored positive requests for diagnostics without creating phantom visible anchors. | `test/expense_receipt_parser_direct_parity_test.dart` | `closed` |
| `BUG-RECEIPT-0179` | `privacy_redaction` | Receipt layout redaction diagnostics had no structured privacy-safe summary for ignored line-target requests. | Ignored redaction line numbers were tracked internally but not exposed as safe counts for QA/admin diagnostics. | Add a privacy-safe redaction summary with counts, booleans, protected content types, and anchor codes while keeping raw receipt text out. | `test/expense_receipt_parser_direct_parity_test.dart` | `closed` |
| `BUG-RECEIPT-0180` | `privacy_redaction` | Privacy event telemetry had no safe event for receipt layout redaction plans or ignored line-target counts. | Layout redaction diagnostics existed on the layout plan but were not bridged into the privacy-safe receipt event system. | Add a `fromLineRedactionPlan` privacy event factory and serialize scalar redaction counts/status booleans without raw receipt text or anchor IDs. | `test/receipt_privacy_event_test.dart` | `closed` |
| `BUG-RECEIPT-0181` | `privacy_redaction` | Admin privacy health summaries could lose layout redaction status and ignored-line counts even after events recorded them safely. | The privacy event serializer emitted layout redaction fields, but the storage policy allowlist and health aggregation did not preserve or roll them up into snapshots/Command Center maps. | Allowlist layout redaction event fields and aggregate status, visible, hidden, ignored, protected-type, and context counts through receipt privacy health snapshots. | `test/receipt_privacy_event_store_test.dart` | `closed` |
| `BUG-RECEIPT-0182` | `privacy_redaction` | Expense telemetry Command Center summaries could lose layout redaction counts even when receipt telemetry metadata carried them. | The client-proof telemetry accumulator counted selected lines and image redaction plans but ignored `clientProofLayout*` metadata. | Aggregate layout redaction status and visible, hidden, ignored, protected-type, merchant-context, and totals-context counts into expense telemetry health snapshots and Command Center maps. | `test/expense_screen_telemetry_failure_source_workflow_test.dart` | `closed` |
| `BUG-RECEIPT-0183` | `camera_capture_quality` | Active native camera service docs could still list tap focus as a hardware control after the receipt camera moved to continuous autofocus/readability guidance. | The native service spec retained an older `Tap focus` control bullet while later docs said tap focus was legacy diagnostics only. | Replace the active control wording with continuous autofocus/readability guidance and extend the active-doc focus regression to cover the native service spec. | `test/receipt_active_camera_docs_focus_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0184` | `camera_capture_quality` | The long receipt camera handoff could still tell the next model that users can tap to focus in the production capture flow. | The 2026-07-03 handoff was outside the active focus-doc regression list and retained stale tap-to-focus flow wording. | Replace the handoff flow bullet with continuous autofocus/readability guidance and add the handoff to the active-doc focus regression. | `test/receipt_active_camera_docs_focus_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0185` | `receipt_line_review_mode` | Shared receipt capture could default inventory and maintenance launches to price-only review when the caller did not force detailed lines. | `_cameraSettingsFor` used `ReceiptNativeReviewDepth.pricesOnly` as the universal fallback for every module. | Add module-specific review-depth defaults so materials inventory and maintenance/repair use detailed lines while expenses/shared stay price-only unless explicitly overridden. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0186` | `receipt_line_review_mode` | A receipt line with raw OCR evidence but a generic display description could be treated as price-only. | `isAllocationOnlyLine` looked only at the cleaned description and ignored raw OCR text, catalog item names, and parser classification evidence. | Add `hasReceiptLineDetailEvidence` so OCR/catalog/parser evidence keeps detailed review mode while privacy-safe contracts still exclude raw receipt text. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0187` | `qa_harness` | The module review-depth default regression depended mostly on source-text assertions instead of directly proving behavior. | The default review-depth rule lived in a private helper, so the test could only inspect source snippets around the shared flow. | Move the rule onto `ReceiptCaptureFlowOptions.effectiveReviewDepth` and assert module/override behavior directly. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0188` | `qa_harness` | Long-receipt continuation coverage did not directly prove module-default detailed review intent survived Add Photo/continuation guide application. | The continuation test preserved an explicit `forceReviewDepth`, but did not assert the module-based detailed defaults for inventory and maintenance after `ReceiptCaptureContinuationGuide.applyTo`. | Add continuation guide regressions proving materials inventory and maintenance stay detailed-line review while expenses stay price-only by default. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0189` | `ghost_overlap_stitching` | A manually constructed continuation guide could pass untrimmed reason text or non-finite ghost overlay fractions into shared capture options. | The factory normalized continuation values, but the public guide constructor fed values directly through `applyTo`. | Normalize manual guide reason/guidance/path values and clamp or drop malformed ghost fractions before they reach flow options. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0190` | `qa_harness` | The active camera docs focus-policy regression could miss the exact phrase `tap-focus`. | The test rejected `tap-to-focus`, `tap to focus`, and active tap-focus bullets, but did not reject the hyphenated `tap-focus` wording directly. | Add a direct active-doc guard so production docs stay continuous-autofocus/readability-first and cannot reintroduce tap-focus wording. | `test/receipt_active_camera_docs_focus_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0191` | `barcode_qr_scanning` | Generic barcode decoder failures were not directly covered as a corrupted/wrong-file regression. | Existing scanner tests covered invalid paths and platform exceptions, but not a non-platform decoder exception that could carry private receipt/barcode text. | Add a regression proving generic decoder failures become the safe `barcode_scan_failed` warning without leaking raw exception text into privacy-safe summaries. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0192` | `receipt_line_numbering` | In-entry draft receipt lines could show impossible OCR line or section labels before save. | Saved ledger lines clamp OCR line and section numbers, but private draft helpers used any positive OCR source number directly in labels and redaction anchors. | Add bounded draft line/section helpers and route proof labels, OCR labels, and redaction anchors through them. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0193` | `native_bridge` | Android/iOS native receipt cameras could re-enable tap-focus if a stale or malformed platform argument set `tapFocusEnabled` true. | Dart session config retired tap-focus, but native session readers still trusted raw `tapFocusEnabled` and `tapToFocusPolicy` arguments. | Force tap-focus disabled and the continuous-focus-primary policy at both native session argument boundaries. | `test/receipt_native_android_bridge_close_controls_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0194` | `ghost_overlap_stitching` | iOS could keep whitespace around the previous-section ghost guide photo path. | The iOS native session reader checked the trimmed path for emptiness but assigned the original untrimmed string, unlike Android. | Store the trimmed guide photo path on iOS so long-receipt ghost overlays can resolve the intended local file path. | `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0195` | `camera_capture_quality` | iOS native settings copy could still tell users to use focus assist after the receipt camera retired focus-assist/tap-focus as the product answer. | The active settings summary retained stale focus-assist wording even after the camera contract moved to continuous autofocus and readability guidance. | Replace the settings copy with continuous-autofocus/readability guidance and add a focused source regression that rejects the retired phrase. | `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0196` | `qa_harness` | Focused iOS bridge QA could require raw `latestReadabilitySignal == ...` equality checks after the privacy/readability policy moved to named set membership. | The source regression drifted behind the native policy helper introduced to prevent raw readability-signal equality chains. | Update the focused test to require `receiptQualityReviewReadabilitySignals.contains(...)` and reject direct `latestReadabilitySignal ==` checks. | `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0197` | `camera_capture_quality` | Android native settings copy could still tell users to use focus assist after the receipt camera retired focus-assist/tap-focus as the product answer. | The Android settings dialog retained the same stale focus-assist sentence that had already been removed from iOS. | Replace the Android settings copy with continuous-autofocus/readability guidance and add a source regression that rejects the retired phrase. | `test/receipt_native_android_bridge_settings_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0198` | `camera_capture_quality` | Shared Flutter receipt help could imply a generic focus control instead of continuous autofocus/readability guidance. | The help sheet and first-use intro used older `continuous focus` and `flash, focus` copy while native settings had moved to the clearer guidance contract. | Update shared help copy to continuous-autofocus/readability language and add help-flow assertions rejecting generic focus copy. | `test/receipt_camera_help_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0199` | `native_bridge` | Malformed native temporary capture IDs could leave the Android/iOS result service before being sanitized by staging helpers. | `ReceiptNativeCameraService` copied `temporaryCaptureIds` directly from the platform channel while only later staging code normalized them. | Sanitize, trim, bound, and count-limit native capture IDs at the shared service boundary before constructing `ReceiptNativeCaptureResult`. | `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0200` | `privacy_redaction` | Sanitized malformed native capture IDs could still preserve user-looking words with underscores. | The first service-boundary sanitizer removed unsafe characters but kept path-like or spaced tokens as readable words. | Preserve only already-safe native IDs and replace malformed, path-like, spaced, or oversized IDs with opaque indexed `native_capture_N` values. | `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0201` | `source_preservation` | Native camera results could hand non-local or relative receipt photo paths to OCR/staging. | The service rejected empty and duplicate paths but did not require returned original photo paths to be local absolute file paths. | Reject non-local, URL-like, relative, or NUL-containing native receipt paths before constructing the capture result. | `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0202` | `source_preservation` | Native camera results could hand a local non-image path to OCR/staging as if it were a receipt photo. | The local-path guard required absolute paths but did not require image-like file extensions. | Require native receipt paths to end in a common local image extension before constructing the capture result. | `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0203` | `qa_harness` | The native camera result rejection test was one line under the project cap, leaving no room for future receipt path regressions. | Duplicate, non-local, and non-image path regressions lived in the broader result rejection test instead of a focused path-validation file. | Split path-validation regressions into their own focused test file while preserving the same behavior coverage. | `test/receipt_native_camera_result_path_validation_test.dart`; `test/receipt_native_camera_result_rejection_test.dart` | `closed` |
| `BUG-RECEIPT-0204` | `source_preservation` | NUL-containing native receipt paths were guarded in code but not pinned by a direct regression. | The path-validation test split created room for more source-preservation edge cases, but the NUL guard still lacked direct focused coverage. | Add a focused native path-validation regression for NUL-containing receipt photo paths. | `test/receipt_native_camera_result_path_validation_test.dart` | `closed` |
| `BUG-RECEIPT-0205` | `multi_photo_ordering` | Native camera results could return more receipt sections than the Dart session allowed. | The service trusted the returned path count after checking emptiness/safety/duplicates, but did not compare it with `config.maxSectionCount`. | Reject native capture results whose path count exceeds the session max section count. | `test/receipt_native_camera_result_path_validation_test.dart` | `closed` |
| `BUG-RECEIPT-0206` | `multi_photo_ordering` | Rapid native long-receipt captures could theoretically reuse timestamp-only temp filenames. | Android and iOS capture file helpers named receipt photos with millisecond timestamps only. | Add UUIDs to native receipt capture temp filenames and pin the source contract with Android/iOS bridge regressions. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0207` | `source_preservation` | Oversized native receipt photo diagnostics could move into OCR/staging despite the session byte budget. | The native service forwarded sanitized diagnostics but did not compare `photoByteSize` or `totalCapturedByteSize` against `maxLocalPhotoBytes`. | Reject native capture results whose sanitized byte-size diagnostics exceed the session local photo byte budget. | `test/receipt_native_camera_result_path_validation_test.dart` | `closed` |
| `BUG-RECEIPT-0208` | `source_preservation` | Native byte-budget validation could miss an oversized total if a smaller `photoByteSize` appeared first. | The helper returned the first positive byte-size diagnostic instead of evaluating all known byte-size fields. | Enforce the largest positive value across `photoByteSize` and `totalCapturedByteSize`. | `test/receipt_native_camera_result_path_validation_test.dart` | `closed` |
| `BUG-RECEIPT-0209` | `source_preservation` | Native Android/iOS capture callbacks could append an over-budget receipt photo before Dart rejected it later. | The native callbacks tracked total bytes but did not delete or reject the just-saved photo before adding it to captured sections. | Reject and delete over-budget native captures before section append, while preserving already-captured sections on close-after-capture flows. | `test/receipt_native_android_bridge_auto_capture_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0210` | `qa_harness` | Android auto-capture QA could require raw `latestReadabilitySignal == ...` equality checks after the privacy/readability policy moved to named set membership. | The Android auto-capture source regression drifted behind the native readability helper introduced to prevent raw readability-signal equality chains. | Update the focused test to require `receiptQualityReviewReadabilitySignals().contains(...)` and reject direct `latestReadabilitySignal ==` checks. | `test/receipt_native_android_bridge_auto_capture_test.dart` | `closed` |
| `BUG-RECEIPT-0211` | `multi_photo_ordering` | Native over-budget add-photo attempts could return existing sections even when the user was not closing the camera. | The over-budget guard returned existing captured sections whenever any prior section existed, instead of requiring a pending close-after-capture flow. | Gate the return-existing-sections path behind `pendingCloseAfterCapture` and keep ordinary add-photo failures on the camera for retry. | `test/receipt_native_android_bridge_auto_capture_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0212` | `camera_capture_quality` | Dormant native tap-to-focus gesture paths could return despite the receipt camera policy retiring tap focus. | Android and iOS still carried disabled tap-focus gesture handlers and readiness checks after continuous focus became the primary receipt-camera strategy. | Remove native tap-focus gesture handlers, keep pinch zoom/readability controls, and pin active sources against tap metering regressions. | `test/receipt_camera_native_bridge_layout_test.dart`; `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_android_bridge_close_controls_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0213` | `qa_harness` | Android import hygiene QA rejected legitimate imports used by continuous focus and unique native receipt filenames. | The expected import set was stale and did not include `CaptureRequest`, `Camera2Interop`, `ExperimentalCamera2Interop`, or `UUID`. | Update the import hygiene expected set so it still catches copied import blocks without rejecting current native camera hardening. | `test/receipt_native_android_import_hygiene_test.dart` | `closed` |
| `BUG-RECEIPT-0214` | `camera_capture_quality` | Native diagnostics could still report tap focus as an expected receipt-camera control if a stale flag flipped. | The runtime gesture path was retired, but `tapFocusControlExpected` still read from `tapFocusEnabled` in Android and iOS diagnostics. | Hard-code the expected tap-focus diagnostic to false and pin both native bridge contracts against deriving it from `tapFocusEnabled`. | `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0215` | `camera_capture_quality` | Native control readiness summaries could look unhealthy because retired tap/manual-focus controls were missing. | The readiness summary still included tap focus, focus lock, exposure lock, and white-balance lock statuses after those controls were retired. | Limit readiness summary evaluation to active receipt-camera controls and keep retired-control values as separate diagnostics only. | `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0216` | `native_bridge` | Native control readiness summaries could mark older or limited devices unhealthy when optional hardware controls were unavailable. | Optional controls such as pinch zoom, brightness slider/reset, and torch were still evaluated as readiness blockers even though their actual statuses are already reported separately. | Limit core readiness to required capture controls while preserving optional hardware control diagnostics for admin/device review. | `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0217` | `native_bridge` | Retired focus/exposure/white-balance lock controls could still be reported as expected receipt-camera controls. | Android, iOS, and Dart bridge diagnostics still derived lock-control expected fields from legacy lock settings after the manual lock path was retired. | Hard-code retired lock-control expected fields to false across native diagnostics and Dart channel arguments while preserving legacy setting fields separately. | `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart`; `test/helpers/receipt_native_camera_previous_section_channel_expectations.dart`; `test/helpers/receipt_native_capture_staging_manifest_expectations.dart` | `closed` |
| `BUG-RECEIPT-0218` | `native_bridge` | Current Dart receipt camera sessions still advertised retired lock controls through enabled flags, contract tags, and settings descriptors. | The native path retired manual lock controls, but the Dart session/settings layer still enabled lock flags when hardware support existed. | Force current lock enablement false, remove lock descriptors from current settings, replace lock-unavailable policy noise with a retired-lock policy code, and update channel/staging/UI regressions. | `test/receipt_native_camera_contract_test.dart`; `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_camera_previous_section_channel_test.dart`; `test/receipt_native_capture_staging_test.dart`; `test/receipt_camera_result_native_ui_ready_test.dart` | `closed` |
| `BUG-RECEIPT-0219` | `native_bridge` | Active specs and dormant tag-builder lines could still reintroduce retired focus/exposure/white-balance lock controls. | The current service spec still listed lock controls, and the session tag builder kept conditional lock-tag additions even after current lock enablement was forced false. | Remove active lock-control spec wording, remove dormant lock-tag additions, and add active-doc/session source regressions. | `test/receipt_active_camera_docs_focus_policy_test.dart`; `test/receipt_native_camera_session_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0220` | `native_bridge` | Retired lock controls could make native capability policy look degraded on otherwise capable devices. | The policy helper added a retired-lock code whenever lock controls were false, even though false is now the intentional product policy. | Remove retired lock controls from capability policy scoring and pin capable sessions against retired-lock policy noise. | `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_camera_previous_section_channel_test.dart` | `closed` |
| `BUG-RECEIPT-0221` | `native_bridge` | Retired tap-focus controls could still block a capable device from reporting full camera assist. | Capability policy scoring treated intentionally disabled tap focus as a missing device capability instead of a retired product control. | Remove tap focus from capability policy scoring while keeping tap-focus retirement health regressions in the native UI health layer. | `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_camera_contract_test.dart`; `test/receipt_native_camera_previous_section_channel_test.dart` | `closed` |
| `BUG-RECEIPT-0222` | `native_bridge` | The Dart native service payload could reconnect retired tap-focus expected diagnostics from session config. | `_nativeControlContract` derived `tapFocusControlExpected` from `config.tapFocusEnabled`, leaving the service boundary dependent on upstream retirement guards. | Hard-code the service contract flag to false and add a source regression rejecting derivation from `config.tapFocusEnabled`. | `test/receipt_native_camera_service_basics_test.dart` | `closed` |
| `BUG-RECEIPT-0223` | `native_bridge` | The Dart native service payload could reconnect retired focus, exposure, or white-balance lock enablement from session config. | `_sessionArguments` forwarded lock-enabled fields from config even though the product policy retired those controls. | Hard-code retired lock enablement false at the service boundary and add a source regression rejecting config-derived lock enablement. | `test/receipt_native_camera_service_basics_test.dart` | `closed` |
| `BUG-RECEIPT-0224` | `native_bridge` | The Dart native service payload could still forward retired tap-focus enablement from session config. | `_sessionArguments` sent `tapFocusEnabled` from config while only the expected-control diagnostic was hard-coded false. | Hard-code `tapFocusEnabled` false at the service boundary and extend the service source regression against config-derived tap-focus payloads. | `test/receipt_native_camera_service_basics_test.dart` | `closed` |
| `BUG-RECEIPT-0225` | `native_bridge` | Android and iOS native session readers could still re-enable retired white-balance lock diagnostics from stale arguments. | Native argument readers trusted `whiteBalanceLockEnabled` or `whiteBalanceMode == locked` even after manual lock controls were retired. | Hard-code native `whiteBalanceLockEnabled` false at both platform argument boundaries and update bridge regressions to reject stale argument trust. | `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0226` | `native_bridge` | Android and iOS native diagnostics could serialize retired white-balance lock state from mutable controller variables. | Diagnostic payload builders emitted `whiteBalanceLockEnabled` from platform state even though lock controls are retired and expected false. | Hard-code serialized `whiteBalanceLockEnabled` diagnostics false and update storage-contract regressions to reject variable-derived lock diagnostics. | `test/receipt_native_android_bridge_diagnostics_storage_test.dart`; `test/receipt_native_ios_bridge_storage_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0227` | `qa_harness` | Native capture staging fixtures still normalized retired lock attempts and locked white-balance status. | The fixture used locked focus/exposure/white-balance modes and nonzero lock counts after the product retired manual lock controls. | Reset fixture diagnostics to continuous focus, auto exposure/white balance, zero lock attempts, and `not_requested` lock status. | `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0228` | `privacy_redaction` | QR URL payloads could become inventory lookup values and leak customer/order tokens. | Barcode sensitivity allowed `url` value types through lookup normalization even though receipt QR URLs can contain private session or order data. | Treat URL barcode payloads as sensitive and add a regression proving they stay out of lookup values and privacy-safe summaries. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0229` | `privacy_redaction` | Malformed barcode value-type labels containing sensitive terms could be treated as harmless `other` payloads. | Barcode value-type bucketing only matched exact official ML Kit names, so malformed labels like customer/private/email variants could still expose raw QR payloads as inventory lookup values. | Classify unknown value-type labels containing privacy tokens as `sensitiveOther` and block lookup values while keeping summaries privacy-safe. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0230` | `privacy_redaction` | Sensitive QR payloads mislabeled as generic text could still become inventory lookup values. | Lookup eligibility trusted the privacy-safe value-type bucket and did not inspect raw payload patterns such as URLs, Wi-Fi configs, sessions, or customer tokens. | Add payload-pattern sensitivity checks for URL/contact/Wi-Fi/token/customer-style QR content while keeping benign text QR inventory values available. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0231` | `receipt_line_numbering` | Duplicate stable OCR line IDs could collapse line-number/proof maps without a review-needed handoff signal. | The handoff deliberately preserved the first duplicate map entry for compatibility but did not expose the identity collision in the privacy-safe contract. | Add duplicate line ID counts and a `duplicate_line_ids_need_review` status to the parser handoff contract so downstream receipt proof/review can avoid silent trust. | `test/receipt_ocr_parser_handoff_line_identity_test.dart` | `closed` |
| `BUG-RECEIPT-0232` | `receipt_line_numbering` | Duplicate OCR line IDs could still leave readiness paths reporting usable parser or split-classification states. | The duplicate identity signal was exposed in the handoff contract but not wired into parser, downstream, merchant-independent, mixed-classification, or lean-local OCR readiness. | Treat duplicate line IDs as a review blocker across readiness gates and parser review task counts. | `test/receipt_ocr_parser_handoff_line_identity_test.dart` | `closed` |
| `BUG-RECEIPT-0233` | `receipt_line_review_mode` | Padded or case-varied saved receipt review style values could silently fall back to prices-only review. | `ExpenseReceiptReviewStyle.fromName` matched only the exact enum name and did not normalize persisted or restored values. | Trim and case-normalize receipt review style hydration while keeping unknown values safely on prices-only. | `test/expense_settings_store_test.dart` | `closed` |
| `BUG-RECEIPT-0234` | `receipt_line_review_mode` | Native review-depth diagnostics using snake-case or hyphenated strings could be treated as invalid and fall back to prices-only metadata. | `_normalizedNativeReceiptReviewDepth` lowercased values but did not remove separators from native bridge payloads. | Normalize spaces, underscores, and hyphens before mapping review-depth diagnostics. | `test/receipt_camera_result_frozen_brain_install_test.dart` | `closed` |
| `BUG-RECEIPT-0235` | `native_bridge` | Android and iOS native argument readers could downgrade review-depth intent when bridge values used snake-case or hyphenated strings. | Native `safeReceiptReviewDepth` helpers trimmed exact values only and did not normalize separators or case before comparing review-depth payloads. | Normalize native review-depth arguments by removing separators and comparing lowercase tokens on both Android and iOS. | `test/receipt_native_android_bridge_ui_contract_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0236` | `receipt_line_review_mode` | Corrupted non-string Hive values for receipt review style could crash expense settings and camera handoff. | The settings getter cast the stored dynamic Hive value directly to `String?`. | Type-check the stored review-style value before hydration and fall back safely to prices-only for non-string values. | `test/expense_settings_store_test.dart` | `closed` |
| `BUG-RECEIPT-0237` | `camera_capture_quality` | Invalid native bottom/top luma readings could be bucketed as top/bottom brightness-close evidence. | Android returned `-1` for invalid captured bottom/top luma deltas and both native bucket helpers treated non-finite deltas as the default close bucket. | Return the existing unknown sentinel for missing Android luma and classify non-finite bottom/top deltas as `unknown` on Android and iOS. | `test/receipt_native_android_bridge_close_controls_test.dart`; `test/receipt_native_ios_bridge_ui_session_test.dart` | `closed` |
| `BUG-RECEIPT-0238` | `camera_capture_quality` | Non-finite native live-to-saved luma deltas could be bucketed as saved-preview match evidence. | The live-to-saved delta helpers did not reject non-finite preview/saved luma inputs or non-finite deltas before parity bucketing. | Return the existing unknown sentinel for non-finite live/saved luma and classify non-finite live-to-saved deltas as `unknown` on Android and iOS. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0239` | `camera_capture_quality` | Non-finite native live brightness could be classified as healthy lighting or aligned exposure. | Android/iOS live brightness buckets and exposure mismatch helpers only rejected negative brightness, so `NaN` could fall through to the default healthy path. | Treat non-finite live brightness as `unknown` in native brightness buckets and saved-photo exposure mismatch diagnostics on Android and iOS. | `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0240` | `camera_capture_quality` | Malformed native live framing bounds could appear as a valid framed receipt. | Android/iOS live framing paths trusted a `found=true` framing object even when width, height, coverage, or edge ratios were non-finite or invalid. | Add shared native usable-bounds guards so malformed live framing emits invalid-bounds diagnostics and perspective correction skips unsafe geometry. | `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0241` | `camera_capture_quality` | Optional native auto-capture could count malformed live framing as edge-ready. | Auto-capture readiness checked `found=true`, not touching edges, and strong/usable edge labels, but did not require the live framing bounds to be finite and valid. | Require the native usable-bounds guard before Android/iOS auto-capture can count a frame as edge-ready. | `test/receipt_native_android_bridge_auto_capture_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0242` | `camera_capture_quality` | Non-finite native live readability samples could be reported as `lighting_ok`. | Android/iOS live readability fallback logic did not reject non-finite brightness, motion, or shadow scores before assigning healthy readability. | Emit `readability_unknown` for non-finite live readability inputs while keeping manual capture available. | `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0243` | `camera_capture_quality` | Non-finite native live brightness could drive auto-exposure adjustments. | Android/iOS auto-exposure guards rejected negative brightness but allowed infinity through the brighten/dim comparison paths. | Require finite non-negative brightness before native auto-exposure can stabilize or apply brighten/dim adjustments. | `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0244` | `camera_capture_quality` | Non-finite native pinch zoom input could reach camera zoom controls. | Android/iOS pinch handlers multiplied current zoom by raw gesture scale without rejecting non-finite zoom or scale values first. | Reject non-finite zoom state, gesture scale, or computed zoom with `zoom_invalid_scale` before writing native camera zoom. | `test/receipt_native_android_bridge_close_controls_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0245` | `camera_capture_quality` | Non-finite iOS manual exposure bias could reach native camera exposure controls. | iOS clamped raw slider bias with `min/max` but did not normalize non-finite values or guard the final `setExposureTargetBias` boundary. | Normalize malformed slider bias to zero before clamping and reject non-finite direct bias at the setter boundary. | `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0246` | `camera_capture_quality` | Non-finite live brightness could drive pre-capture exposure changes. | Android/iOS pre-capture exposure prep rejected negative brightness but allowed infinity into last-second brighten/dim target selection. | Require finite non-negative live brightness before pre-capture exposure can adjust; otherwise use the existing `brightness_unknown` skip path. | `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0247` | `camera_capture_quality` | Non-finite saved-photo quality samples could be bucketed as glare or high-contrast evidence. | Android/iOS captured brightness and sharpness bucket helpers rejected negative values but allowed `NaN` to fall through to the final healthy/risk buckets. | Treat non-finite saved-photo brightness and sharpness as `unknown` before quality scoring. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0248` | `camera_capture_quality` | Non-finite saved-photo vertical samples could look like even receipt quality. | Android/iOS vertical quality helpers only rejected negative luma values, allowing non-finite band luma or edge scores to fall through to even/blur decisions. | Treat non-finite vertical luma and edge samples as `unknown` before vertical quality scoring. | `test/receipt_native_android_bridge_close_controls_test.dart`; `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0249` | `camera_capture_quality` | Non-finite bottom-edge evidence could mark the receipt bottom visible. | Android/iOS bottom-edge status allowed live edge coverage fallback even when saved-photo bottom edge or edge scores were non-finite. | Require finite saved-photo bottom-edge score, edge score, and live edge coverage before bottom-edge visibility can be evaluated. | `test/receipt_native_android_bridge_close_controls_test.dart`; `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0250` | `multi_photo_ordering` | Malformed previous-section ghost crop fractions could create unsafe long-receipt overlap crops. | Android/iOS ghost crop helpers trusted mutable source start/height fractions at the crop boundary even though only session argument loading sanitized them. | Reapply finite bounded source start/height fallbacks inside native ghost crop helpers before cropping overlap guides. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0251` | `multi_photo_ordering` | Unsafe previous-section guide paths could be loaded as long-receipt ghost overlays. | Android accepted any existing file and fell back to `setImageURI`, while iOS checked the untrimmed path before image decoding. | Require trimmed local absolute image guide paths and hide the guide when the image cannot be decoded. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_long_receipt_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0252` | `multi_photo_ordering` | Dart session config could mark an invalid previous-section guide path as active before native received the long-receipt handoff. | The session factory only trimmed non-empty guide paths and left native code as the first defense against relative paths, URLs, non-image paths, or NUL-tainted strings. | Sanitize previous-section guide paths at the Dart session boundary and only allow local absolute image-like paths before enabling reason, guidance, ghost fractions, or channel guide arguments. | `test/receipt_native_camera_session_limits_test.dart` | `closed` |
| `BUG-RECEIPT-0253` | `multi_photo_ordering` | Capture-flow continuation options and diagnostics could report an unsafe previous-section guide photo as available even though the camera session would reject it. | Continuation guide creation, manual guide application, and flow diagnostics only trimmed guide paths instead of applying the same local absolute image-path contract used by native camera sessions. | Reuse the native camera local-image sanitizer at capture-flow continuation boundaries before options or diagnostics can mark the guide photo active. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0254` | `multi_photo_ordering` | Direct malformed continuation ghost fractions could produce non-finite diagnostics or crash slice-percent rounding. | Capture-flow diagnostics emitted raw optional ghost fractions and rounded raw source-height values instead of bounding them like guide application and session construction. | Bound all previous-section ghost diagnostic fractions to finite 0..1 values before emitting metadata or computing slice percent. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0255` | `camera_review_state` | A negative or oversized continuation `initialSelectedIndex` could open review on an older existing receipt photo instead of the newly staged section. | Shared capture flow added the raw requested selected index to the existing-photo offset and relied on the review screen's global clamp afterward. | Clamp the requested selected index inside the newly staged photo count first, then add the existing-photo offset. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0256` | `camera_review_state` | Raw, duplicated, or unnormalized initial review photo paths could become the review screen's in-memory source list while adjacent quality/recovery logic used path-set normalization. | `ReceiptPhotoReviewScreen` copied `widget.initialPhotoPaths` directly and other helpers mixed raw widget paths with normalized path-set checks. | Normalize and de-duplicate initial photo paths once, then use that normalized list for review state, initial quality checks, review-mode selection, and recoverable-photo detection. | `test/receipt_capture_flow_shareability_test.dart`; `test/receipt_photo_review_save_lifecycle_test.dart` | `closed` |
| `BUG-RECEIPT-0257` | `camera_review_state` | Initial photo-review quality checks could keep raw, duplicate, or non-review path keys after the review photo list was normalized. | `_initialQualityChecksByPath` spread `widget.initialQualityChecksByPath` directly, mixing raw caller keys with normalized review photo paths. | Normalize incoming quality-check map keys, keep only keys present in the normalized initial photo list, and preserve first normalized key wins. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0258` | `camera_review_state` | Initial photo-review capture diagnostics could keep raw, duplicate, or non-review path keys after the review photo list was normalized. | `_captureDiagnosticsByPath` spread `widget.initialCaptureDiagnosticsByPath` directly, mixing raw caller keys with normalized review photo paths. | Normalize incoming diagnostics map keys, keep only keys present in the normalized initial photo list, freeze accepted maps, and preserve first normalized key wins. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0259` | `camera_review_state` | Review-opening selected-index math could still use raw existing-photo counts after the review screen normalized and de-duplicated initial photo paths. | `_reviewInitialSelectedIndex` added the new-section index to `options.initialPhotoPaths.length`, which could include invalid or duplicate paths the review screen drops. | Use `uniqueNormalizedReceiptPhotoPaths(options.initialPhotoPaths).length` as the existing-photo offset before selecting a newly staged section. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0260` | `camera_review_state` | Attachment-panel picked-photo review could open on the wrong section when existing receipt photo paths contained invalid or duplicate entries. | `reviewPickedPhotoPaths` used raw `_photoPaths.length` for the first-new-photo index even though the review screen normalizes and de-duplicates initial photo paths. | Use `uniqueNormalizedReceiptPhotoPaths(_photoPaths).length` before opening photo review so the selected index matches the review screen's source list. | `test/receipt_camera_capture_layout_test.dart`; `test/receipt_camera_native_bridge_layout_test.dart` | `closed` |
| `BUG-RECEIPT-0261` | `camera_review_state` | Recovered native-capture review could open on the wrong section when existing receipt photo paths contained invalid or duplicate entries. | `reviewRecoveredCapture` used raw `options.initialPhotoPaths.length` for the first recovered photo index even though review normalizes and de-duplicates initial photo paths. | Use `uniqueNormalizedReceiptPhotoPaths(options.initialPhotoPaths).length` before opening recovered-photo review. | `test/receipt_capture_flow_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0262` | `qa_harness` | Long-receipt guidance source regression still required the retired raw initial photo count after the review screen moved to normalized initial paths. | The source test asserted `widget.initialPhotoPaths.length` instead of the normalized `_initialPhotoPaths.length` review-mode gate. | Update the regression to require normalized initial path setup and review-mode gating. | `test/receipt_camera_long_receipt_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0263` | `camera_review_state` | Adding picked receipt photos from the attachment panel could drop existing photo capture diagnostics when opening review. | `reviewPickedPhotoPaths` merged existing quality checks but passed only new `initialCaptureDiagnosticsByPath`, losing diagnostics for already attached receipt sections. | Merge `_photoCaptureDiagnosticsByPath` with new pick diagnostics before opening `ReceiptPhotoReviewScreen`. | `test/receipt_attachment_panel_recovery_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0264` | `source_preservation` | Accepted review results could lose existing attachment IDs or read states when review normalized receipt photo paths. | `_acceptReviewedPhotoResult` and `reviewReceiptPhotos` restored IDs/read states with raw map key lookups against `result.photoPaths`. | Restore previous IDs and read states by normalized receipt photo path identity instead of raw string equality. | `test/receipt_camera_ocr_source_attachment_read_test.dart` | `closed` |
| `BUG-RECEIPT-0265` | `source_preservation` | Adding or accepting reviewed receipt photos could reset existing attachment read states to not-read. | `_acceptReviewedPhotoResult` preserved previous photo IDs but rebuilt every accepted photo read state as `ReceiptAttachmentReadState.notRead`. | Restore previous read states by normalized receipt photo path identity and only default newly added photos to not-read. | `test/receipt_camera_ocr_source_attachment_read_test.dart` | `closed` |
| `BUG-RECEIPT-0266` | `source_preservation` | OCR source quality badges and native warning diagnostics could be lost when OCR source paths differed only by normalization. | Attachment and capture-flow OCR handoff helpers read `photoQualityChecksByPath` and `captureDiagnosticsByPhotoPath` with raw source path keys. | Resolve OCR source quality and diagnostics by normalized receipt photo path identity before falling back to section index. | `test/receipt_camera_ocr_source_attachment_read_test.dart`; `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0267` | `source_preservation` | OCR scanner preparation document signals and risk flags could be lost when OCR source paths differed only by normalization. | Attachment and capture-flow scanner preparation helpers read `preparationDiagnosticsByOcrPath` with raw OCR source path keys. | Resolve preparation diagnostics by normalized receipt photo path identity before deriving scanner signals or risk flags. | `test/receipt_camera_ocr_source_attachment_read_test.dart`; `test/receipt_capture_flow_handoff_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0268` | `source_preservation` | User-facing camera copy could imply full-size original receipt photos are retained permanently. | Android, iOS, and shared receipt handoff labels said OCR reads the original photo/source first without clarifying that it is temporary and separate from the saved proof. | Reword OCR source copy to temporary full-quality photo/source and keep saved proof as the normal retained artifact. | `test/receipt_native_android_bridge_analysis_exposure_test.dart`; `test/receipt_native_ios_bridge_settings_close_test.dart` | `closed` |
| `BUG-RECEIPT-0269` | `camera_capture_quality` | Critical-but-decodable receipt photos could publish conflicting guidance about whether manual review/Next remained available. | Quality guidance said retake was safer and Next still worked, but `canContinueWithReview` returned false for all critical quality issues. | Treat decodable photos as manually reviewable while keeping retake guidance and auto-capture blocks for dark, glare, or very blurry photos. | `test/receipt_camera_quality_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0270` | `source_preservation` | Android camera status-strip copy could still imply permanent full-size original receipt retention. | The native settings dialog was corrected, but the Android status strip still said OCR reads the original first. | Reword the status strip to temporary full-quality OCR source language and add a source regression rejecting the stale original-first copy. | `test/receipt_native_android_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0271` | `qa_harness` | The broader Android native UI contract test still required retired original-first OCR copy. | Pass 783 corrected the status strip, but `receipt_native_android_bridge_ui_contract_test.dart` still asserted the stale phrase. | Update the contract test to require temporary full-quality OCR source copy and reject the retired original-first wording. | `test/receipt_native_android_bridge_ui_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0272` | `source_preservation` | Active camera handoff docs could still instruct future agents to preserve full original receipt photos as source truth. | Several active blueprint/service/handoff docs used old original-source language after the storage policy changed to temporary full-quality OCR source plus compressed saved proof by default. | Reword active docs to temporary OCR source and explicit opt-in original-quality proof retention, then pin the contract with active doc regressions. | `test/receipt_camera_release_one_blueprint_test.dart`; `test/receipt_active_camera_docs_focus_policy_test.dart` | `closed` |
| `BUG-RECEIPT-0273` | `source_preservation` | The saved-proof preview panel could still label the comparison size as an original photo. | The data-saver panel used "Original photo" for the source-size detail and "Original ..." in the footer, which could imply full original retention. | Reword the comparison labels to capture source size while keeping OCR-source-first and saved-proof-default language. | `test/receipt_camera_help_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0274` | `source_preservation` | Native capture staging could default omitted data-saver settings to original-quality retention. | `ReceiptNativeCaptureStaging.stage` defaulted `dataSaverLevel` to `ReceiptDataSaverLevel.original`, so a caller that forgot to pass settings could keep a full-size proof. | Default native staging to balanced saved proof and add a regression for no-argument staging. | `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0275` | `source_preservation` | Shared camera session policy still depended on a misleading original-first setting name. | The platform bridge keeps a legacy `ocrUsesOriginalFirst` key, but Dart session policy read that name directly after the product contract moved to temporary full-quality OCR source. | Add a correctly named temporary-source policy getter and route shared session protection through it while preserving bridge compatibility. | `test/receipt_native_camera_contract_test.dart` | `closed` |
| `BUG-RECEIPT-0276` | `source_preservation` | Native camera memory-policy diagnostics still described temporary OCR source handling as original-for-OCR. | The policy strings were internal diagnostics, not bridge compatibility keys, so the old wording could mislead future camera/storage work. | Rename native memory-policy outcomes to temporary-source-for-OCR while preserving Android/iOS diagnostic flow. | `test/receipt_native_camera_contract_test.dart`; `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_camera_storage_contract_test.dart`; `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0277` | `source_preservation` | Command Center OCR-start telemetry fixtures still reported `use_original_for_ocr`. | A shared telemetry fixture kept the old OCR storage outcome after production policy moved to clear source before saved proof. | Update the fixture to `ocr_clear_source_before_saved_proof_copy` and verify telemetry aggregation still passes. | `test/expense_screen_telemetry_parser_category_test.dart` | `closed` |
| `BUG-RECEIPT-0278` | `source_preservation` | OCR source-first handoff tokens still exposed original-source wording for temporary full-quality sources. | The user-facing cue was correct, but the decision code, relationship code, and review status token still said original source. | Rename the handoff/status tokens to temporary full-quality source wording and add source regressions against the stale token. | `test/receipt_photo_review_quality_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0279` | `source_preservation` | OCR source-first outcome tokens still kept `original_source_ready` after the decision token was renamed. | Pass 791 updated the main decision path but missed the outcome aggregation token and continuation action switch. | Rename the outcome token to `temporary_full_quality_ready` and reject the old source token in handoff regressions. | `test/receipt_photo_review_quality_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0280` | `qa_harness` | Native capture staging fixtures still showed retired tap-focus activity. | The fixture reported nonzero tap-focus counts and `lastFocusStatus: requested` even though native receipt camera code no longer increments tap focus. | Reset retired tap-focus fixture counts to zero and use continuous-focus status in staging, manifest, signal, and recovery-index expectations. | `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0281` | `barcode_qr_scanning` | Barcode/QR scanning could hand non-image or non-local paths to ML Kit. | The scanner service used generic receipt path normalization, which allowed relative paths and non-image absolute files. | Require local absolute image paths with receipt image extensions before invoking the barcode decoder. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0282` | `source_preservation` | OCR source quality-guard policy tokens still described temporary full-quality source handling as original-quality retention. | Pass 791-792 renamed the main OCR source handoff, but the scanner-prep storage outcome and risk flags still used original-quality guard wording. | Rename the internal storage outcome, risk flags, and privacy-safe scanner path label to temporary full-quality source guard wording while leaving explicit original-proof retention intact. | `test/receipt_camera_result_stitch_scanner_test.dart` | `closed` |
| `BUG-RECEIPT-0283` | `source_preservation` | Scanner decision codes and cleanup actions still described temporary full-quality source selection as original-source preservation. | The source-prep scanner emitted `ocr_source_original_selected_quality_guard` and `original_quality_preserved`, and downstream handoff counts/fixtures copied those names. | Rename scanner decision, cleanup, handoff count, telemetry, and OCR-source risk fixtures to temporary full-quality source guard wording while preserving explicit original-proof storage names. | `test/receipt_image_cleanup_settings_test.dart`; `test/receipt_image_ocr_source_guard_test.dart`; `test/receipt_camera_result_stitch_scanner_test.dart`; `test/expense_screen_telemetry_failure_source_workflow_test.dart`; `test/receipt_privacy_event_capture_handoff_test.dart`; `test/receipt_ocr_service_source_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0284` | `source_preservation` | Unreadable OCR source-prep cleanup actions implied the original image was used after missing-file or decode failure. | The failure reports kept stable scanner decision codes but named cleanup actions `source_file_unavailable_original_used` and `decode_failed_original_used`, which could be confused with original-proof retention. | Rename unreadable-source cleanup actions to no-clear-OCR-source wording and add regressions rejecting the stale `_original_used` action family. | `test/receipt_image_ocr_source_guard_test.dart` | `closed` |
| `BUG-RECEIPT-0285` | `receipt_line_numbering` | Privacy-safe receipt line review contracts did not expose OCR source section line numbers for long receipts. | Saved receipt line records exposed display labels and section numbers, but omitted the exact source section line from the privacy-safe contract used by review/redaction tooling. | Include safe OCR source line and section-line numbers in privacy-safe line review contracts while excluding malformed row numbers. | `test/expense_receipt_line_record_test.dart` | `closed` |
| `BUG-RECEIPT-0286` | `receipt_line_numbering` | Pre-save assisted receipt line drafts had no privacy-safe review contract matching saved receipt line records. | The saved ledger line exposed sanitized line-review anchors, but the entry draft line model only kept labels and raw OCR source fields before save. | Add a privacy-safe draft line review contract with sanitized OCR line and section-line anchors plus price-only/detail detection parity. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0287` | `receipt_line_review_mode` | Pre-save assisted receipt line contracts did not expose whether a line was price-only or detailed. | The draft privacy-safe contract added line anchors but omitted the saved-record `reviewMode` and business-use label fields. | Add draft `reviewMode` and business-use label contract fields with source regressions for price-only and detailed-line parity. | `test/expense_receipt_assisted_review_flow_test.dart` | `closed` |
| `BUG-RECEIPT-0288` | `ghost_overlap_stitching` | Manual Add Photo continuation could reopen the camera without a previous-section ghost overlay. | `ReceiptCaptureContinuationGuide.fromPreviousPhotos` returned inactive whenever no reason code was supplied, even if a valid prior receipt section photo existed. | Use a safe `manual_add_photo_continuation` reason when prior section photos exist without an OCR-specific reason, while keeping no-photo continuation inactive. | `test/receipt_capture_flow_shareability_test.dart` | `closed` |
| `BUG-RECEIPT-0289` | `native_bridge` | Devices without continuous focus had no explicit fallback-review policy in the native camera handoff. | The session exposed `continuousFocusExpected` and a focus strategy token, but downstream review had to infer whether live or saved-photo readability review was required. | Add a privacy-safe `focusReadabilityFallbackPolicy` to session arguments, native control diagnostics, and review health counts. | `test/receipt_native_camera_session_contract_test.dart`; `test/receipt_native_camera_service_basics_test.dart`; `test/receipt_camera_result_native_ui_health_test.dart` | `closed` |
| `BUG-RECEIPT-0290` | `native_bridge` | Older-device focus/readability fallback review could be counted but not marked as a receipt attachment risk. | Native UI risk classification looked for missing, unknown, or regressed tokens but did not classify `review_required` fallback policies as actionable risk flags. | Treat native camera `review_required` health tokens as risk flags in both shared-flow and attachment-panel native signal helpers. | `test/receipt_camera_result_native_ui_health_test.dart`; `test/receipt_capture_flow_native_handoff_health_test.dart` | `closed` |
| `BUG-RECEIPT-0291` | `barcode_qr_scanning` | Barcode/QR batch scanning could scan the same receipt image path more than once. | The batch scanner deduped decoded barcode values but not duplicate OCR-source image paths before calling the decoder. | Normalize and skip duplicate local image paths before ML Kit work, with a privacy-safe duplicate-image warning bucket. | `test/receipt_barcode_scanner_service_test.dart` | `closed` |
| `BUG-RECEIPT-0292` | `fixture_generation` | Damaged OCR fixture coverage did not include a true low-light/too-dark receipt source. | The synthetic damaged pack covered blur, glare, crop, and low contrast, but missed the dark-room capture class that should block OCR until the user adds light or uses torch guidance. | Add a low-light damaged receipt fixture with too-dark quality expectations and pack-focus regression coverage. | `test/receipt_qa_runner_pack_focus_test.dart` | `closed` |
| `BUG-RECEIPT-0293` | `qa_harness` | Damaged OCR fixtures still expected critical-but-decodable photos to block manual Next/review. | Older fixture expectations predated the policy that retake can be recommended while manual review/Next remains available for decodable receipt photos. | Update blurry, glare, and low-light damaged fixture expectations to require continue-with-review availability and current autofocus guidance copy. | `test/receipt_qa_runner_pack_focus_test.dart` | `closed` |
| `BUG-RECEIPT-0294` | `camera_capture_quality` | Low-light receipt behavior was covered through fixture scoring but not pinned directly at the quality-model boundary. | The direct quality regression covered glare and generic dark scoring, but did not assert that a too-dark receipt blocks auto capture while still allowing manual review/Next with torch guidance. | Add a low-light quality-model regression for manual review availability, auto-capture blocking, retake action code, and torch guidance. | `test/receipt_camera_quality_guidance_test.dart` | `closed` |
| `BUG-RECEIPT-0295` | `ocr_handoff_contract` | Generic saved photo quality warning tokens did not drive OCR source review family status. | The handoff summary counted `photo_quality_*` warning tokens, but dark/glare/blur review classifiers only recognized native saved-photo and OCR-source action tokens. | Classify saved photo quality warning tokens for dark exposure, glare, and blur so OCR diagnostics expose the right review action family. | `test/receipt_ocr_service_source_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0296` | `ocr_handoff_contract` | Parser/admin diagnostics did not summarize generic saved photo quality warning tokens into dark/glare/blur review buckets. | The source handoff contract could classify `photo_quality_*` warning tokens, but the derived diagnostics task counts still only recognized older native saved-photo and OCR-source action tokens. | Add generic photo-quality warning tokens to OCR diagnostics task-count mapping for dark exposure, glare, and blur review families. | `test/receipt_ocr_service_source_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0297` | `ocr_handoff_contract` | The generic photo-quality warning handoff fix only had direct dark exposure coverage. | Dark, glare, and blur shared the same warning-token classifier family, but only the too-dark branch had a focused service regression. | Add focused service regressions for glare and blur warning tokens so each source-quality review family is pinned. | `test/receipt_ocr_service_source_quality_test.dart` | `closed` |
| `BUG-RECEIPT-0298` | `ocr_handoff_contract` | Dirty lens/hazy saved-photo warnings did not have a first-class OCR source quality review family. | Native camera warnings produced `saved_photo_dirty_lens_or_haze` and `wipe_lens_or_retake`, but source handoff status/action logic did not classify that family separately. | Add `saved_hazy_lens_review` with `wipe_lens_or_retake` action and diagnostics task-count mapping. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0299` | `camera_review_state` | Dirty lens/hazy saved-photo warnings could be flattened in receipt review handoff copy. | The warning-profile priority list and handoff action label did not include `saved_photo_dirty_lens_or_haze`, even though native warnings and OCR handoff could classify it. | Add dirty-lens warning profile selection and wipe-lens handoff copy with result-level regression coverage. | `test/receipt_camera_result_saved_photo_warning_test.dart` | `closed` |
| `BUG-RECEIPT-0300` | `camera_capture_quality` | Live `shadow_risk` guidance did not survive as a named saved-photo warning after capture. | Native live analysis could tell the user that the receipt had heavy shadows, but saved-photo review did not have a shadow warning/action family for post-capture review, OCR handoff, or diagnostics. | Add `saved_photo_shadow_risk`, `saved_shadow_review`, `move_to_even_light_or_retake`, and parser/admin task-count mapping with focused regressions. | `test/receipt_camera_saved_photo_warning_diagnostics_test.dart`; `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0301` | `ocr_handoff_contract` | Invalid stitch OCR source contracts could look ready at the photo-review result boundary. | `ocrSourceReviewRiskCode` only checked missing OCR sources, saved-proof fallback, and scanner prep, so a stitched source mismatch could reach later handoff layers without requiring manual review. | Treat invalid stitch OCR contracts as `stitch_ocr_source_contract_review_required` and require manual review before saving receipt details. | `test/receipt_ocr_source_relationship_test.dart`; `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0302` | `ocr_handoff_contract` | Result-level OCR source paths could disagree with the embedded stitch contract while downstream signals only saw a valid stitch object. | `ReceiptStitchResult` could be internally valid, but `ReceiptPhotoReviewResult.ocrSourcePhotoPaths` could point at a different OCR artifact. | Add `ocrSourcePathsMatchStitchContract`, expose it in privacy-safe summary, and route mismatches to manual stitch review. | `test/receipt_ocr_source_relationship_test.dart`; `test/receipt_camera_ocr_source_handoff_test.dart` | `closed` |
| `BUG-RECEIPT-0303` | `camera_capture_quality` | Native camera could show experimental blur/glare/shadow/dirty-lens quality guidance even when those warnings were disabled. | Unknown live readability samples were tied to receipt visibility guidance instead of the experimental quality-warning toggles, so users could see unproven guidance during normal capture. | Gate experimental receipt-quality guidance behind `hasExperimentalReceiptQualityWarningsEnabled` on Android and iOS. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0304` | `camera_capture_quality` | Native shadow guidance could stay on screen after live conditions recovered. | The defensive reset recognized `Receipt has heavy shadows`, but the normal healthy-frame recovery branch did not clear that same string. | Clear stale heavy-shadow guidance in both Android and iOS live-readability recovery paths. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart` | `closed` |
| `BUG-RECEIPT-0305` | `camera_diagnostics` | Diagnostics could not tell whether a live quality warning came from opt-in experimental guidance or neutral receipt workflow guidance. | Native payloads stored individual warning toggles but did not carry a single privacy-safe contract flag for experimental receipt-quality guidance state. | Add `experimentalReceiptQualityWarningsEnabled` to Android/iOS diagnostics and preserve it through native capture staging. | `test/receipt_native_android_bridge_settings_quality_test.dart`; `test/receipt_native_ios_bridge_analysis_exposure_test.dart`; `test/receipt_native_capture_staging_test.dart` | `closed` |
| `BUG-RECEIPT-0306` | `ocr_handoff_contract` | Stitch contract review risks could be visible in quality-review status while source-review risk stayed unknown. | `ReceiptOcrSourceHandoffSummary.sourceReviewRiskStatus` did not include the stitch-contract review family and only classified fallback, scanner-prep, missing-source, or ready states. | Add stitch-contract review to source-review risk status and pin the invalid-stitch service regression. | `test/receipt_ocr_service_test.dart` | `closed` |
| `BUG-RECEIPT-0307` | `camera_docs` | Active native camera service docs still described live steadiness and dark/glare warnings as completed default guidance. | The implementation contract moved unproven blur/glare/shadow/dirty-lens/steadiness claims behind experimental toggles, but the active service spec still instructed future agents to warn by default. | Reword active camera docs so release-one guidance is neutral by default and experimental quality warnings stay gated. | `test/receipt_camera_release_control_priority_test.dart`; `test/receipt_active_camera_docs_focus_policy_test.dart` | `closed` |
