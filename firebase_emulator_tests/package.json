{
  "name": "maintainiac-firebase-emulator-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Emulator-only Firebase rules and security contract tests for Maintainiac.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:emulator": "firebase emulators:exec --project demo-maintainiac-rules-test --only firestore \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.6.0",
    "firebase-tools": "^15.22.0"
  },
  "engines": {
    "node": ">=20"
  }
}
