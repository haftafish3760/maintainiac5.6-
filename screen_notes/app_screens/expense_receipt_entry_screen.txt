Screen: lib/screens/expenses/entry/expense_receipt_entry_screen.dart

Source: visible user message, 2026-06-20 Batch 1

"Don't get rid of any placeholder stuff yet, because I think I may actually
like some of that placeholder text, like on the dashboard that we have right
now. When I tap fuel, it actually pulls up a screen all about my fuel stuff. I
love that part of it."

Source: /Users/rbbie/.codex/attachments/0033c4ad-9f3e-426c-bd00-8fbc98c13b44/pasted-text.txt

"I've also considered integrating Google Vision for receipt stuff, but as long
as we have a good parser, and I can tell you now that we have or are supposed
to have a really good camera system in here, and if it isn't, we will harden
that as well to make sure that is state-of-the-art camera system to help the
user actually get good pictures."

Source: visible user message, 2026-07-11 Batch 2

- Keep the back button on every secondary receipt screen and place the receipt
  screen title on the same top row as Back.
- Remove the generic "Record what was spent" and business/personal helper copy
  below the receipt title.
- Directly above the active-vehicle selector, tell the individual user to
  select which vehicle the expense belongs to.
- The individual mode may have multiple vehicles but does not show fleet or
  employee controls. Fleet mode remains future-facing.
- Receipt drafts belong on Expense Home. Add Expense starts a new entry and
  must not show a draft-resumption panel inside the new receipt flow.
- Expense Home should present one professional Receipt Drafts section. Draft
  rows show useful metadata such as receipt date, saved date, merchant, amount,
  photo count, or review status without user-facing "interrupted" terminology.
- Keep the active-vehicle row pinned above scrolling Expense Home and Calendar
  content. This pinned behavior is an app-wide direction for other sections.
- Move the Daily Expenses date/vehicle context block from Expense Home into the
  Expense Calendar day view.

Source: visible user message, 2026-07-12

- Receipt-camera, review, OCR-progress, compression, recovery, and related
  expense UI must be designed for English and U.S. Spanish from the start.
  Use localizable UI copy rather than hard-coded English so Spanish support
  does not become a later rewrite. Receipt evidence and user-entered merchant
  text remain unchanged; only app-controlled UI is translated.

Source: visible user messages, 2026-07-26 Receipt entry refactor

- After photo review, show one concise Extracting Receipt Information surface,
  then reveal the selected editable receipt form rather than developer-style
  pipeline cards.
- The detailed form begins with OCR-suggested merchant/contact information and
  presents numbered printed item rows with description, quantity, unit price,
  line total, classification, and category controls. The user confirms all
  suggestions before saving.
- Total Only and Category Summary intentionally hide item rows. Detailed Items
  always provides Add Item, including when OCR returns no usable text.
- Failed or timed-out OCR keeps the receipt proof and opens the same form for
  manual completion instead of leaving an indefinite spinner.

Source: visible user message, 2026-07-26 Manual receipt layout polish

- The manual Details step must not lead with generic helper copy. Its first
  action is a compact bordered Odometer card using concise instructional copy:
  "Enter your current odometer to add it to the receipt."
- Details, Items, and Review keep the pinned vehicle header and each header gear
  opens settings titled for the exact current step. Step indicators use compact
  squared tabs, not social-media-style pill containers.
